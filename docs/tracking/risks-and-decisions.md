# Risks, Future Enhancements & Decision Log

## Risk Analysis

| Risk | Impact | Mitigation |
|------|--------|------------|
| Zod 4 crash | Server won't start | `import { z } from "zod/v3"` from day 1 |
| C++ plugin compile errors | No custom tools | Phase 3 is independent — server works with Phases 1-2 complete |
| RC API not enabled in Project B | 8 RC tools fail | RC tools return helpful error; 106 other tools still work |
| TCP 55558 port collision | Custom plugin unreachable | Configurable port via console variable |
| PowerShell blocked by policy | Auto-detection fails | Falls back to WMIC → manual UNREAL_PROJECT_ROOT |
| Project B team confused by new plugin | Team friction | Communicate via Confluence before submitting to P4 |
| Large actor lists exceed MCP limits | Truncated data | Pagination/limit params on list tools |
| GAS module not available in Project B | GAS tools fail | Conditional compilation: `#if WITH_GAMEPLAY_ABILITIES` |
| Hot reload breaks TCP thread | Plugin crash | FRunnable stop flag + socket cleanup in Deinitialize |
| `find_tools` returns wrong toolset | Claude enables irrelevant tools, wasting context | Weighted scoring with coverage bonus; auto-enable only top-scoring toolsets (not all partial matches). Manual `disable_toolset` as escape hatch. Tunable via alias map updates. |
| `tools/list_changed` not supported by client | Claude doesn't see newly enabled tools | Fallback: `find_tools` response includes full tool schemas for matches, usable even if client ignores notification. Document which MCP clients support dynamic tool lists. |
| Toolset auto-enable confusion | Claude enables too many toolsets, approaching tool limit | `list_toolsets` shows active count and warns when approaching 40-tool threshold. `disable_toolset` available. `find_tools` caps auto-enable at top 3 matching toolsets per query. |
| Alias map gaps | `find_tools` misses obvious queries | Ship with conservative alias set (18 entries). Expand per-project in Phase 6 step 41. Alias map is a flat JS Map — trivial to add entries without code changes. |
| D24 ad-hoc error detection fragile | UMG handler adds extra field to error response → false negative (error looks like success) | Current guard checks `resultKeys.length === 1`. All 18 UMG error sites return `{error: "msg"}` only — safe today. Phase 3 fix: normalize error format in C++ plugin (use CommonUtils::CreateErrorResponse consistently). If relaxing the JS guard, risk false positives on legitimate responses with informational `error` fields. |
| Base64 image exceeds stdio limit | `visual-capture` tools crash server | Enforce max resolution cap (1024×1024 default). JPEG fallback for screenshots. Warn in tool description about ~1MB stdio payload limit. |

### Audit-Discovered Risks (April 2026)

Surfaced during plan audit. Dispositions: **IMPLEMENT** (budgeted), **DEFER** (add if needed), **SKIP** (not worth the cost).

| Risk | Severity | Disposition | Mitigation |
|------|----------|-------------|------------|
| `run_python_command` arbitrary code execution | CRITICAL | **IMPLEMENT Phase 3** | Deny-list (`os.`, `subprocess`, `eval`, `exec`, `open`, `__import__`), confirmation dialog, execution logging. See D14. ~1.5 hrs. |
| Project B plugin distribution undefined | CRITICAL | **IMPLEMENT Phase 5** | PowerShell `sync-uemcp-plugins.ps1` copies git plugin → both project Plugins/ folders → P4 submit. Confluence page for team. ~1 hr. |
| `delete_asset` ignores dependency graph | WARNING | **IMPLEMENT Phase 3** | Query `IAssetRegistry::GetReferencers()` before delete. Refuse if hard refs exist unless `force=true`. Return referrer list. ~3 hrs. |
| TCP command race on shared socket | WARNING | **IMPLEMENT Phase 1** | Command queue: TCP thread enqueues (FCriticalSection), game thread dequeues one per tick. Node.js serializes outbound. ~2 hrs. |
| TCP timeout retry causes duplicate ops | WARNING | **IMPLEMENT Phase 1** | Request ID (SHA256 of command+params). Server + plugin cache results 5 min. Write ops deduplicated; reads are naturally idempotent. Included in command queue work. |
| TCP reconnection on editor close/reopen | WARNING | **IMPLEMENT Phase 2** | Simple retry-on-failure in ConnectionManager. Try connect, if fail, retry on next tool call. No exponential backoff needed for localhost. ~1.5 hrs. |
| Plugin dependency failures silent | WARNING | **IMPLEMENT Phase 1** | `list_toolsets` reports unavailable toolsets with reason + fix instruction. Query enabled plugins via TCP handshake. ~1 hr. |
| RC API port hardcoded to 30010 | WARNING | **DEFER** | Add `UNREAL_RC_PORT` env var override in .mcp.json. Default 30010. Port scan and config parsing deferred — not needed for known setups. ~0.5 hrs if triggered. |
| Reflected property writes without type validation | WARNING | **DEFER** | RC API does its own type coercion and returns errors. Wrap errors in clean messages. Full pre-validation cache deferred until RC API errors prove insufficient in practice. |
| No rate limiting on MCP requests | WARNING | **SKIP** | Claude sends one tool call at a time. Game thread naturally serializes. Not a real problem on localhost. Revisit only if editor hangs observed. |
| Concurrent RC API + TCP commands race | INFO | **SKIP** | Different transport layers, different threads. No shared state between HTTP and TCP paths. |
| No undo for property writes | INFO | **DEFER** | Snapshot-before-write undo queue is ~35 hrs for marginal value. Use editor Ctrl+Z instead. Revisit if bulk property writes become common. |

### UE5 API Verification (April 2026)

All 6 critical APIs confirmed present in UE5.6. No plan changes needed.

| API | Status | Notes |
|-----|--------|-------|
| `FTcpListener` | CONFIRMED | FRunnable-based, FTcpSocketBuilder fluent API. Networking module. |
| `IAssetRegistry::GetReferencers/GetDependencies` | CONFIRMED | Multiple overloads with dependency type filtering. AssetRegistry module. |
| `FRemoteControlModule` | CONFIRMED | HTTP + WebSocket, configurable ports. RemoteControl module. |
| `UEdGraph` / `UK2Node` | CONFIRMED | Full graph traversal via Nodes array + pin connections. BlueprintGraph module. |
| `GameplayAbilities` module | CONFIRMED | Plugin-based, enabled via .uproject. Correct module name. |
| `FKismetEditorUtilities::CompileBlueprint` | CONFIRMED | Delegates to FBlueprintCompilationManager. UnrealEd module. |

---

## Future Enhancements (Not in Initial Build)

1. **WebSocket Subscriptions** — RC API supports WebSocket for property change notifications. Could enable "watch" mode for live debugging.

2. **Niagara System Editing** — Create/modify particle systems. Requires Niagara editor module dependency.

3. **Physics Asset Editing** — Create/modify physics bodies and constraints. Requires PhysicsAssetUtils.

4. **Landscape Editing** — Height modification, material layers. Very specialized.

5. **Multi-Editor Port Negotiation** — If both editors bind 55557, the second fails. Could add port negotiation protocol.

6. **Wwise Integration** — Create/manage AkAudioEvent assets. Requires Wwise SDK in plugin dependencies.

7. **Sequencer/Level Sequence Editing** — Create/modify level sequences for cinematics. Requires Sequencer module deep integration.

8. **Behavior Tree Introspection** — Serialize UBehaviorTree assets (BTTask/BTDecorator/BTService nodes). Not currently used in our target projects but useful for AI-heavy projects.

9. **Control Rig Graph Introspection** — Serialize UControlRig animation rigging graphs. Not currently used in our target projects.

10. **MCP Resources** — Expose project metadata, asset catalogs, and build outputs as read-only MCP resources with URI-based access and subscriptions. Would enable efficient caching and Claude accessing project data without tool calls. High value, low effort. **Status: Deferred to Phase 1 end as optional polish (D21). Verify client support first.**

11. **MCP Prompts** — Define reusable prompt templates for common Unreal workflows ("create a GAS ability for X", "set up a combo chain with N hits"). Would standardize quality of Claude's output for repeated patterns. **Status: Deferred to Phase 4+ (D22). Depends on working tools.**

12. **MCP Logging** — Stream Unreal Editor diagnostic output (compilation, PIE, packaging) to Claude's UI with severity levels. Better visibility than text-only tool responses. **Status: Basic `logging: {}` capability included in Phase 1 (D19). Editor-specific log streaming deferred.**

13. **Asset Import Pipeline** — `import_static_mesh` and `import_skeletal_mesh` tools wrapping UE's FBX import. Would bridge Blender MCP output → Unreal, enabling model-in-Blender → import-to-UE workflows without manual editor interaction.

14. **Perforce Auto-Checkout** — Wrap `p4 edit` in a UEMCP tool that auto-checks out files before asset creation/modification. Eliminates bash context-switching during create workflows. Could be as simple as the server calling `p4 edit` before sending the create command to the plugin.

15. **Collision & Physics Toolset** — Physics material editing, collision preset management, constraint configuration. The primary target uses physics extensively but has no dedicated toolset.

16. **Build/Cook/Package Automation** — Editor-side build triggering, cook status monitoring, package configuration. Important for CI/CD but not blocking for daily dev workflow.

17. **Skeletal Mesh & LOD Management** — Bone/socket manipulation, LOD configuration, physics asset bodies. Useful for character pipeline but low priority vs. GAS workflow.

---

## Decision Log

| # | Decision | Rationale |
|---|----------|-----------|
| D1 | Node.js server | Consistency with 3 other MCP servers. Same SDK. Same `zod/v3` import pattern. |
| D2 | New C++ plugin, not modify existing | Project B team shares existing via P4. Zero disruption. |
| D3 | Auto-detection via process inspection | 95%+ reliability. No editor dependency. Works even if plugins crashed. |
| D4 | Separate TCP port (55558) | Avoids conflict with existing plugin (55557). Both run simultaneously. |
| D5 | Dynamic toolsets with progressive disclosure | 114 tools across 15 toolsets, loaded on demand. 6 always-loaded management tools. Hybrid of GitHub MCP Server's explicit toolsets and Speakeasy's progressive disclosure. Keeps active tools at 15-30 per task. |
| D6 | Leave old Python servers | May be tracked in P4. No conflicts. Clean up later with team coordination. |
| D7 | Full feature parity both projects | User requirement. Same plugin, same server, same tools. |
| D8 | GAS commands as highest priority | The primary target is a GAS-heavy combat game. Most impactful for daily workflow. |
| D9 | RC Components plugin enabled | Data-driven preset system. Free capability with no downside. |
| D10 | Python Editor Script Plugin enabled | Enables `run_python_command` tool. Full `unreal` Python module access. Low-cost, high flexibility. |
| D11 | 6-phase implementation | C++ plugin is independent phase. Server works without it (Phases 1-2-4). |
| D12 | Geometry Script plugin enabled | Procedural mesh generation, CSG booleans, UV manipulation. Adds 4 geometry tools. |
| D13 | Slate Scripting not enabled | Read-only editor UI inspection — can't construct UI. UMG tools already handle UI creation. |
| D14 | `run_python_command` ships with deny-list + confirmation | Balances flexibility with safety. Deny-list blocks `os`, `subprocess`, `eval`, `exec`, `open`, `__import__`. Confirmation dialog required. All executions logged. Full sandboxing (Tier 2) deferred — overkill for solo dev on localhost. |
| D15 | Project B distribution via P4 (Option A) | Team benefits from expanded tools. Script copies git → P4. Matches existing UnrealMCP distribution pattern. Confluence page documents installation. |
| D16 | Defer RC API port discovery, rate limiting, property pre-validation, undo system | Over-engineered for solo dev on localhost. RC API validates types internally. Claude sends one call at a time. Editor Ctrl+Z covers undo. Revisit if real problems emerge during daily use. |
| D17 | Plain `.mjs` over TypeScript for MCP server | Matches existing MCP servers (jira-bridge, perforce, miro). No build step — edit and restart. Zod provides runtime validation where it matters (tool params). Trade-off: no IDE autocomplete on internal functions. Worth it for iteration speed with AI-assisted development. |
| D18 | Develop in `UEMCP/server/`, point `.mcp.json` at repo path | Avoids copy-to-`~/.claude/` friction during development. `.mcp.json` points directly at `D:/DevTools/UEMCP/server/server.mjs`. Defer copying to `~/.claude/` until distribution (Phase 5) if needed at all. |
| D19 | Include `logging: {}` capability from Phase 1 | Near-zero cost (~30 min). Enables `ctx.mcpReq.log()` in tool handlers for debugging auto-detection, TCP connections, toolset operations. Works with MCP Inspector. If Claude doesn't render logs, costs nothing. |
| D20 | Include server `instructions` string from Phase 1 | 15-minute effort. Guides Claude through `find_tools` → `enable_toolset` → use tools workflow. High value for correct dynamic toolset usage. |
| D21 | Defer MCP Resources to Phase 1 end (optional polish) | Resources duplicate management tools (`list_toolsets`, `detect_project`). Client support uncertain — Claude may not surface resources in UI. ~2 hrs effort. Add only after verifying client consumption. |
| D22 | Defer MCP Prompts to Phase 4+ | Prompt templates depend on working tools existing first. ~3-4 hrs effort for quality templates. Same outcome achievable through tool descriptions and `find_tools` response content. |
| D23 | UEMCP absorbs all tools onto TCP:55558; deprecate UnrealMCP dependency post-Phase 3 | Phase 2 uses existing UnrealMCP (TCP:55557) as a **conformance oracle** — validates TCP transport, command serialization, and tool contracts. Phase 3 reimplements those 26 tools (actors, blueprints-write, widgets) in the custom C++ plugin on TCP:55558, fixing issues found in the existing implementations and adding functionality beyond what UnrealMCP provides. Post-Phase 3: remap layer assignments in tools.yaml from `tcp-55557` to `tcp-55558`, remove TCP:55557 layer from ConnectionManager, drop UnrealMCP dependency entirely. Result: one plugin, one port, one codebase. Layer assignments for `actors`, `blueprints-write`, and `widgets` in tools.yaml are **transitional** — they read `tcp-55557` now because that's what serves them during Phase 2, but will flip to `tcp-55558` when Phase 3 absorbs them. |
| D24 | Handle UMG ad-hoc error format in ConnectionManager.send() | Oracle research (Section 4.4–4.6) found 3 UMG handlers return `{"error": "msg"}` without `"success": false`. The Bridge doesn't catch these — they arrive as `{"status": "success", "result": {"error": "msg"}}`. ConnectionManager.send() must check for `result.error` field even on successful status and throw. This is a universal fix, not per-tool. |
| D25 | Tool name → type string translation lives in TCP tool handlers, not ConnectionManager | tools.yaml uses shortened names (e.g., `get_actors` → `get_actors_in_level`). Translation is tool-layer concern, not transport-layer. Each tool handler maps its name to the C++ type string. ConnectionManager stays name-agnostic. |
| D26 | Drop `create_input_mapping` — legacy input, not worth exposing | Oracle Section 5.1: uses `FInputActionKeyMapping` (legacy input, not Enhanced Input). UEMCP already plans Enhanced Input tools in `input-and-pie` toolset for Phase 3. No tools.yaml entry needed. |
| D27 | Drop `add_widget_to_viewport` or mark as informational | Oracle Section 4.3: the C++ handler is a no-op that returns "use Blueprint nodes instead." Either remove from tools.yaml or rename to `get_widget_class_path` with description noting it's informational only. |
| D28 | `bDetailed` actor serialization is a Phase 3 enhancement | Oracle Section 6.4: `ActorToJsonObject(bDetailed)` never branches on the param — both modes return identical output. Phase 2 passes it through as-is. Phase 3 reimplementation adds component list, tag list, and display label when detailed=true. |
| D29 | Actor name-or-label resolution is a Phase 3 UEMCP plugin feature | Existing UnrealMCP uses `Actor->GetName()` (internal FName like `BP_OSControlPoint_C_0`) for all lookups. Users expect to use Outliner display names (Actor Labels like `BP_OSControlPoint2`). Phase 3 plugin adds: (1) `label` field to all actor JSON responses via `GetActorLabel()`, (2) `FindActorByNameOrLabel()` helper — tries FName first, falls back to label match, (3) `find_actors_by_name` matches against both name and label. Not worth modifying the conformance oracle (UnrealMCP) since it's deprecated post-Phase 3. |
| D30 | Offline-first sequencing — any tool identified as belonging offline is built before further TCP work (supersedes original Phase 2 → Phase 3 ordering) | Source: `docs/audits/offline-tool-expansion-audit-2026-04-13.md` §7.1. The CLI-agent-usefulness lens (D17 philosophy applied to tool surface) revealed that several planned TCP:55558 tools actually belong offline — they parse file-shaped data (binary registries, `.umap`/`.uasset` headers, CSV DataTables) with no need for a live `UEngine`. Building them on TCP first would be throwaway work. Policy: when a tool is identified as belonging offline, it becomes critical path before more TCP scope lands. Consequence: Phase 2's remaining TCP work pauses until `query_asset_registry` + Phase 1.5 cleanup (D31) + DataTable/StringTable readers + `inspect_blueprint`/`list_level_actors` land. Phase 3 scope then re-enters reduced (see D32). |
| D31 | Phase 1.5 cleanup — drop 3 offline tools redundant with Claude Code built-ins; reframe 1 around asset registry | Source: `docs/audits/offline-tool-expansion-audit-2026-04-13.md` §5, §7.3. Tools that wrap `Read`/`Grep`/`Glob` with a Zod schema do not earn their place in the 40-tool dynamic-toolset budget — they reduce ToolIndex precision, add `tools/list` noise, and invite CLI agents to prefer the wrapper over the native tool. **Drop**: `read_source_file` (inferior to `Read`), `search_source` (inferior to `Grep`), `browse_content` (equivalent to `Glob`). **Reframe**: `get_asset_info` — drop the current `fs.stat()`-based implementation, reserve the name, reimplement as "given an asset path, return all Asset Registry metadata" once `query_asset_registry` lands. `SERVER_INSTRUCTIONS` gains a section orienting CLI agents toward `Read`/`Grep`/`Glob` for generic file operations and UEMCP offline tools for UE-specific parsing. The "safety rail" framing used when these tools were originally built was wrong — they predate our full understanding of Claude Code's native capabilities and are redundant, not defensive. |
| D32 | Phase 3 TCP scope reduction — registry-backed tools move offline permanently (not reimplemented on TCP:55558) | Source: `docs/audits/offline-tool-expansion-audit-2026-04-13.md` §2.2, §7.1. `query_asset_registry` (offline, binary parser over `DevelopmentAssetRegistry.bin`) subsumes: `search_assets`, `get_class_hierarchy`, `get_asset_references`, `get_asset_dependencies`, `find_blueprints_implementing_interface`, and reframed `get_asset_info`. These tools are removed from the Phase 3 TCP:55558 scope in `tools.yaml`. `inspect_blueprint` (offline, `.uasset` export-table walker) covers Blueprint structural introspection — Phase 3 TCP scope for variable/function/interface enumeration is reduced accordingly. Stale-registry mitigation documented in D33. D23 is unaffected — UEMCP still absorbs the existing UnrealMCP TCP:55557 tools (actors, blueprints-write, widgets); only the registry-backed tools leave the TCP plane entirely. |
| D33 | Asset index freshness uses header-scan + in-memory cache; invalidation via hybrid TTL + mtime/size diff + write-suspicion flag (Option D); no user-facing `allow_stale` knob | **REVISED 2026-04-13 after Agent 1 verification.** Original decision assumed a `DevelopmentAssetRegistry.bin` binary parser. Verification found Project B has never been cooked (no registry exists) and Project A's registry is routinely hours-to-days stale. Approach superseded by header scanning: every `.uasset` embeds an `FAssetRegistryData` block (the source from which the registry cache itself is built) — parsing headers directly achieves parity on 4 of 5 use cases and works without a cook step. Source: `docs/audits/offline-tool-expansion-audit-2026-04-13.md` §7.2 (superseded — see Amendment A). **Freshness model now:** in-memory `assetCache` keyed by path, holding parsed header data + mtime + sizeBytes. Per-file invalidation via `shouldRescan(cacheEntry, fsMtimeMs, fsSizeBytes, {indexDirty})` in `server/offline-tools.mjs`: (a) missing entry → parse; (b) `indexDirty` set → re-parse (covers Windows/Node coarse mtime rounding blind spot — stat reports 1-2s resolution despite NTFS's 100ns); (c) disk mtime advanced → parse; (d) equal mtime + different size → parse (secondary signal for same-second saves); (e) else → cache hit. **Bulk query TTL:** 60s sweep budget. On expiry, readdir+stat the asset tree, re-parse deltas only (no full re-parse). Response metadata reports `files_added_since_last_sweep` and `files_removed_since_last_sweep` for transparency, no threshold gate — the sweep is cheap enough (~1s for 10k files on SSD) that automatic refresh replaces the explicit-opt-in `allow_stale` flag. **Cross-layer coupling (new requirement):** Phase 3 TCP write-ops must `import { assetCache } from './offline-tools.mjs'` and set `assetCache.indexDirty = true` after any write. This is the only mechanism covering the coarse-mtime blind spot for writes our own server performs. Documented in the header comments at the top of `assetCache` definition. **Dropped from original D33:** time-based staleness warnings, P4 `fstat headModTime` fallback (no longer relevant — we're not comparing against a registry snapshot), `allow_stale` parameter. |
| D34 | Two-track parallel dispatch: Agent 2 (TCP P0 remediation, server-side wraps only) + Agent 3 (offline expansion + D31 cleanup + `.uasset` parser research) run concurrently with non-overlapping file scopes | Source: `docs/handoffs/agent2-tcp-p0-remediation.md`, `docs/handoffs/agent3-offline-expansion.md`. Handoffs finalized 2026-04-14; both committed to repo so agents read a stable snapshot rather than chat-thread prose. **Scope split (no overlap):** Agent 2 touches `connection-manager.mjs`, `tcp-tools.mjs`, `test-tcp-tools.mjs`, `test-helpers.mjs`, and creates `docs/specs/phase3-plugin-design-inputs.md`. Agent 3 touches `offline-tools.mjs`, `tools.yaml`, `test-phase1.mjs`, `server.mjs` (SERVER_INSTRUCTIONS only), and creates `docs/research/uasset-parser-options-2026-04-14.md`. **Dispatch order:** Agent 3 first (Track 2c parser research is the only decision gate in either track — earlier landing unblocks items 1/3/4), Agent 2 a few minutes later. **D-log slot allocation:** this entry (D34) documents the orchestration decision; Agent 2 writes D35 summarizing the server-patchable vs plugin-only scope split per the locked P0 classification table. **Classification locked at dispatch** (not "proposed, confirm on first step"): P0-1/7/9/10 server-patchable, P0-2/3/4/5/6/8/11 plugin-only → captured as Phase 3 design inputs, UnrealMCP C++ untouched per D23. **Conflict tiebreaker** if mid-work scope overlap surfaces: Agent 2 yields to Agent 3 on production code (Agent 3's scope is longer with external dependencies — parser research — so delaying it compounds worse). **Carve-out:** `test-helpers.mjs` uses first-touch-wins, since it's a named-export registry per `CLAUDE.md` ("Shared infrastructure — not a runner") and concurrent additions produce trivially-mergeable diffs. In both cases the yielding/second agent logs the conflict in its final report so the orchestrator can verify nothing was silently reverted. **Amendment convention:** if either agent discovers new audit findings mid-work, use the Amendment A blockquote pattern established in `offline-tool-expansion-audit-2026-04-13.md` §7.2 rather than editing audit bodies in place. |
| D35 | Server defends at wire boundary for 4 of 11 P0s; Phase 3 plugin design absorbs the other 7 + residues; UnrealMCP C++ untouched per D23 | Source: `docs/handoffs/agent2-tcp-p0-remediation.md`, audit `docs/audits/unrealmcp-comprehensive-audit-2026-04-12.md`. Agent 2 landed at commit `0a4f1fc`. **Server-patchable (landed)**: P0-1 via `extractWireError()` in `connection-manager.mjs` (normalizes Bridge envelope / CommonUtils / UMG ad-hoc + sibling-error leaks + raw escapes, with multi-key false-positive guard); P0-7 via `stripDoubledAssetSuffix()` in `tcp-tools.mjs` widgets execute (collapses self-doubled `X.X` widget asset paths only — `X.Y` and plain `X` untouched); P0-9 + P0-10 via `z.object(def.schema).parse(args)` defense-in-depth at entry of all 3 execute functions (required-field + Vec3 length-3 enforced before wire dispatch). **Plugin-only (deferred to Phase 3 custom plugin on TCP:55558)**: P0-2 actor lookup across all loaded levels (3A INF-1 + 3B), P0-3 name-or-label resolution (3A + 3B), P0-4 property-handler registry for FStruct/FObject/FArray (3A INF-6), P0-5 compile error reporting via `FCompilerResultsLog` (3C), P0-6 `FScopedTransaction` for graph edits (3C), P0-8 valid binding-graph construction (3D, depends on P0-11), P0-11 pin-type validation before `MakeLinkTo` (3C). **Residues retained as defense-in-depth**: server wraps for P0-1/7/9/10 stay in place at least one release after TCP:55557 retirement to handle other MCP clients targeting 55558 directly. Phase 3 ordering: 3A infrastructure cluster (P0-1 envelope + INF-1 lookup helper + INF-2 transform parser + INF-6 property registry) lands first since 3B/3C/3D all depend on it. **Test artifacts**: `test-tcp-tools.mjs` 218 → 234 assertions (Groups 22 P0-1 expanded / 23 P0-7 widget path / 24 P0-9 required / 25 P0-10 vec shape). Full per-P0 specs with 5-field template (current behavior / required behavior / wire-protocol implications / test case / subsystem bucket) live in `docs/specs/phase3-plugin-design-inputs.md`. |
| D35 | Server defends at wire boundary for 4 of 11 P0s; Phase 3 plugin design absorbs the other 7 + residues; UnrealMCP C++ untouched per D23 | Source: `docs/handoffs/agent2-tcp-p0-remediation.md`, audit `docs/audits/unrealmcp-comprehensive-audit-2026-04-12.md`. Agent 2 landed at commit `0a4f1fc`. **Server-patchable (landed)**: P0-1 via `extractWireError()` in `connection-manager.mjs` (normalizes Bridge envelope / CommonUtils / UMG ad-hoc + sibling-error leaks + raw escapes, with multi-key false-positive guard); P0-7 via `stripDoubledAssetSuffix()` in `tcp-tools.mjs` widgets execute (collapses self-doubled `X.X` widget asset paths only — `X.Y` and plain `X` untouched); P0-9 + P0-10 via `z.object(def.schema).parse(args)` defense-in-depth at entry of all 3 execute functions (required-field + Vec3 length-3 enforced before wire dispatch). **Plugin-only (deferred to Phase 3 custom plugin on TCP:55558)**: P0-2 actor lookup across all loaded levels (3A INF-1 + 3B), P0-3 name-or-label resolution (3A + 3B), P0-4 property-handler registry for FStruct/FObject/FArray (3A INF-6), P0-5 compile error reporting via `FCompilerResultsLog` (3C), P0-6 `FScopedTransaction` for graph edits (3C), P0-8 valid binding-graph construction (3D, depends on P0-11), P0-11 pin-type validation before `MakeLinkTo` (3C). **Residues retained as defense-in-depth**: server wraps for P0-1/7/9/10 stay in place at least one release after TCP:55557 retirement to handle other MCP clients targeting 55558 directly. Phase 3 ordering: 3A infrastructure cluster (P0-1 envelope + INF-1 lookup helper + INF-2 transform parser + INF-6 property registry) lands first since 3B/3C/3D all depend on it. **Test artifacts**: `test-tcp-tools.mjs` 218 → 234 assertions (Groups 22 P0-1 expanded / 23 P0-7 widget path / 24 P0-9 required / 25 P0-10 vec shape). Full per-P0 specs with 5-field template (current behavior / required behavior / wire-protocol implications / test case / subsystem bucket) live in `docs/specs/phase3-plugin-design-inputs.md`. |
| D37 | .uasset/.umap binary parser landed — `inspect_blueprint` + `list_level_actors` complete the D32 scope reduction | Agent 3 Track 2c parser research (`docs/research/uasset-parser-options-2026-04-14.md`) landed as `server/uasset-parser.mjs` — pure JS FPackageFileSummary → name table → FObjectImport (UE 5.0+ 40-byte stride, includes PackageName) → FObjectExport (112-byte stride) → FPackageIndex resolver (positive N → `exports[N-1]`, negative N → `imports[-N-1]`, zero → null) → FAssetRegistryData tag block. Four-commit sequence: (1) parser + format tests against real Project A fixtures, (2) `get_asset_info` reframed from `fs.stat` wrapper to AR-metadata reader per D31, (3) `query_asset_registry` bulk scan with the hybrid cache from D33, (4) `inspect_blueprint` + `list_level_actors` exposing export/import tables. **Per D32**, `inspect_blueprint` + the Asset Registry trio permanently replace the Phase 3 TCP plan for `search_assets`, `get_class_hierarchy`, `get_asset_references`, `get_asset_dependencies`, `find_blueprints_implementing_interface`, plus BP structural introspection — these tools no longer appear in the TCP:55558 scope. **YAGNI cut on `list_level_actors`**: enumerates exports only (`{name, className, classPackage, outer, bIsAsset}`); no transforms, no component tree, no brush geometry. Callers needing transform data route through Phase 3 TCP actor tools per D32. **Test coverage**: 42 parser-format assertions + 15 `get_asset_info` + 16 `query_asset_registry` + 30 `inspect_blueprint`/`list_level_actors` = 103 offline-parser assertions across 4 test files, all gated on `UNREAL_PROJECT_ROOT`. Phase 2 TCP tests unchanged at 218. **Known limitations**: class-name resolution walks imports only (no deep chain across external packages); tag block coverage is AR-export metadata only (no thumbnail/dependency parsing — D33 leaves dependency offsets as a `dependencyDataOffset` field for future use). |
| D38 | Tier-2 parser validation: parser production-grade, 7 handler-layer findings require pre-Phase-3 fixes | Source: `docs/audits/phase2-tier2-parser-validation-2026-04-15.md` (sealed). Agent 5 ran 32 tests (2 skipped) against real Project A content — zero parse errors across 19K+ files spanning 30+ UE asset classes including the 56.5 MB Bridges2 `.umap`. **All 7 findings are in the tool handler layer, not the parser.** 3 High: F0 (response verbosity — FiBData/ActorMetaData/ActorsMetaData blobs blow MCP token cap on BPs, maps, and high-volume folders), F3 (`list_level_actors` oversizes on real maps), F4 (`list_level_actors` returns full export table instead of placed actors — 2.4% signal on dev maps). 3 Medium: F1 (no pagination on `query_asset_registry`), F2 (`inspect_blueprint` duplicates `get_asset_info` tags payload), F6 (`class_name:` filter requires full `/Script/Module.Class` path — short names silently return 0). 1 Low: F5 (BP/BPGC duplicate rows). **Action**: fix F0+F2+F4+F6+F1 before Phase 3 dispatch. Parser (`uasset-parser.mjs`) needs no changes. |
| D39 | Level 1+2 property parser before Phase 3 C++ plugin — determine actual plugin scope empirically | Extends D30 (offline-first sequencing) and D32 (TCP scope reduction). The current parser reads `.uasset` headers (summary, name table, imports, exports, AR tags) but not serialized object data. **Level 1** (FPropertyTag iteration) adds tagged property reading — property names, types, and values for simple types (int, float, bool, string, name, enum, object refs). **Level 2** (struct deserialization) adds handlers for FVector, FRotator, FTransform, FLinearColor, FGameplayTag/Container, FSoftObjectPath. Combined, this surfaces: actor transforms from `.umap` offline (fixes F4 properly + enables `list_level_actors` without TCP), BP variable defaults, gameplay tag assignments on actors/components, asset references. **Sequencing**: Level 1+2 runs parallel with Phase 2 handler fixes (D38). Phase 3 C++ plugin dispatch deferred until Level 1+2 lands and we can empirically measure what the plugin still needs to provide. Research into existing uasset parser projects (UAssetAPI/C#, CUE4Parse, FModel, any JS implementations) feeds the implementation. File scope: `uasset-parser.mjs`, `offline-tools.mjs`, `test-phase1.mjs` — no overlap with Phase 3 `plugin/` when it eventually starts. |
| D40 | Oracle retirement is a single milestone flip, not two | D23 established UEMCP absorbs all tools onto TCP:55558 post-Phase 3. Retirement is one commit: flip all three transitional toolsets (`actors`, `blueprints-write`, `widgets`) from `tcp-55557` to `tcp-55558` in `tools.yaml`, remove 55557 layer from ConnectionManager. D35 server-side residues (`extractWireError`, `stripDoubledAssetSuffix`, Zod param validation) remain as defense-in-depth for at least one release cycle after the flip, then become dead code to clean up. A two-milestone cadence (flip some toolsets, hold others) would recreate the dual-port state D23 explicitly rejected. |
| D41 | `bp_find_global` deferred to v1.2+ — not in 3F v1 or v1.1 | The 3F amendment ships 9 in-graph traversal verbs in v1. v1.1 gets `bp_paths_between` (intra-graph, deferred for cycle/path-pruning complexity) and `apply_patch` (graph write). `bp_find_global` (cross-BP search) is a different category: requires sidecar corpus loaded + asset-registry walk, different failure modes. The offline workaround (`query_asset_registry` + iterate `inspect_blueprint`) covers the "find BPs that call X" scenario. Ship `bp_find_global` in v1.2 or when the offline-loop workaround proves visibly painful. |
| D42 | Engine skew: `#if ENGINE_MINOR_VERSION` wrappers, single codebase — no fork branch | Project A is UE 5.6, Project B is UE 5.7. D7 mandates feature parity. D15 distributes via P4 copy-in. A fork branch multiplies every feature landing ×2, risks drift, and complicates the distribution script. `#if ENGINE_MINOR_VERSION >= 7` wrappers are textbook UE cross-version practice. The 5.6↔5.7 API delta over our surface (GAS, BP graph, Asset Registry, FScopedTransaction) is narrow. Fork only if a deep API boundary emerges where `#if` noise exceeds readability. Track skew sites as they're added for future engine-bump audits. |
| D43 | `delete_blueprint` accepts `force: true`, mirrors the `delete_asset` pattern from the audit-discovered risks table | The risks table already locked `delete_asset`: query `IAssetRegistry::GetReferencers()` before delete, refuse if hard refs exist unless `force=true`, return referrer list. `delete_blueprint` follows the same contract: (1) default refuse when hard refs exist, return referrer list in error payload; (2) `force: true` overrides, response includes `orphaned_references: [...]` warning; (3) wrapped in `FScopedTransaction` per P0-6 so editor Ctrl+Z works; (4) soft refs reported as warnings but don't gate. |
| D44 | `tools.yaml` is the single source of truth for offline tool descriptions and params; eliminate `server.mjs:offlineToolDefs` duplication | Source: `docs/audits/uemcp-server-codebase-audit-2026-04-16.md` §3 (M4 finding) + §6 recommendations. Pre-fix state: the 13 offline tools had descriptions + params duplicated in `server.mjs:offlineToolDefs` (lines 458-525) and `tools.yaml:55-108`, with drift confirmed on `inspect_blueprint`, `query_asset_registry`, and `get_asset_info`. `tools/list` (SDK) showed server.mjs descriptions; `find_tools` (ToolIndex) showed yaml descriptions — callers saw inconsistent metadata depending on discovery path. Structurally identical to how TCP tools already work via `getActorsToolDefs()` etc. in `tcp-tools.mjs`. **Decision**: refactor the offline registration loop to consume yaml via `toolsetManager.getToolsData()`, then delete the `offlineToolDefs` const. This enforces the "tools.yaml is the single source of truth" contract from CLAUDE.md's Key Design Rules. **Rejected alternative**: manually sync the 13 descriptions — leaves drift surface open for the next edit. **Implementation**: orchestrator-direct (not a new Agent), lands parallel with Agent 9 dispatch (Agent 9 is design-only, touches different files). The `inspect_blueprint.verbose` description (M3) is fixed in yaml-alignment form as a prerequisite step. M6 stale supplementary tests are fixed and wired into the CLAUDE.md rotation (now 436 assertions total). **Scope**: `server/server.mjs` only; no yaml changes beyond what M1+M2+M3 already landed. **Verification**: `tools/list` and `find_tools` must report identical descriptions for all 13 offline tools post-refactor. |
| D45 | L3A full-fidelity UEdGraph byte parsing is permanently EDITOR-ONLY; 3F sidecar is the offline-read path for BP logic introspection with acknowledged soft editor dependency | Source: `docs/research/level3-feasibility-study.md` (Agent 11). Pure .uasset byte parsing of the 200+ K2Node subclass hierarchy has no reference-project port source (CUE4Parse, UAssetAPI, FModel all rely on editor-side reflection for BP graph data). Spec-writing burden does not shrink under multi-agent workflow — the bottleneck is reverse-engineering 200+ heterogeneous serialization paths, not typing speed. **3F sidecar (`docs/specs/blueprints-as-picture-amendment.md`) is the offline-read path**: editor plugin writes JSON dump on BP save, offline tools read the dump. **Caveat acknowledged in this D-log entry so future sessions don't lose track**: the 3F sidecar is editor-dependent-to-produce. If a BP is modified without the editor (or with the plugin inactive), the sidecar goes stale and downstream agents get out-of-date BP logic. The sidecar is "pragmatic offline" not "pure offline." **Open follow-up (Agent 11.5)**: whether a skeletal byte parser covering ~10-13 K2Node types (events, variable access, function calls, core control flow) is feasible as a robust-floor complement to the sidecar — delivers bounded BP-logic introspection even when the sidecar is stale/missing. Verdict pending Agent 11.5 study. **Rejected alternative**: treating plugin-handles-this as equivalent to offline-not-needed — this conflates complementary goals (offline reads are first-class per Noah's framing 2026-04-16; plugin is for writes + genuinely-offline-infeasible reads, not as a substitute for reads the offline tier can reasonably handle). |
| D46 | L3B simple-element container properties (TArray / TSet of scalars, engine structs, enums) ship with Agent 10 as Level 2.5; complex-element containers (maps, arrays of custom structs) deferred to Agent 10.5 follow-on | Source: `docs/research/level3-feasibility-study.md` (Agent 11) + resolves Agent 9's Q6 (Option C design doc, was open). **Simple-element scope**: `TArray<int/float/bool/string/FName>`, `TArray<FVector>`/`TArray<FRotator>`/`TArray<FTransform>` and other Level 2 engine structs, `TArray<FGameplayTag>`, `TArray<enum>`, `TSet<>` of the same. Reference ports exist in CUE4Parse (`FScriptArray`, `FScriptSet` readers) and UAssetAPI. Multi-agent cost estimate from Agent 11: ~3-4 agent sessions. **Deferred scope**: `TMap<K,V>` (adds key-serialization complexity), `TArray<FMyUserStruct>` (depends on custom-struct resolver from D47). These ride a follow-on agent after Agent 10 + D47 custom-struct work lands. **Decision-affecting context**: Agent 9's Option C design treats containers as `{unsupported: true, reason: "container_deferred"}` markers. D46 flips simple-element containers from unsupported to supported without changing the tool surface design (response values replace the marker; unsupported marker semantics stay for the deferred subset). **Verification**: Agent 10 extends `test-phase1.mjs` Test 9 with simple-element container assertions; supplementary suite(s) gain per-type parser coverage in the `test-uasset-parser.mjs` pattern. **Rejected alternative**: ship only Level 1+2 with all containers as unsupported markers, revisit L3B as a separate agent — extra orchestration cost for a ~3-4 session scope Agent 10 can absorb. |
| D47 | UUserDefinedStruct property resolution graduates to PURSUE (scheduled as Agent 10.75 or absorbed into Agent 10.5); Project A spot-check confirms meaningful usage | Source: `docs/research/level3-feasibility-study.md` (Agent 11) §L3C + orchestrator Project A spot-check 2026-04-16 (`query_asset_registry class_name:UserDefinedStruct` returned 30 matches, spread across character/mutable-mesh data, AnimGame sample blend stacks + chooser I/O + traversal, sky/weather multiplayer replication, VFX/Niagara params, core DataTable row structs in `/Game/Project A/Data/DataTables_Structs/`). Usage is pervasive enough that BP CDOs holding `TMap<FName, FBlendStackInputs>` or `TArray<FTraversalCheckResult>` are real and common — under Level 1+2 alone these return `{unsupported: true, reason: "unknown_struct"}`, which is a meaningful gap in the "seamlessly read BPs from CLI" goal. **NOTE (2026-04-16 amendment)**: Approach SUPERSEDED-BY-D50. The two-pass resolver design described below was based on an incorrect mental model of UUserDefinedStruct serialization — Agent 10.5 discovered empirically that member definitions live inside `EditorData.VariablesDescArray`, not as child exports. See D50 for the pivoted tagged-fallback approach that shipped. The two-pass reasoning is preserved below as historical context for future agents who might face similar research questions about struct-registry design. **Approach**: two-pass parser — first pass reads the UserDefinedStruct's own `.uasset` to learn member layout (name + type per field), caches in an asset-scoped struct registry; second pass resolves when a `StructProperty` references a project struct. Cycle-safe (bounded by package-load depth); invalidation via existing `assetCache.indexDirty` mechanism (D33). **Scheduling**: after Agent 10 ships Level 1+2 + D46 simple-element containers. Candidate for absorption into Agent 10.5 (maps + complex-element containers already needs custom-struct resolution for `TArray<FMyStruct>` — same infrastructure). Or standalone as Agent 10.75 between Agent 10 and Agent 10.5 if 10.5 is bigger than expected. **Rejected alternative**: KEEP-DEFERRED per Agent 11's pending-spot-check disposition — spot-check resolved the uncertainty in PURSUE direction. **Companion deferrals still standing**: FInstancedStruct (narrow Project A usage — 3 files in Chooser utility, not justified), DelegateProperty (binding lives in graph = L3A territory, would need sidecar anyway), cross-package ref resolution (bolt-on enhancement to L2 ObjectProperty reads later, not a tier). |
| D48 | L3A skeletal parse splits along a cost/coverage seam — Tier S-A (name-only, tagged-property) PURSUE; Tier S-B (pin-tracing with LinkedTo edges) FOLD-INTO-3F. The split IS the research contribution. | Source: `docs/research/level3a-skeletal-parse-study.md` (Agent 11.5). Original question: is a skeletal UEdGraph parser (~10-13 K2Node types) feasible as a robust-floor complement to the 3F sidecar? Answer: the question was load-bearing on an implicit premise ("skeletal = single bounded artifact") that the reference record contradicts. Reference coverage maps cleanly: **tagged-property data** (UPROPERTY fields on K2Nodes — `FMemberReference`, `FGraphReference`, `FUserPinInfo`, etc.) is ~100% reference-backed via CUE4Parse + standard FPropertyTag iteration, already Agent 10 infrastructure. **Pin binary block** (UEdGraphPin LinkedTo edges) has ZERO reference coverage — CUE4Parse `/EdGraph/` contains only 4 type-classification files; UAssetAPI `/ExportTypes/` has no K2Node or EdGraph handlers; UEdGraphPin is not a UCLASS (engine forum confirms) so its serialization is bespoke binary emitted by `UEdGraphNode::Serialize()`. **Tier S-A** (name-only skeletal — answers "which BPs call X," "where is variable Y accessed," "what events does this BP handle"): ~1.5-2 agent sessions total, 13 K2Node types planned (shipped as 19 per D48 amendment 2026-04-19: 17 non-delegate + 2 delegate-presence; scope grew during implementation as Agent 10.5 incorporated additional common K2Node subclasses), coverage ratio 62-100% (median ~75%) on representative Project A BPs — above the 60% decision threshold. Reuses Agent 10.5's struct-registry extension pattern (adds FMemberReference + FGraphReference + FSimpleMemberReference + FUserPinInfo to the registry). **Tier S-B** (full pin tracing — answers "trace exec chain from ReceiveDamage," "what feeds this pin"): ~8-13 agent sessions, zero reference coverage, duplicates the 3F sidecar's exec/data-trace domain at 4-8× higher cost and without 3F's editor-mediated version-correctness. **Three-layer offline-BP stack** (post-S-A landing): L0 structural (inspect_blueprint export walker — already shipped D37); L1 semantic name-only (S-A — always-available, no editor dependency); L2 spatial + trace (3F sidecar — requires editor save-hook, acceptable soft dependency per D45). Robustness floor expands: find/grep workflows stay fully offline-reliable even when sidecar is stale/missing; trace/spatial workflows gracefully degrade to `{available: false}` rather than blocking the entire offline-BP story. **Sequencing (Q-1 resolved 2026-04-16, Mode A)**: Agent 10.5 is a single bundled session covering D46 complex containers + D47 UUserDefinedStruct + D48 S-A. All three share the same struct-registry extension pattern — bundling is more coherent than splitting S-A off as Agent 10.75. Agent 10 stays its own session (ships first: Level 1+2 + L2.5 simple containers + Option C tools). Agent 10.5 is the bundled follow-on. Incremental cost of S-A inside 10.5 ≈ 1.25-1.5 sessions on top of D46+D47 baseline. Rejected Mode B (standalone Agent 10.75) — ~0.25-0.5 session orchestration surcharge for no clarity gain; re-evaluate only if 10.5's scope bloats unexpectedly. **Rejected alternatives**: (a) PURSUE full skeletal as single tier — contradicted by zero-reference pin serialization, cost blows past threshold; (b) FOLD-INTO-3F entirely — loses the robustness floor that makes find/grep work without editor, leaves D45's "soft editor dependency" unmitigated for the dominant workflow class Noah described. **S-B reopening signals**: (1) 3F sidecar work slips indefinitely AND an agent-automation workflow surfaces requiring offline pin-trace specifically (not just name-level find); (2) sidecar-missing scenarios fire frequently enough in practice that sidecar-coverage discipline becomes untenable. Neither trigger present today. **Out-of-scope for S-A v1** (per Q-2 deferrals): math/comparison operators (PromotableOperator, CommutativeAssociativeBinaryOperator, EnumEquality) — ~8% of K2Nodes in gameplay BPs, stay out to keep skeletal brand coherent; can graduate on workflow demand. **Delegate-node presence reporting** (Q-3): class identity only (no payload) — zero-cost addition since tag iteration already emits `exports[].className`. |
| D49 | Parallel-session git commits must be path-limited: `git commit <path> [<path>...] -m "..."` not `git add <path> && git commit -m "..."`. Document as universal rule for any agent working in the repo while another session is active. | Source: Housekeeping Worker collision incident 2026-04-16 (report at `docs/handoffs/housekeeping-worker-report-2026-04-16.md`). **Failure mode**: when two Claude Code sessions share a working tree, the git index is also shared. Agent B stages file X at time T1. Agent A does `git add A.md && git commit -m "..."` at T2 — `git commit` with no path args commits the ENTIRE index, sweeping A.md AND X into the same commit. Detected via `git show --stat <sha>` showing unexpected files. Recovery: `git reset --soft HEAD~1` then `git commit <path> -m "..."` (path-limited) to put the sweep back into the staged pool and produce a clean path-correct commit. **Universal rule going forward**: (a) after `git add`, run `git diff --cached --name-only` and abort if the staged set contains anything unexpected; (b) always prefer `git commit <explicit-paths> -m "..."` over `git commit -m "..."` when a parallel session may be active. Path-limited commits ignore other staged files. This works for both tracked-and-modified files and for untracked-then-added files. **Incident evidence**: Worker Option A kept as the resolution (both commits `c550807`, `8e45ae2` are path-correct post-recovery; Agent 10's 539 staged + 47 unstaged lines all preserved). **Scope of rule**: applies to every agent dispatched while any other session is active in the same working tree, not just the housekeeping worker pattern. Worth referencing in future handoffs when parallelism is planned. **Related CLAUDE.md update**: the "Shell & Tooling Requirements" section documents FUSE-mount lock issues for Cowork sandbox bash, but does not cover shared-index collisions between native-CLI sessions. Consider adding a "Parallel session safety" subsection when the next CLAUDE.md maintenance pass happens. **Not rejected — deferred**: a more elaborate solution (e.g., per-worker git worktrees, so each agent has its own index) would eliminate the failure mode entirely. Cost is nontrivial; path-limited commit discipline is sufficient for current workflow. Revisit if parallel-session count grows. |
| D50 | Tagged-fallback replaces the D47 two-pass resolver design; self-describing FPropertyTag streams decode unknown struct values without loading the referenced struct asset. Agent 10.5 empirically verified + shipped. | Source: `docs/research/level3-feasibility-study.md` context + Agent 10.5 final report (commits `33409f6`, `d7f4c83`, `cca6690`, `f339773`). **Original D47 design**: two-pass parser loading each UUserDefinedStruct `.uasset`, walking its exports for member definitions, caching the layout, applying to consumers. **Empirical reality discovered by Agent 10.5**: UUserDefinedStruct `.uasset` files have only 2 exports (the struct + UserDefinedStructEditorData); member definitions live inside `EditorData.VariablesDescArray`, NOT as child exports. The handoff's mental model was incorrect. **Insight**: FPropertyTag streams are self-describing — every tag carries name+type+size inline. Value decoding doesn't need the struct's asset at all; you walk the consumer's tagged sub-stream and it tells you what it holds. **Shipped design (tagged-fallback)**: no cache, no cycle detection, no struct registry entries for UDSs. 601 unique struct names decoded automatically across the 19K-file corpus. **Marker reductions**: unknown_struct 251,092 → 22,000 (91% reduction); complex_element_container 65K → 6K (91%); container_deferred (TMap) 24,171 → 0 (100%); total unsupported markers 377,713 → 107,732 (71% reduction). **Tier 1 redundancy note**: explicit struct handlers for FBox/FVector4/FIntPoint/FBodyInstance turned out to be redundant with the tagged fallback in practice (all 4 resolve via fallback). FExpressionInput is the one exception — 99.8% native-binary in practice, so its tier-1 handler only works on the ~0.2% tagged-path instances; marker relabeled to `expression_input_native_layout_unknown` (21,876 instances) for future reference-port work. **Test impact**: 561 → 612 assertions green. **D44 invariant** verified for `find_blueprint_nodes` — tools/list ↔ find_tools ↔ yaml byte-identical. **Performance**: 1.06× Agent 10 baseline (17.1s vs 16.1s bulk; 0.89ms vs 0.80ms avg) — well under 2× SLA. **Rejected original D47 design**: kept in-place as SUPERSEDED-BY-D50 footnote; preserved as historical record of the mental model correction. **Follow-on items**: FExpressionInput native binary layout (deferred, ~21K relabeled markers); nested FieldPathProperty in structs (pre-existing L1 edge case, separate cleanup). |
| D51 | `tools.yaml` serves a dual role: live registration source (for shipped toolsets) AND Phase 3 planning table (for un-shipped toolsets). Document as an explicit principle. | Source: Audit B (Agent B, 2026-04-17 delivery) §6 Q3 flagged this implicit convention. **The dual role**: (a) for shipped toolsets (offline, actors, blueprints-write, widgets), yaml is the single source of truth and drift is a bug (D44 structurally enforces this at registration time). (b) For un-shipped Phase 3 toolsets (blueprint-read, asset-registry, data-assets, materials, gas, input-and-pie, visual-capture, editor-utility, animation, cpp-introspection, remote-control), yaml entries are stubs with placeholder `params: {}` — the yaml header comment hints with *"params: stubs — populated incrementally during implementation."* Both roles live in the same file; the distinction is soft convention, not structural. **Risk without D51**: future agents scanning yaml for "orphans" or "dead entries" might propose deleting stub Phase 3 entries thinking they're unused, when they're intentional planning placeholders. Workflow Catalog's 120-tool baseline reflects BOTH roles. **Rule codified**: stub entries for un-shipped toolsets must NOT be deleted without explicit Phase 3 scope-decision backing. The stub header comment remains authoritative for identifying planning rows. **Phase 3 scope refresh will re-populate stubs**: when Phase 3 dispatches, each stub entry either gets fleshed out (shipped) or removed (de-scoped per D32/D37/D45/D48/D50 reductions + refresh findings). **Side benefit**: makes the Workflow Catalog's coverage-projection math auditable — the 120-tool denominator includes planning rows, so coverage % is "(shipped-offline-capable) / (total-planned)." That fraction is meaningful precisely because the planning rows are stable. **Rejected alternative**: split tools.yaml into `tools.shipped.yaml` + `tools.plan.yaml` — structural separation at the cost of search/discovery friction and a more complicated registration path. Not worth the maintenance overhead for the clarity gain. |
| D52 | Near-plugin-parity for offline READ functionality is a first-class project goal. Any read capability reasonably servable offline should be built offline BEFORE the equivalent plugin tool. Plugin scope reduces to writes + genuinely-offline-infeasible reads. | Source: Noah 2026-04-19 in response to "can we get exact BP logic offline?" discussion. **Prior framing** (D45 "offline reads are first-class", D48 Mode A bundling): offline was preferred but plugin was the fallback. **D52 sharpens the goal**: rather than "offline preferred," the goal is near-parity between plugin-enabled and plugin-absent read functionality. Plugin's read-side scope shrinks to: (a) runtime/PIE state (live UObject values); (b) UEdGraph pin topology (D48 S-B — deferred but reopening triggered under D52 if 3F sidecar slips); (c) compiled/derived data (Niagara VM, baked lighting, shader compilation); (d) reflection-only metadata (EditAnywhere/BlueprintReadWrite runtime flags, interface lists via UClass walk). **What "near-parity" means concretely**: currently 67% fully-offline + 9% partial = 76% read coverage per Workflow Catalog. Target trajectory: push toward 85-90% fully-offline through Phase 3 scope-refresh + subsequent enhancement work (EN-1 through EN-6). **Structural limits acknowledged**: full parity is impossible — runtime state, pin topology (without sidecar), and compiled data will always be editor-dependent. **Sidecar reframe**: 3F sidecar (D45/DR-3) becomes a transition tool — plugin-mediated short-term parity, not the final answer. Long-term offline parity for BP pin-trace requires tackling D48 S-B (~8-13 agent sessions) despite cost. **Reopening triggers for S-B** (updated per D52): weakens from "3F must slip AND workflow must surface" to just "workflow pressure accumulates OR 3F slips." S-B aligns with D52 even without a sidecar crisis. **Downstream effects already in motion**: EN-2 promoted from deferred to pre-Phase-3 worker (Noah 2026-04-19); MCP-wire integration test harness promoted from backlog-EN-6 to dedicated worker (D52 makes agent-facing usability a first-class concern since any Zod-coerce-class blocker nullifies offline-first for typed params). **Rejected alternative**: keep "offline preferred" framing and let plugin absorb complex reads — rejected because it cedes the agent-CI and plugin-absent workflows that D45 explicitly values. **Open-ended**: revisit D52 if plugin ships and changes the cost/benefit curve materially (e.g., if plugin distribution becomes frictionless across Project A + Project B + CI, the incremental value of hard-to-reach offline coverage drops). |
| D53 | Phase 3 plugin-TCP surface refined to ~86 tools (6 DROP, 3 MOVE-TO-SIDECAR, 18 KEEP-reduced, 35 KEEP-full, 32 transitional rebuild). Every retained tool justified under a D52 category; zero defensibility failures. | Source: `docs/research/phase3-scope-refresh-2026-04-20.md` §Q1 + §Q2 (commit `9e9dbe5`). **DROP**: `search_assets`, `get_class_hierarchy`, `get_asset_metadata`, `get_data_asset_properties`, `list_gameplay_tags_runtime`, `create_asset` — all fully covered by shipped offline surface post-D37/D50/EN-2. **MOVE-TO-SIDECAR**: `get_blueprint_graphs`, `get_animbp_graph`, EventGraph subset of `get_widget_blueprint` — absorbed by 3F dump_graph + traversal verbs. **KEEP (reduced)**: 18 tools retain only the reflection/compiled/runtime subset their offline displacer can't serve (e.g., `get_blueprint_variables` retains EditAnywhere flags only; CDO values covered offline post-Agent-10). **KEEP (full)**: 35 tools are writes or have no offline displacer. **Transitional**: 32 tcp-55557 tools (actors/blueprints-write/widgets) rebuild on tcp-55558 with P0-1 through P0-11 quality upgrades per D23/D40 oracle retirement. **D52 category coverage**: (a) 11 runtime, (b) 5 pin-topology (all MOVE-TO-SIDECAR or sidecar-subset), (c) 14 compiled/derived, (d) 8 reflection-only, (w) 35 writes. **Rejected**: treating `list_gameplay_tags_runtime` as KEEP — judgment-call DROP per FA-5; reversible if live-editor tag state surfaces a real workflow (can re-enter via Phase 4 Remote Control). **Follow-on**: M0 yaml grooming ships the DROP + MOVE-TO-SIDECAR + reduced-scope annotations per §Q5.3 M0 scope. |
| D54 | DR-3 resolved: SHIP-SIDECAR-PHASE-A-FIRST. 3F splits into Phase A (save-hook + editor-menu prime + offline reader + 9 traversal verbs — NO TCP dependency) and Phase B (dump_graph TCP + agent-invocable prime — post-M1). Phase A parallelizes with M1 3A scaffolding. | Source: `docs/research/phase3-scope-refresh-2026-04-20.md` §Q3 (commit `9e9dbe5`), advisor-refined during research. **Why the split wins**: the "sidecar first vs last" framing in DR-3 was a false dichotomy — Phase A has no TCP dependency, so it ships the same wall-clock window as M1 (3A scaffolding) without competing for the same agent sessions. Phase B adds the on-demand dump_graph path after M1 scaffolding exists. **Workflow unlock timing**: sidecar-read surface (9 traversal verbs — `bp_list_graphs`, `bp_trace_exec`, `bp_trace_data`, `bp_show_node`, `bp_neighbors`, `bp_subgraph_in_comment`, `bp_find_in_graph`, `bp_list_entry_points`, `bp_paths_between`) available as soon as the save-hook plugin loads, not gated on the full plugin. **Note per FA-3**: the 9 traversal verbs are **offline tools** (not plugin scope) that ship in `server/offline-tools.mjs` — they read the sidecar JSON, not the editor. Easy to miss in a plugin-focused scope doc. **Supersedes backlog DR-3**: remove DR-3 from backlog.md deferred-research section; the decision is made. **Dispatch sequencing**: M0 (0.5) → M1 + M2-Phase-A in parallel → M2-Phase-B (post-M1) + M3 + M4 in parallel → M5. |
| D55 | Skeletal S-B verdict: PURSUE-AFTER-SIDECAR at **honest 6-9 sessions** (was aspirational 4-6 per scope-refresh handoff). Sidecar becomes the validation oracle — S-B's byte-level parse validates against sidecar's known-correct JSON LinkedTo data. Commissioned optional as M6. | Source: `docs/research/phase3-scope-refresh-2026-04-20.md` §Q4 + §FA-1 (commit `9e9dbe5`). **Why 6-9 not 4-6**: the 19-type restriction collapses per-node override cost (~1 session saved vs Agent 11.5's 2) but does NOT collapse the fixed-cost floors — base pin-block RE (3-4 sessions reverse-engineering `UEdGraphNode::Serialize()`), LinkedTo resolution (1-2 sessions for pin-ID graph walker), version-skew buffer (2-3 sessions for 5.6↔5.7 format shifts). Reference-coverage evidence (zero-reference on pin binary per Agent 11.5 §2.2) does not support every buffer simultaneously collapsing. **Why AFTER-SIDECAR not NOW**: sidecar's known-correct `LinkedTo` data per node-ID becomes the validation oracle for S-B's byte parse. Without the oracle, S-B has no ground-truth signal for correctness on a per-node-type basis. Oracle gating also orders nicely against D52's weakened reopening trigger ("workflow pressure accumulates OR 3F slips"). **M6 is OPTIONAL per FA-6**: not default-scheduled. Commission only if D52 near-parity goal is under-served by sidecar alone OR agent-automation surfaces pin-trace pressure. **Priority order if commissioned** (§Q4.5): CallFunction → VariableGet/Set → Event/CustomEvent → control-flow → remaining. **Supersedes**: backlog DR-1 reopening conditions updated (S-B oracle dependency now primary); D48 S-B FOLD-INTO-3F verdict superseded to PURSUE-AFTER-SIDECAR. |
| D56 | `get_widget_blueprint` splits by sub-scope: EventGraph + functions → MOVE-TO-SIDECAR (UBlueprint subclass, 3F-1 covers); widget hierarchy tree + property bindings → KEEP (reduced) plugin-TCP (UMG/Slate data, separate node hierarchy from UEdGraph). Sidecar schema does NOT extend to widget designer in v1. | Source: `docs/research/phase3-scope-refresh-2026-04-20.md` §Q1.2 + §FA-2 (commit `9e9dbe5`). **Research correction**: initial MOVE-TO-SIDECAR framing missed that the widget designer view (hierarchy + bindings) is UMG/Slate data, not UEdGraph. Sidecar Design Session Q4's defer-on-workflow-signal pattern (same as `get_material_graph`) applies — widget traversal verbs are v1.1+ territory. **Concrete split**: EventGraph + functions of `UWidgetBlueprint` covered by sidecar dump_graph (UBlueprint subclass inheritance). Widget-tree + property bindings served by plugin-TCP `get_widget_blueprint` with reduced scope (D52 category (d) reflection-only: named-binding metadata + tree walk via editor reflection). **Offline partial coverage**: `read_asset_properties` on widget slot exports gives a partial picture but misses named-binding function references that need editor UClass reflection. **Impact on M4 scope**: `get_widget_blueprint` listed in M4 reduced-read worker with explicit widget-tree-only surface (EventGraph portion NOT implemented there — sidecar handles it). **Rejected alternative**: extend sidecar schema in v1 to cover UMG designer — Sidecar Design Session Q4 already declined this for `get_material_graph`; same reasoning applies. |
| D57 | M-alt feasibility spike verdict: **PRESERVE + AUGMENT**. Current D54 M-sequence (SHIP-SIDECAR-PHASE-A-FIRST) is correct; add `3F-4 DumpBPGraphCommandlet` to M2-Phase-A bundle at +0.5-1 incremental session. Commandlet complements save-hook for CI / fresh-checkout / stale-sidecar cases; does NOT replace save-hook and does NOT defer M1. | Source: `docs/research/m-alt-commandlet-feasibility-2026-04-20.md` (commit `44b080e`). **Empirical grounding**: UE 5.6 cold boot is 5.7s floor / 17.5s with AR scan of 22,064 Project A packages — better than handoff's 30-60s assumption but still can't displace save-hook's 0ms warm path. Clean exit 0 both runs. **Cost reality**: Q2 found C++ implementation cost is ~symmetric between commandlet and save-hook (~1 session each); hybrid costs ~0.5-1 incremental session because the JSON serializer is the dominant cost and it's shared. Reference pattern `UCompileAllBlueprintsCommandlet` is 359 lines; `DumpBPGraph` would be ~100-200 lines. **Coverage reality**: Q3 walked all 22 (c)+(d) tools — 55% FULL commandlet-accessible, 36% PARTIAL, 9% NONE. Below the >70% threshold the handoff set for RESTRUCTURE-AGGRESSIVE. **Why M1 doesn't defer under D52**: Q4/FA-(a) identified that M1's primary purpose is writes-gated (35 tools in category (w)), not reads-gated. Commandlet fundamentally cannot serve writes/PIE regardless of read-coverage math. Orchestrator's push to defer M1 (this session, pre-spike) conflated read-surface coverage with M1 purpose — corrected by spike. **What 3F-4 unlocks**: CI agent workflows with no persistent editor, fresh-checkout agents priming the sidecar cache on-demand, stale-sidecar regeneration when BP mtime > sidecar mtime. **M1 constraint discovered**: UEMCP plugin's `MCPServerRunnable` must gate on `!FApp::IsRunningCommandlet()` to avoid port contention when the plugin is loaded in the commandlet process. Captured as M1 scope item (the existing UnrealMCP plugin does NOT have this gate — silent failure mode in commandlet mode). **Six framing concerns surfaced** (FA-a through FA-f in deliverable) — carry forward to M2-Phase-A handoff writing. **Rejected alternative**: RESTRUCTURE-AGGRESSIVE (defer M1, commandlet-only reads) — failed steel-man on both latency (17.5s cold can't displace 0ms warm) and M1-purpose (writes independent of reads). **Open item**: DR-3 mini-amendment — the 3F v1 bundle expands to `3F-1 dump_graph` + `3F-2 save-hook` + `3F-3 editor-menu prime` + `3F-4 commandlet`; reflected in sidecar amendment spec when M2-Phase-A handoff drafts. |
| D58 | Re-sequence framing correction (Noah 2026-04-20): "MCP-first, plugin-enhances." Offline edge-topology parse is the foundation, not an optional complement. D48 S-B FOLD-INTO-3F, D54 SHIP-SIDECAR-PHASE-A-FIRST, D55 PURSUE-AFTER-SIDECAR, and D57 PRESERVE+AUGMENT substrate are all amended per `docs/research/phase3-resequence-mcp-first-2026-04-20.md` §Q6 (commits `40b511b` + `21b80e2`). | Source: Noah's direct correction — *"I really would like an MCP first solution that works and is ENHANCED by plugin code rather than enabled by it."* Re-sequence research delivered via `docs/research/phase3-resequence-mcp-first-2026-04-20.md` (654 lines, two commits). **D48 amendment**: S-B FOLD-INTO-3F is SUPERSEDED-BY-D58. Offline pin-topology parse is first-class foundation, not optional complement. S-B is re-sequenced as primary M-new ahead of (not after) the plugin enhancement layer. S-A (shipped) and S-B (edge topology) now form the two-tier offline BP-logic foundation. **D54 amendment**: SHIP-SIDECAR-PHASE-A-FIRST is SUPERSEDED-BY-D58. Pin-topology foundation moves from sidecar to offline S-B (M-new). 9 traversal verbs still ship, but against offline bytes rather than editor-emitted JSON. Sidecar narrows to plugin-only fields (compile errors, reflection metadata, runtime values) and ships as M-enhance in parallel with M3/M4/M5, not as pre-M1 Phase A. Phase A/B sub-split of 3F is no longer load-bearing; 3F-1 dump_graph + 3F-3 editor-menu prime become enhancement-layer fast-paths, not foundational. **D55 amendment**: PURSUE-AFTER-SIDECAR ordering REVERSED. S-B ships BEFORE enhancement layer. Cost updates from 6-9 to **6.5-10 sessions** with ~0.5-1 session bootstrap-oracle surface (differential commandlet + fixture corpus replaces sidecar-already-there as validation oracle). M6-optional framing superseded — S-B is critical-path for D52 edge-topology offline near-parity, not an optional follow-on. Priority order within S-B preserved (CallFunction → VariableGet/Set → Event/CustomEvent → control flow → remaining 19-type skeletal). **D57 amendment**: PRESERVE substrate (D54) is gone. AUGMENT verdict retargets: (1) **bootstrap-oracle dev variant** ships inside M-new as differential-testing infra for S-B development (~0.5-1 session, minimal emit-edges-only scope, dev-only not end-user); (2) **production variant** ships inside M-enhance as CI/fresh-checkout/stale-sidecar priming for narrow plugin-only-fields sidecar (~0.5-1 session, reuses narrow serializer from save-hook). M1's `!FApp::IsRunningCommandlet()` gate stays load-bearing for both commandlet variants. FA-a reinforcement: M1 writes-gated independent of read coverage; re-sequence does NOT defer M1. **New milestone M-spatial** surfaced via FA-γ: 1-2 session intermediate ships 5 of 9 traversal verbs on today's infra (positions, comments, `contains[]`, via_knots partial) without S-B dependency. **Unchanged**: D45 L3A full-fidelity EDITOR-ONLY (19-type skeletal is not full fidelity); D52 near-plugin-parity (sharpened implicitly — edge topology now explicit first-class offline goal); D53 Phase 3 tool surface (86 tools; 3 MOVE-TO-SIDECAR entries reframe to "offline-primary + enhancement-augmented" via D58 ripple); D56 widget-blueprint SPLIT (EventGraph subset reframe inherits D54 amendment). **New M-sequence** per §Q5.1: M0 shipped → (**M1** 3-5 ∥ **M-spatial** 1-2 ∥ **M-new** 6.5-10) → (**M-enhance** 3-5 ∥ **M3** 6-10 ∥ **M4** 3-5 ∥ **M5** 6-10). Total ~28.5-47 sessions; wall-clock ~14-22 with parallelism. **Rejected alternative**: preserve D54 as-is — rejected because D52's "plugin-absent read functionality is first-class" wording was already load-bearing but satisfied only aspirationally; the re-sequence closes the gap. **Follow-on items**: 4 D-log entries (D48/D54/D55/D57) now cross-reference D58 rather than being individually amended; readers should follow D58 for current framing. Orchestrator action: update backlog.md M-sequence to reflect M-new primary + M-spatial intermediate; FA-ε (M-enhance TCP brokers vs Phase 4 RC boundary) flagged as open orchestrator call, deferred to when M-enhance dispatches. |
| D59 | M1 3A TCP scaffolding landed (commits `2b86369` through `1d3f6cf`); M-spatial wire validation confirms D50 tagged-fallback coverage through second independent channel. Response envelope diverges deliberately from 55557 oracle per P0-1. | Sources: M1 worker final report 2026-04-21 (6 commits); M-spatial manual tester final report 2026-04-21 (commit `8ad69bd`, 18/18 PASS). **M1 shipped**: plugin/UEMCP/ scaffold, 6 P0 helpers (P0-1 envelope, P0-2 FindActorInAllLevels, P0-3 label resolver, P0-4 REGISTER_PROPERTY_HANDLER + 6 scalars, P0-9 null-check, P0-10 TransformParser-returns-bool), MCPServerRunnable on TCP:55558, MCPCommandRegistry with ping default handler, 8 automation tests, server-side integration test. D57 `!FApp::IsRunningCommandlet()` gate in StartupModule as scaffold's first commit — Oracle-A unblocked. **Response envelope change**: M1's single-shape `{status, result}` / `{status, error, code}` is a BREAKING CHANGE vs existing UnrealMCP (TCP:55557) which uses two formats (`{status:"error", error:msg}` and `{success:false, message:msg}`). This is deliberate per P0-1 spec (`docs/specs/phase3-plugin-design-inputs.md`) and is the target envelope for M3 oracle-retirement rebuilds. ConnectionManager already normalizes both old shapes — accepting the new single-shape is backward-compatible at the server-side. **M-spatial wire validation**: 18/18 PASS with exact numeric match to unit-test results (10 graphs, 53→7 event filter, 11 contained, 17 entry points, 1424×544 rect at (-1296, 160)). FA-β manifest on all 5 verbs; FA-δ plugin-absent first-class confirmed with UnrealMCP absent + both TCP layers ECONNREFUSED + no Saved/UEMCP/ dir. Second independent verification of D50 tagged-fallback UPROPERTY coverage (channel 1 was M-spatial unit tests). **Workflow gap surfaced**: no verb enumerates comment node_ids — `bp_find_in_graph(node_class:"UEdGraphNode_Comment")` returns 0 because comments aren't K2Nodes. Current workaround uses `inspect_blueprint` which triggers MCP token cap on BP_OSPlayerR (85KB / 275 exports → auto-saved to tool-results file, breaking natural composition). **Fix candidate (EN-8, tracked in backlog)**: add `comment_ids: [...]` to `bp_list_graphs` per-graph rows. Cheap offline JS change, closes gap without new verb. **FA-β forward-compat guidance for M-new**: do NOT rename `pin_block` / `exec_connectivity` tokens when S-B adds coverage — just remove from `not_available[]` and add new tokens to `available_fields[]`. `pins: []` is the correct extension point (populate in place). **Pending user verification for M1**: (a) plugin visibility in Project A via `mklink /D Project A\Plugins\UEMCP D:\DevTools\UEMCP\plugin\UEMCP` OR `AdditionalPluginDirectories` entry in Project A.uproject; (b) UBT compile cleanly in Project A; (c) commandlet gate log line on `-run=NullCommandlet`. M1 worker's sandbox environment couldn't exercise these. **UE 5.6 API notes from M1 worker**: `FUTF8ToTCHAR(const ANSICHAR*, int32)` constructor used for recv-side — compiles against existing UnrealMCP's exact engine build; fallback `reinterpret_cast<const UTF8CHAR*>(...)` if first compile errors. `TryGetObjectField(FString, const TSharedPtr<FJsonObject>*&)` reference-to-pointer signature confirmed on 5.6. **Test baseline unchanged**: 899; integration test is supplementary (skips on ECONNREFUSED when editor not running). **Next critical path**: M-new Oracle-A dispatchable immediately (scaffold + gate in place); M-new S-B-base gated on Oracle-A; M-enhance parallel with Oracle-A/S-B-base wave. **Deferred to M3+**: P0-5, P0-6, P0-7, P0-8, P0-11 per handoff scope; struct/vector/object/array property handlers (registry + macro shipped; per-tool registration lives in M3 handler files). |
| D60 | M1 compile hotfix: `IsRunningCommandlet()` is a global free function in `CoreGlobals.h`, NOT a member of `FApp` on UE 5.6. Fixed in `UEMCPModule.cpp:112`. Also removed `GameplayAbilities` from `UEMCP.Build.cs` PrivateDependencyModuleNames — M1 scaffold doesn't reference GAS, and listing it as a module dep without adding it to `UEMCP.uplugin` Plugins[] triggered a UBT warning. | Source: user compile attempt 2026-04-21 against Project A, error C2039 at UEMCPModule.cpp(112,12): `'IsRunningCommandlet': is not a member of 'FApp'`. M1 worker's report flagged `FUTF8ToTCHAR` as a potential compile risk but the actual slip was the commandlet gate call pattern. **Rule for future C++ workers**: when referencing "global state" checks (IsRunningCommandlet, GIsEditor, IsRunningGame, etc.), verify they live at namespace scope in `CoreGlobals.h` rather than assuming they're FApp members. FApp::IsUnattended() exists and is different (returns true when `-unattended` flag passed, not when running as commandlet). **UBT plugin-dependency consistency rule**: adding a module to PrivateDependencyModuleNames without also adding the containing plugin to UEMCP.uplugin's Plugins[] array produces "plugin does not list X as a dependency, but module X depends on X" warning. If a C++ worker adds GameplayAbilities/GameplayTags/RemoteControl/etc. module dep, they MUST also add the plugin entry to .uplugin. M3+ GAS tool workers re-introduce this when they need it. **Hotfix confirmed compile-ready** pending user re-run of UBT. Project A .uproject still needs a GameplayAbilities reference elsewhere (not UEMCP's concern) but UEMCP no longer raises the warning. **Pre-existing Project A warning** (`StructUtils` not listed in Project A.uproject) is unrelated to UEMCP. **Visual Studio 2022 compiler version warning** — toolchain 14.44 is newer than Epic's preferred 14.38, informational only, not a real issue. **Test baseline unchanged** at 899 — this was a compile-gate fix, no JS tests affected. |
| D61 | D57 commandlet gate verified end-to-end against Project A 2026-04-21. `test-uemcp-gate.bat` [PASS] confirms plugin loads + gate fires + TCP server suppressed under `-run=NullCommandlet`. Unblocks M-new Oracle-A dispatch (scaffold + gate both in place). **Root cause of prior test failures: UBT incremental-build cache shipped stale DLL for 6+ gate-test iterations.** | Source: user test output 2026-04-21 showing `[PASS] D57 gate fired. TCP server was suppressed.` with both diagnostic lines emitted (`UEMCP StartupModule entered (pre-gate diagnostic)` + `UEMCP: commandlet detected — TCP server suppressed`). **Debug arc**: 6+ rounds of gate-test [FAIL] across plugin-junction / plugin-symlink / physical-xcopy / RemoteControl-dep-removal / IsBetaVersion-flip / diagnostic-log-add iterations. Advisor pass surfaced that DLL timestamp stayed frozen at 13:07 throughout despite source changes at 14:05 and `.modules` rewrites at 14:49 — UBT was short-circuiting each invocation at 0.72s via incremental-build cache even though source content changed. **Fix that unlocked**: clean nuke + rebuild per advisor recipe — `rmdir /s /q Project A\Plugins\UEMCP\Binaries Intermediate` then `Engine\Build\BatchFiles\Build.bat YourProjectEditor Win64 Development -project=Project A.uproject -WaitMutex`. Test passed on first run after nuke. **Rule for future C++ workers**: when post-compile behavior doesn't match source changes, check DLL mtime against .cpp mtime BEFORE iterating further on the source. If DLL is older than source, UBT is serving stale artifact — nuke Binaries + Intermediate and rebuild cleanly. Symptoms of UBT cache staleness: compile completes in <1s with "no work to do" phrasing; `.modules` file rewrites but DLL does not; source-level logic changes have zero effect on runtime output. **Secondary lesson — physical plugin copy over symlink/junction for multi-project plugin sharing**: mklink-based plugin sharing thrashed through EPERM / .uproject-edit / plugin-discovery issues across 4+ iterations; physical xcopy to `Project A\Plugins\UEMCP\` worked first try. Dev workflow open question: sync-plugin.bat to propagate `D:\DevTools\UEMCP\plugin\UEMCP\` source-of-truth changes to project Plugins directories (tracked for future tooling decision). **Artifacts preserved**: `setup-uemcp.bat` (new-PC onboarding with GUI browse + physical plugin copy, commits `adea280` → `dba53c5`); `test-uemcp-gate.bat` (standalone gate verifier at repo root, commit `5f6995f`). **Diagnostic log cleanup deferred**: `UEMCPModule.cpp:113` "UEMCP StartupModule entered (pre-gate diagnostic)" is Warning-level — demote to Log or remove once M-new Oracle-A has exercised the gate independently. **Editor run cross-check**: plugin simultaneously verified loading in interactive editor (port 55558 LISTENING confirmed on editor PID), so gate correctly returns false in editor context and true in commandlet context. **M1 now definitively shippable**: scaffold compiles, loads, gates correctly — Oracle-A handoff at `docs/handoffs/m-new-oracle-a-commandlet.md` (commit `130fd0a`) is unblocked. **Test baseline unchanged** at 899 — gate-test is manual/shell-level, not wired into JS rotation. |
| D62 | M-new Oracle-A landed 2026-04-21 (commits `b8e64a5` commandlet + `b1fb2e7` fixtures). 6-BP corpus with 204→4 node range, 596→4 edge range. Clean 20.66s build, 9s BP_OSPlayerR invocation. D57 gate regression-tested PASS post-build. **Critical UE 5.6 API correction discovered by Oracle-A, load-bearing for S-B-base**: `UEdGraphPin::LinkedTo` is `TArray<UEdGraphPin*>` (raw-pointer), NOT `TArray<FEdGraphPinReference>` as the Oracle-A handoff (and S-B-base draft) originally stated. `FEdGraphPinReference` is a separate compile-time serialization helper holding `{OwningNode, PinId}`, used inside compiled node data. At walk-time resolution goes via `Linked->GetOwningNodeUnchecked()->NodeGuid` + `Linked->PinId` directly. | Source: Oracle-A final report 2026-04-21 (insight block). Engine source `EdGraphPin.h:375` is the authoritative cite. **Implication for S-B-base byte parser**: the raw byte stream will emit `FEdGraphPinReference`-shaped entries (pairs of `{OwningNode FPackageIndex, PinId FGuid}`) because byte serialization can't persist raw pointers — the reference helper is the persistence shape, the pointer is the in-memory shape. S-B-base's reader reads reference-shaped entries from bytes, Oracle-A's in-memory walker reads pointer-shaped entries from loaded objects; both converge on the same edge set. **S-B-base handoff amended** (same commit as this D-log entry) — §2 pin-block binary layout field #12 retained as `TArray<FEdGraphPinReference>` (correct for BYTES), but added clarifying note that runtime in-memory shape is `TArray<UEdGraphPin*>` and cross-references Oracle-A's README.md §Edge cases for gotchas. **Fixture corpus**: BP_OSPlayerR (204 nodes / 596 edges, densest), BP_OSControlPoint (182/330), TestCharacter (11/24), BP_OSPlayerR_Child / _Child1 / _Child2 (6/4 each, inheritance triple). 7 zero-edge candidate BPs excluded per success criterion #5 but listed in `fixtures.txt` for opt-in. **Additional gotchas** captured in Oracle-A's fixtures `README.md` §Edge cases (S-B-base must read): GUID format is 32-hex-no-dashes (Oracle-A emits consistent format; S-B-base must match), self-loops allowed, orphaned-pin null-check required, `SubPins` not emitted (could false-positive for leaf pins), sub-graph dotted-key flattening for collapsed nodes. **MSYS path mangling warning**: `/Game/...` paths get mangled to `C:/Program Files/Git/Game/...` when commandlet runs from Git-Bash; use `cmd.exe` or `MSYS_NO_PATHCONV=1` prefix. Documented in `fixtures.txt`. **S-B-base now unblocked**: oracle JSON is the differential-test contract. **Next critical path**: S-B-base dispatches after EN-8/9 lands (to share server/offline-tools.mjs cleanly) — 10-45 min wait maximum. |
| D63 | EN-8 + EN-9 bundle landed 2026-04-21 (commit `1bc3e8b`, single commit, 3 files). Test baseline **899 → 914** (+15 assertions — more than handoff's projected +7-8 because worker added full-contract coverage for the shared helper). `withAssetExistenceCheck` exported from `offline-tools.mjs` for M-new verb-surface reuse. Comment class-name in BYTES is `EdGraphNode_Comment` (UE strips U/A prefix at serialization) — parser code already used correct form. | Source: EN-8/9 worker final report 2026-04-21. **Helper signature**: `(handler: (projectRoot, params) => Promise<object>) => same-signature-guarded`. Only catches `err.code === 'ENOENT'`; all other errors re-thrown. Full-contract coverage includes happy-path passthrough, non-ENOENT re-throw, null params handling — M-new Verb-surface worker inherits this contract. **Workflow gap closed**: `bp_list_graphs` now emits `comment_ids: []` per-graph row; callers skip `inspect_blueprint` entirely when enumerating comments (closes MCP token-cap-on-BP_OSPlayerR blocker from D59). **FA-β graceful-degradation contract** now extends uniformly across all 5 M-spatial verbs — bogus asset_path returns `{available: false, reason: "asset_not_found", asset_path: "..."}` instead of propagating raw ENOENT through MCP error channel. **EN-6 bundle question closed**: EN-8/EN-9 were natural bundles (same file, shared test rotation); EN-6 is caller-ergonomics in a different concern — stays as separate enhancement candidate per worker recommendation. **Comment class-name gotcha propagated** to S-B-base handoff — any byte-level class-name matching in S-B-base's parser must use stripped form (no U/A prefix). **Test baseline 914** is the new reference for S-B-base + all future workers. |
| D64 | sync-plugin.bat landed 2026-04-21 (commit `117b7d9`). 0.59s wall-clock smoke test against Project A; byte-identical sync confirmed; target `Binaries\`+`Intermediate\` preserved (UBT cache intact). | Source: sync-plugin.bat worker final report 2026-04-21. **D61 deferred follow-on closed**. Automates physical xcopy of `D:\DevTools\UEMCP\plugin\UEMCP\` → `<target>\Plugins\UEMCP\` with `Binaries\`+`Intermediate\` exclusion preserving per-project UBT cache. GUI browse via PowerShell `OpenFileDialog` when no .uproject arg; explicit arg form via `sync-plugin.bat "<uproject>" [-y]` for scripted use. **CMD-parser gotchas encountered + fixed** (worker report §Edge cases): (1) `(call )` pattern for DLL write-lock probe caused CMD parse errors inside nested IF blocks → replaced with `tasklist` + DLL-existence check; (2) bare parens in `echo ... (auto-yes).` closed outer IF block prematurely → replaced with `[auto-yes]`; (3) `^^` echo continuations for multi-line nuke-rebuild hint broke parsing → flattened to single line. These are the same CMD-parser class of issues encountered during `setup-uemcp.bat` debug; pattern is "parens/carets inside IF blocks break; use labels or simpler control flow." **DRY follow-on (not urgent)**: `setup-uemcp.bat` duplicates plugin-copy logic at lines 181-234 without the Binaries/Intermediate exclusion. Could delegate to `sync-plugin.bat` but current duplication works; backlog candidate if setup-uemcp.bat needs revision later. **Future plugin workers**: use `sync-plugin.bat "<uproject>" -y` for scripted sync instead of hand-crafting xcopy commands. Supersedes the manual D61 recipe for the copy step; D61 nuke-rebuild recipe is still the remedy when UBT cache staleness is suspected. |
| D65 | UEMCPModule diagnostic log demoted Warning → Log, 2026-04-21 (commit `60bb94a`, +1/-5). D61 follow-on closed; editor console no longer polluted by the D61 debug-arc artifact. | Source: log-demotion worker final report 2026-04-21. **Clean rebuild via D61 nuke recipe** — 16 actions, 12.19s. DLL mtime 17:05:06 post source mtime 16:53:54, confirming no UBT cache staleness (cf. D61 root-cause pattern). Gate test re-run [PASS] — D57 gate line fires at 17:05:24 from fresh DLL proving demoted StartupModule body runs correctly. **Wave-1 4-worker parallel dispatch fully closed**: Oracle-A (D62) + EN-8/9 (D63) + sync-plugin.bat (D64) + log-demotion (D65) all shipped clean in a single session window. All 4 path-limited per D49; zero merge conflicts despite parallel dispatch. **Baseline unchanged at 914** (log-demotion is C++-only; doesn't touch JS tests). **D61 diagnostic-log cleanup marker**: historical reference inside D61 entry's "Notes" cell mentions "Diagnostic log cleanup deferred" — that sub-item is now closed by D65; D61 entry itself is a dated snapshot and not rewritten. **Orchestrator lesson**: 4 workers parallel-dispatched with D49 path-limited commits landed zero conflicts over ~single session window. Validates the parallel-kickoff pattern for future waves where worker scopes are genuinely file-isolated. |
| D66 | FA-ε verdict: **HYBRID M-enhance transport** with RC HTTP (30010) for flat UPROPERTY/UFUNCTION reads + metadata subset, TCP (55558) for compile diagnostics / UEdGraph walks / compiled-state / editor-static functions. **Phase 4 as a scheduled milestone is ABSORBED into M-enhance**; D23's Layer 4 semantic allocation for RC-based reflection reads persists. Split rule is server-side (tool signatures stay transport-agnostic; RC-vs-TCP dispatch is implementation detail). Aggregate Phase 3 delta: **−2 to −4 sessions** from collapsing Phase 4 as separate kickoff. | Source: `docs/research/fa-epsilon-tcp-vs-rc-2026-04-21.md` (commit `56ff6f6`, 404 lines). **Load-bearing inputs (all empirically grounded)**: (1) UE 5.6 `RemoteControlModels.h:31-46` — `SanitizeMetadata` allowlist hard-limits RC-exposed Metadata to 5 fields `{UIMin, UIMax, ClampMin, ClampMax, ToolTip}`. Category/Replicated/EditAnywhere flags are deliberately NOT on the HTTP surface → RC reflection coverage capped at PARTIAL. (2) `UBlueprintEditorLibrary::CompileBlueprint` is `UFUNCTION(void)` — RC can trigger compile, cannot capture `FCompilerResultsLog`. Plugin C++ required regardless of transport. (3) Phase-4-collapse delta saves 2-4 sessions aggregate (§Q3.4 in deliverable) with zero D23 architectural cost — decisive cost arbitrage. **Architectural move — separating "scheduled milestone" from "architectural layer"**: Phase 4 as a kickoff-ordering label collapses, but D23's Layer 4 semantic allocation for RC-based reflection reads persists. This is the lever that lets HYBRID respect D23 without amending it. **Agent-facing signature invariant**: tool signatures stay transport-agnostic; dispatcher internally routes RC-or-TCP based on data-shape discriminator. Claude never sees two tools for "read BP variable tooltip vs flag set" — one tool, RC is the internal substrate for the free subset. **Cross-reference amendments** (later entries supersede earlier framing — readers follow D66): **D23** (4-layer architecture) — layer-vs-schedule distinction clarified; Layer 4 remains architecturally, Phase 4 as scheduled milestone does not. **D53** (86-tool Phase 3 surface) — category-d tools split across L3 (TCP) and L4 (RC) per split rule. **D58** (re-sequence + FA-ε flag) — FA-ε now resolved HYBRID; Phase 4 absorbed. **D52** (near-plugin-parity) — RC-subset refinement: some plugin-parity functionality achievable via RC instead of plugin code (narrows plugin-gated surface). **M-enhance cost: 3-5 sessions unchanged** from D58 baseline; aggregate −2 to −4 sessions comes from Phase 4 absorption, not M-enhance itself. **Open items** (tracked for M-enhance handoff / follow-on work): (a) WebSocket subscription surface deferred — deep RC WS capability not explored; (b) PARTIAL-RC tool implementation strategy recommends TCP-external-signature-with-RC-internal-substrate pattern; (c) UE 5.7 RC version-drift unverified (minor risk for S-B-overrides timing); (d) cross-transport transaction semantics flagged for M-enhance test plan. **Next action**: M-enhance handoff draftable — §Q1 coverage table in deliverable is per-tool work breakdown input. **Test baseline unchanged** at 914 (research deliverable, no JS/C++ edits). |
| D67 | S-B-base Session 1 blocker: UE regenerates pin IDs on every load for K2Node_EditablePinBase subclasses (FunctionEntry, FunctionResult, CustomEvent) and K2Node_PromotableOperator via `PostLoad → ReconstructNode → AllocateDefaultPins` which calls `FGuid::NewGuid()`. **Disk pin IDs are load-session-ephemeral for these types** — they reflect whatever random GUIDs some prior load generated, frozen at save time. Next load generates different GUIDs. Oracle captures one specific in-memory set; parser sees a different on-disk set. They can NEVER agree across separate load sessions. **Path A (re-save) verified negative**: Noah re-saved all 6 fixture BPs in editor 2026-04-21 to 2026-04-22; differential numbers byte-identical pre/post-save (772/749 match on BP_OSPlayerR unchanged). **Path B rejected**: 3.5% edge miss rate (34/962) concentrates entirely in K2Node_FunctionEntry — the natural start node for exec-chain tracing from functions. Documenting the exclusion disables `bp_trace_exec` from function entries, violating D52 near-parity for every function in every BP. **Going Path C**: Oracle-A-v2 amendment emits `pin_name` alongside `pin_id`; differential harness matches on (node_guid, pin_name) tuples instead of (node_guid, pin_id). | Source: S-B-base worker Session 1 final report (commits `cdf951b` + `e35f431`, no Session 2 commits per verification-only pass). **Worker's empirical measurement**: all 4 clean fixtures remain 100%; BP_OSPlayerR still 749/772 + 669/673 BP_OSControlPoint. **UE architecture fact captured**: `UEdGraphNode::AllocateDefaultPins` for these specific node types unconditionally `FGuid::NewGuid()`'s on every load. This is the same root cause that Agent 11.5 §2.1 identified implicitly — "pin IDs are runtime-generated for type-inference nodes" — but at the disk-vs-memory ID-instability level, not the format-instability level. **Scope of Path C**: (a) plugin-side: `plugin/UEMCP/Source/UEMCP/Private/Commandlets/EdgeOnlyBPSerializer.cpp` adds `"pin_name"` field; schema_version bumps `oracle-a-v1` → `oracle-a-v2`; regen all 6 fixtures. Micro-worker, ~30-60 min. (b) server-side: S-B-base worker resumes with differential harness switch to name-based matching; parser already captures `PinName` via existing `cur.skip(8)` call in `readPinBody`. ~15-30 min additive to existing session. **File-collision-safe dispatch order**: Oracle-A-v2 first (plugin + fixtures touch), then S-B-base resume (server/* scope). **Cost delta**: ~45-60 min wall-clock for the combined Path C vs Path B's "ship-now" shortcut. Acceptable trade-off for D52 compliance. **Forward-compat implication for Verb-surface**: `extractBPEdgeTopology` output shape should include pin_name per pin (not just pin_id) so Verb-surface verbs can resolve pins by name when needed. Already noted in M-new Verb-surface handoff §2 implicitly; amend if needed. **Forward-compat implication for M-enhance narrow-sidecar**: same — pin_name is a first-class field, pin_id is an ephemeral implementation detail for K2Node_EditablePinBase subclasses. **UE version note**: this behavior has been stable across UE 4.x → 5.6 per `UEdGraphNode::AllocateDefaultPins` implementations; unlikely to change in 5.7 but flag for S-B-overrides verification. **Test baseline unchanged at 966** (worker's Session 1 CP1+CP2 count preserved; verification-only Session 2 shipped no new assertions). |
| D68 | Orchestrator research pass correcting D67's root-cause understanding before Oracle-A-v2 dispatch. **Empirical finding**: two sequential cold-start `DumpBPGraph` commandlet runs against BP_OSPlayerR produced BYTE-IDENTICAL JSON output (SHA256 match). This DISPROVES D67's (and worker's) claim that UE calls `FGuid::NewGuid()` per load in `AllocateDefaultPins`. Post-load pin IDs are actually DETERMINISTIC (stable across cold loads) — likely seeded from node context (signature hash + NodeGuid + pin index) rather than random. **Revised root cause**: post-load IDs for K2Node_EditablePinBase subclasses + K2Node_PromotableOperator are DETERMINISTICALLY-DIFFERENT from disk IDs — UE generates them from node context rather than reading from disk, so they're stable run-to-run but don't match what's serialized. Disk retains legacy IDs from when pins were first created; post-load IDs reflect current-signature-derived canonical values. | Source: orchestrator inline research pass 2026-04-22. **Test conducted**: ran `UnrealEditor-Cmd.exe -run=DumpBPGraph` twice against `/Game/Blueprints/Character/BP_OSPlayerR` with editor closed between runs. Both invocations produced `SHA256=1ffe14e325b49ad5d36efed0b2ac5108eac0077c4b73ee5e989c5799246ff060`. Also byte-identical to committed fixture — confirms oracle stability is baseline-property, not happenstance. **Secondary finding**: of BP_OSPlayerR's 11 K2Node_EditablePinBase / K2Node_PromotableOperator nodes, only 1 (ApplyVFX_Niagara FunctionEntry) shows parser-vs-oracle divergence. 10/11 match perfectly. If the mechanism were universal per-load ID regeneration, all 11 would diverge. Narrow divergence suggests the issue is **stale disk data on that specific node** — function signature was edited from 23 params to 13, but disk still holds the 23-param serialization. UE's `AllocateDefaultPins` generates IDs for the current 13-param signature; the 10 legacy disk pins are silently dropped at load. **Plus**: their IDs don't match the 13 current IDs because the current IDs are signature-derived while the 10 legacy pins have IDs from their original creation time. **Implication for Path A (re-save)**: theoretically could work if UE's save path re-serializes post-load state (dropping the 10 legacy pins). Not tested with a forced-dirty save due to P4 exclusive-checkout constraint on BP_OSPlayerR by teammate — coordinating the save has higher friction than Path C dispatch. Path A remains untested, not disproven. **Implication for Path C design**: hybrid ID+name matching is the correct design, NOT name-replaces-id. Oracle emits BOTH pin_id (stable, deterministic) AND pin_name (stable, semantic). Differential harness matches by pin_id first (96.5% coverage with strong evidence that two IDs map to the same pin); falls back to (node_guid, pin_name) matching for unmatched entries (covers the 3.5% K2Node_EditablePinBase divergence). This is belt-and-suspenders: ID match is stronger evidence of equivalence; name match is the robust fallback for deterministic-but-not-disk-matched cases. **Oracle-A-v2 handoff updated**: plugin-side emit was already correct (adds name alongside id, keeps id). Server-side harness spec clarified to hybrid match. **Also**: P4 ettiquette research — making a read-only P4-locked file writable without `p4 edit` is a silent-fork footgun (saves succeed locally, P4 rejects submit, next sync clobbers). Documented as best-practice reminder for future C++/BP work where teammates may hold exclusive checkouts. **Corrects D67's framing**, does not invalidate Path C verdict — same endpoint, better-understood reasoning. Readers should follow D68 for current M-new Oracle-A-v2 / S-B-base context. **Test baseline unchanged at 966**. |
| D69 | M-new Oracle-A-v2 landed 2026-04-22 (commit `b8ea754`, 9 files, 1603+/14−, path-limited per D49). Plugin compile clean after transient-PCH-VM-exhaustion retry (first Build.bat hit C1076/C3859 on 3 unrelated Project A files; retry without changes succeeded — UnrealEditor-UEMCP.dll relinked 09:41). All 6 fixtures regenerated with `name` field populated alongside preserved `pin_id` primary key. Schema confirmed `oracle-a-v2` across all fixtures. D57 gate test [PASS] preserved. **D68 theory empirically validated**: BP_OSPlayerR ApplyVFX_Niagara K2Node_FunctionEntry emitted exactly 13 pin names matching the current function signature: `then, AuraSystem, Lifetime, SpawnRate, SpawnRate2, SpawnCount, ManualScale, Emissive, Opacity, MaterialInterface1-4`. Parser sees 23 disk pins; the 10 extras are historical/stale entries from prior signature versions. Pin NAMES match current signature exactly — hybrid match will close the gap. | Source: Oracle-A-v2 worker final report 2026-04-22. **Commit breakdown**: 1603 insertions dominated by fixture JSON expansion (one `"name": "..."` line per pin × ~770 pins in BP_OSPlayerR = ~770 lines); source-code diff is single-digit LOC. Minimal-footprint amendment with large fixture footprint — D49 path-limited keeps blast radius contained so S-B-base can cherry-pick or bisect cleanly. **Pre-existing Project A UHT error fixed separately by user** at `OSCheatManager.h:152` (UPROPERTY inside `#if !UE_BUILD_SHIPPING` guard); unrelated to UEMCP but blocked the build until resolved. Flag for future C++ workers: Project A has some latent UHT-strict issues that can surface during plugin rebuilds — if Build.bat hits UHT errors on non-UEMCP files, they're pre-existing and not the UEMCP worker's responsibility to fix but may need coordination with Project A maintainer to resolve. **Transient PCH VM exhaustion pattern**: Build.bat hit C1076/C3859 (out-of-memory for PCH) on first try, succeeded on retry without source changes. This is a Windows paging/memory-pressure artifact, not a real error. Future C++ workers should retry once on C1076 before assuming real compile failure. **Test baseline unchanged** at 966 (plugin-only change; JS tests not affected). **Next critical path**: S-B-base worker's existing session resumes with hybrid-match prompt — pin_id primary pass (~96.5%) + (node_guid, name) fallback (~3.5% K2Node_EditablePinBase divergence), continue CP3-6 per handoff §8 checkpoint structure. Ship gate unchanged: 100% edge match via hybrid on all 6 fixtures. |
| D70 | M-new S-B-base landed 2026-04-22 (commits `cdf951b` + `e35f431` + `3c355fe` + `9250121`). 962/962 edges match (100%) against Oracle-A-v2 on all 6 fixtures via **pure ID-match**; name-fallback counted 0 — D68/D69 hybrid-match infrastructure is architecturally present as safety net but not structurally required by the corpus. Shipped in **3 sessions** (under the 4-6 estimate). Test rotation **914 → ~1034** (+120 assertions: 36 CP1 + 16 CP2 + 68 differential). Corrects D67/D68 root-cause framing. | Source: S-B-base worker final report 2026-04-22. **Revised root cause for the Session 1 reported "23 vs 12 pin divergence"**: a **test-harness map-collision bug in the worker's own harness code** that used lossy NodeGuid-only keys, compounded by **2 genuinely regenerated K2Node_PromotableOperator pins** (zero edge impact). NodeGuids can legitimately collide across sibling UEdGraphs in the same BP — any map keyed only on NodeGuid silently drops data when two graphs share a guid. Session 3's corrected harness (keying on `(graph_name, node_guid, pin_id)` triples) hit 100% match first-pass without needing hybrid-match fallback. **D67 "FGuid::NewGuid() per load" and D68 "deterministic-but-divergent" theories both superseded** — disk IDs and post-load IDs actually MATCH for ~99%+ of pins including the K2Node_FunctionEntry case that originally looked divergent. The worker's initial empirical measurement was an artifact of their own test harness, not a UE architectural fact. **Oracle-A-v2 pivot still defensively valid**: name-fallback safety net unused but exists for future format drifts where hybrid-match may be structurally required (UE 5.7 delta, edge-case node types). Not wasted work. **Empirical K2Node coverage (15 format-verified via corpus)**: K2Node_CallFunction (155 instances), VariableGet (65), Knot (38), FunctionEntry (21), Event (21), IfThenElse (15), VariableSet (13), MacroInstance (10), CallParentFunction (10), SwitchEnum (7), CustomEvent (6), BreakStruct (6), AddComponent (6), PromotableOperator (4), ExecutionSequence (3), plus EdGraphNode_Comment (17). Format-extrapolated (not exercised by corpus): Timeline, CreateDelegate, Composite, Tunnel, Return, InputAction. Parser uses broad predicate `startsWith('K2Node_') \|\| === 'EdGraphNode_Comment'` covering all 30+ distinct classes. **Critical binary-format findings captured in D70**: (a) **4-byte int32 sentinel = 0** written between UObject tagged-UPROPERTY `None` terminator and pin trailer — NOT documented in `EdGraphNode.cpp`; worker verified across 415 graph-node exports. Any future parser extensions or S-B-overrides 5.7 port must preserve this read. (b) **FEdGraphPinType.FArchive bool is int32 (4 bytes), not 1 byte** — matches CUE4Parse reference. 69-byte minimum non-Map size; +32 bytes for Map container. (c) **NodeGuid non-uniqueness across sibling graphs** — schema must key by `(graph_name, node_guid, pin_id)` triples; D70 documents this as a first-class invariant for Verb-surface and M-enhance consumers. **Verb-surface late-binding details (fills Verb-surface handoff markers)**: exported function is `extractBPEdgeTopologySafe` (note `Safe` suffix indicating withAssetExistenceCheck wrap); output shape `{schema_version: 'sb-base-v1', asset_path, graphs, stats}` Oracle-A-v2-aligned; `stats` exposes `edgesEmitted` + `danglingEdges` for observability; self-loops preserved; `bNullPtr` / `bOrphanedPin` pins dropped upstream at parser level (Verb-surface doesn't need to filter). **Hint for S-B-overrides (UE 5.6→5.7 delta)** per worker §7: watch `FEdGraphPinType.Serialize`'s trailing bool `bSerializeAsSinglePrecisionFloat` — most likely 5.7 addition slot is another trailing bool there. Also verify `FText HistoryType` enum additions; parser currently handles only -1 (None) + 0 (Base); any new types throw. **Pre-existing drift failures to flag**: 7 failures on `test-phase1` (3) + `test-uasset-parser` (4) caused by Noah's BP_OSPlayerR/Player CDO re-saves (path A experiment) changing property layouts. Out of S-B-base D49 scope; worker confirmed pre-existing via git-stash verification. Backlog candidate for a fixture-refresh or test-expected-value-update worker. **Pending orchestrator housekeeping**: add `test-s-b-base-differential.mjs` to `package.json` test-rotation + `CLAUDE.md` test-file table. Test rotation count in CLAUDE.md needs updating 914 → ~1034 (or ~1041 once drift-failures resolved). **Next critical path unblocked**: Verb-surface + S-B-overrides are now parallelizable (Verb-surface touches `offline-tools.mjs` + `tools.yaml` + `server.mjs`; S-B-overrides touches `uasset-parser.mjs` primarily — different files, can dispatch in parallel). |
| D71 | CL-1 drift refresh landed 2026-04-22 (commit `1f0dd69`). Two kinds of drift fixed: (a) **threshold drift** on `test-phase1.mjs` P2 setup — `max_bytes=500` for size_budget_exceeded marker stopped firing on BP_OSPlayerR because property stream shrunk post-refactor; dropped threshold to 300 with margin (single root cause → 3 cascading asserts fixed). (b) **fixture swap** on `test-uasset-parser.mjs` L2.5 TArray — BP_OSPlayerR CDO no longer carries `DefaultAbilities`/`DefaultEffects` properties (removed during gameplay refactor, not byte-drift); switched test fixture to sibling `BP_OSPlayerR_VikramProto` (at `Content/WwiseAudio/Blueprints/`) which retains the original class shape. Parser code paths exercised are unchanged; test intent (`TArray<ObjectProperty>` decoding) preserved. Expected values updated: `DefaultAbilities.length` 15 → 10, `DefaultEffects` unchanged at 3, CDO export name switched to `Default__BP_OSPlayerR_VikramProto_C`. Test counts: pre-fix 1027 passing / 7 failing → post-fix **1034 passing / 0 failing** across all 9 test files. | Source: CL-1 worker final report 2026-04-22. **Worker flagged fixture swap as borderline drift/semantic for orchestrator review**: original BP_OSPlayerR losing DefaultAbilities/DefaultEffects is a content-side observation (gameplay refactor removed the GAS defaults from the main player; sibling proto character retains them). This is a content-side fact about Project A, NOT a UEMCP regression. Parser behavior + test intent preserved via sibling fixture. **Orchestrator review resolution**: fixture swap is acceptable cleanup because (a) test purpose (exercise TArray<ObjectProperty> decode path) preserved; (b) parser code unchanged; (c) no assertion got silently rubber-stamped as drift when it was actually semantic — the worker specifically surfaced the distinction. Future CL-class workers should follow this pattern: when a failure looks semantic, surface to orchestrator with the distinction, don't pretend it's drift. **CL-2 deferred**: worker correctly noted that CLAUDE.md still references `899 assertions` (pre-EN-8/9 baseline) and `test-s-b-base-differential.mjs` isn't in the test-file table — proper CLAUDE.md bump requires broader review than CL-1's scope. Handled inline in this commit via orchestrator pass — see same commit as D71. **Content-side heads-up for Noah (not action-required)**: if BP_OSPlayerR losing GAS defaults was UNINTENTIONAL (refactor regression), worth a separate check; if intentional (architectural move to external data), no concern. Flag is purely informational — not UEMCP-scoped. **Parallel Verb-surface worker unaffected**: zero file collision (CL-1 owned test-phase1 + test-uasset-parser; Verb-surface owns offline-tools + NEW test-verb-surface.mjs + tools.yaml + server.mjs). Verb-surface in flight at commit-time. **Test baseline FINAL**: 1034 passing. |
| D72 | M-new Verb-surface landed 2026-04-22 (commits `aa131cd` core + `8acd0b9` placeholder refresh). 5 verbs ship offline-primary per D58 "MCP-first, plugin-enhances": `bp_trace_exec`, `bp_trace_data`, `bp_neighbors`, plus M-new extensions to `bp_show_node` (pins[] populated via topology when guid resolves) and `bp_list_entry_points` (has_no_exec_in precision). Test delta to **1052 passing / 0 failing** across 9 files (test-verb-surface.mjs +83 assertions new suite; test-phase1.mjs net +3 from placeholder refresh). Oracle-cross-check exercised on 3 required fixtures. | Source: Verb-surface worker final report 2026-04-22. **Two empirical format gotchas discovered + handled**: (a) **PinCategory NOT captured in S-B-base's pin-block parse** — `bp_trace_exec` / `bp_trace_data` use name-convention classifier (`then`/`else`/`completed`/`exec` → exec; everything else → data) with a documented Default false-positive risk. Future refinement candidate if precision gaps surface in agent workflows. (b) **NodeGuid hex format mismatch across subsystems**: M-spatial emits LE-lowercase hex (e.g., `ebd309ad6d0969e809fe3b3b8bc8e863b`); S-B-base topology emits BE-uppercase-per-uint32 (e.g., `AD09D3EB49096DE620CA59B83BE8C89F`); Oracle-A-v2 matches S-B-base format. Verb-surface added `toOracleHexGuid` helper to bridge the two formats at verb handler edge. **Future-forward-compat rule for downstream workers (M-enhance, S-B-overrides)**: when correlating NodeGuids across M-spatial ↔ S-B-base/Oracle-A-v2, either call `toOracleHexGuid` (canonical) or match both formats. This is a real interop hazard; document in relevant handoffs. **Scope-deviation landed correctly**: worker violated "do not touch test-phase1.mjs" path-limit for legitimate reason — 4 M-spatial placeholder assertions (literally labeled "M-new fills this in") were semantically invalidated by shipping M-new. CL-1 (`5281676`) had landed BEFORE the Verb-surface edit, so collision risk was zero at that moment. Worker split into separate commit (`8acd0b9`) for independent revert. **This is the RIGHT pattern for path-limit exceptions**: (1) wait for the other worker to land first (no live collision), (2) commit the exception separately (reversibility), (3) flag in final report for orchestrator review. Don't punish; this is how path-limits work in practice when scope boundaries reveal emergent coupling. **BP-subclass variance gap flagged for S-B-overrides**: UWidgetBlueprint + UAnimBlueprint not in Oracle-A corpus; Verb-surface has zero data on whether edge-topology parse matches these subclasses. **S-B-overrides first calibration target**: exercise verbs against a UWidgetBlueprint and UAnimBP fixture BEFORE assuming UBlueprint-only parity extends automatically. **yaml deltas**: 3 new offline entries (`bp_trace_exec`, `bp_trace_data`, `bp_neighbors`); `bp_show_node` + `bp_list_entry_points` descriptions refreshed to match M-new FA-β manifest. No TOOLSET_TIPS updates — all 5 verbs in same always-on offline toolset. **Next critical path**: M-enhance dispatchable in parallel (no Verb-surface dependency per handoff §Final report #8). Wave 3 partially unblocked; M3/M4/M5 depend on M-enhance or can parallelize further. **Test baseline**: 1034 (post-CL-1) → 1052 (post-Verb-surface) +18 NET after placeholder refresh (83 new suite, −4 placeholders −61 apparent accounting drift — worker's pre-count reported 966 which was a stale mid-S-B-base number; actual baseline pre-Verb-surface was 1034, post is 1052 verified in worker's rotation). |
| D73 | T-1a L2.5 synthetic fixture migration landed 2026-04-22 (commit `525d7843`). Live-fixture dependency on `BP_OSPlayerR_VikramProto` replaced with hand-constructed synthetic bytes via Approach A (extended existing synthetic helpers at `test-uasset-parser.mjs` lines 1238-1292 — pseudo-tags + pseudo-resolvers pattern already mature in-file for TArray<IntProperty/FloatProperty/FVector>/TSet/TMap coverage). L2.5 assertion count 7→7 (zero shift; original was 3 colors + 3 abilities + 1 effects, not 4 as handoff mis-stated). Test rotation **1037 passing / 0 failing** across 9 files. **No parser bug surfaced** — synthetic bytes drive same code paths (`SCALAR_ELEMENT_READERS.ObjectProperty` + `readFLinearColorBinary`) as live-fixture path. | Source: T-1a worker final report 2026-04-22. **Test-count reconciliation vs D72**: D72 recorded 1052 post-Verb-surface per worker's self-report. T-1a's ground-truth measurement found 1037; delta=0 on their change means the pre-T-1a baseline was actually 1037, not 1052. Verb-surface worker's arithmetic was off (worker's pre-count `baseline (post-CL-1) 966 → 1052` used a stale mid-S-B-base figure rather than the real post-CL-1 count of 1034; actual growth was +3 from their work). **Authoritative baseline going forward: 1037**. CLAUDE.md needs a minor bump from 1052 → 1037 in the narrative + Testing section; not urgent, bundle with next housekeeping pass. **First successful fixture-philosophy migration**: T-1a validates that the synthetic-fixture approach works for the simplest tier (direct UObject FProperty stream decoding) with minimal effort (~30-45 min). **T-1b feasibility assessment from worker**: `test-phase1.mjs`, `test-query-asset-registry.mjs`, `test-inspect-and-level-actors.mjs` read FULL .uasset packages — name tables + import/export tables + AR tag blocks. Hand-rolling those shapes would require ~300-500 bytes of synthetic scaffolding per test vs L2.5's trivial extension. Feasible but **3-5× the effort** of T-1a. **Recommended T-1b policy**: per-test decision — evaluate synthetic cost vs. engine-stable tier-2 option (UE 5.6 Engine/Content/*.uasset files as stable cross-project fixtures). Synthetic for tight unit-level decoding; engine-stable for integration-level package reads. **BP_OSPlayerR_VikramProto dependency removed** from test JS; the Project A asset is untouched and remains in P4. Parallel M-enhance worker unaffected — zero file collision per pre-dispatch scope audit. **Test baseline FINAL for orchestrator planning**: 1037 passing / 0 failing. |
| D74 | M-enhance Session 1 landed CP1 + CP2 per handoff §8 checkpoint structure (commits `12b1a13` CP1 + `7e91e1d` CP2). CP3-6 deferred to next session — CP3 requires editor-closed plugin rebuilds, clean fresh-session boundary. Test baseline **1037 → 1109** (+72: CP1 +35 wire/translator, CP2 +37 RC handlers). All 10 test files green, zero regressions. **11 FULL-RC tools** shipped (8 rc_* primitives + 3 semantic delegates: `list_material_parameters`, `get_curve_asset`, `get_mesh_info`), all dispatch RC-only. D66 §Q6 "TCP-external-signature with RC-internal-substrate" pattern crystallized in new `rc-tools.mjs:buildRcRequest()`. | Source: M-enhance worker Session 1 final report 2026-04-22. **Two handoff drifts flagged by worker for D-log correction** (documented here per worker recommendation rather than amending handoff post-fact): (a) **Handoff §4 "Group FULL-RC (10 tools)" vs FA-ε §Q1 coverage table (11 tools)** — handoff summary text said 10 but body enumeration resolved to 11. Worker implemented all 11 per FA-ε ground truth. Arithmetic mismatch caught + resolved correctly. (b) **Env var: handoff said `UEMCP_RC_PORT` but existing code uses `UNREAL_RC_PORT`** per server.mjs:47 + risks-and-decisions.md line 36. Worker kept `UNREAL_RC_PORT` to match existing convention. Future M-enhance handoffs or derivatives should use `UNREAL_RC_PORT`. **Cross-worker handoff-vs-code-drift lesson**: when a handoff cites an env var, constant, or API name, the worker should cross-reference against current code and use whichever matches existing convention — don't introduce new name aliases. **Test-count reconciliation**: worker's pre-session measurement of 1037 baseline aligns with D73's authoritative figure (not the stale 1052 D72 recorded). Verb-surface's accounting drift confirmed via third independent measurement now. **HTTP-empty-body-on-PUT-success** normalized as `{success: true}` in `httpCommand` — keeps `extractWireError` uniform across transport layers. Minor but load-bearing invariant for future RC tool authors. **Open items carried forward to Session 2** (all from FA-ε §Open items, unchanged scope): (1) cross-transport transaction semantics unverified — deferred to CP4 test plan; (2) UE 5.7 RC version-drift flagged for S-B-overrides timing; (3) PIE teardown race with TCP — watch at CP4 start_pie/stop_pie handler. **Pre-CP3 prerequisite flagged**: Project A.uproject's `Plugins[]` does NOT yet list RemoteControl as enabled. RC ships as engine plugin (discoverable) but isn't enabled-by-default — CP3 integration smoke needs editor-side RC web server running. **Resolution TBD**: likely auto-enabled via UEMCP.uplugin's `Plugins[]` dep chain (worker added RC entry per handoff §2.1); verify at CP3 start. If not auto-enabled, orchestrator needs to coordinate with Project A team to add `{"Name": "RemoteControl", "Enabled": true}` to Project A.uproject (P4 change). **No blockers**. Session 2 dispatchable when Noah's next-session slot opens. **Test baseline authoritative**: 1109 passing / 0 failing across 10 test files (new `test-rc-wire.mjs` suite added). |
| D75 | M-enhance Session 2 landed CP3 + CP4-partial per handoff §8 (commits `ca479f7` CP3 plugin C++ + `1e811b7` CP4 partial TCP JS handlers). **10 plugin command handlers shipped** at ~1281 LOC across 8 new files + 2 modified: `bp_compile_and_report` (FKismetEditorUtilities::CompileBlueprint out-param overload — the FULL-TCP path per D66 because RC can't capture FCompilerResultsLog), `reflection_walk` (bypasses RC SanitizeMetadata allowlist with full EPropertyFlags/EClassFlags/EFunctionFlags), `get_material_graph`, `get_event_dispatchers`, `get_editor_state`, `start_pie`, `stop_pie`, `is_pie_running`, `execute_console_command`, `get_widget_blueprint`, `get_asset_references`. Build.cs adds UMG + UMGEditor + AssetRegistry + LevelEditor deps per UBT D60 rule. **CP4 partial**: 10 FULL-TCP JS handlers via new `menhance-tcp-tools.mjs` (note: wire-type translation `get_blueprint_event_dispatchers` → `get_event_dispatchers`); new yaml entry for `bp_compile_and_report` in blueprint-read toolset. **+28 assertions**. **Deferred to Session 3** (scope preview): 13 PARTIAL-RC hybrid-dispatch tools (compound RC-primary/TCP-fallback — warrants own pattern + test regime); visual capture (FPreviewScene + FWidgetRenderer heavier than bundled edge cases); struct_definition + bp_functions plugin handlers; CP5 narrow sidecar + save-hook + 3F-4 commandlet + editor-menu; CP6 yaml polish + TOOLSET_TIPS + final rotation. | Source: M-enhance worker Session 2 final report 2026-04-22. **Test baseline: 1109 → 1129 passing on M-enhance-owned files** (+28 CP4 net; CP3 is C++-only so no JS assertion delta). **8 PRE-EXISTING fixture-drift failures surfaced** (worker verified reproducible on clean HEAD without their commits) — second recurrence of the project-specific-fixture drift pattern that motivated T-1 philosophy: (a) 3 failures on `test-uasset-parser.mjs`, (b) 5 failures on `test-s-b-base-differential.mjs`. Root cause: **Noah edited BP_OSControlPoint between Sessions 1-2** (probably a P4 sync or intentional edit); parser now sees 223 exports / 398 edges vs stale oracle's 182 / 330. **Fix = Oracle-A-v2 commandlet re-run** against BP_OSControlPoint (analog to the re-save handling pattern from D69 / Session 1 era): editor closed → commandlet regenerates `BP_OSControlPoint.oracle.json` matching current disk state → differential harness passes. Worker correctly did NOT attempt this fix — cross-file scope + requires editor-closed environment + content-owner (Noah) coordination. **Recurring-drift lesson reinforced**: BP_OSControlPoint is the second Project A BP to drift in as many cycles (BP_OSPlayerR in D71's CL-1, BP_OSControlPoint now). Accelerating T-1b/c (fixture-philosophy migration) is cost-justified — every future M-enhance or sidecar session risks another drift encounter. Backlog priority on T-1b bumps one tier. **RemoteControl plugin enable status**: Project A.uproject `Plugins[]` does NOT explicitly list RC. Worker's CP1 UEMCP.uplugin dep-chain entry requests it; UE's plugin dependency transitively enables on editor launch IF the chain resolves. Verification requires user action post-sync-plugin.bat + Build.bat (editor launch with RC module loaded). If RC fails to load, Noah needs to add `{"Name": "RemoteControl", "Enabled": true}` to Project A.uproject (P4 change). **Pending user actions** (Noah or coordinated with Project A content owner): (1) close editor → `sync-plugin.bat "path\to\YourProject.uproject"` → `Build.bat YourProjectEditor Win64 Development -project=path\to\YourProject.uproject -WaitMutex` → open editor → verify RC plugin loads + per-handler smoke test; (2) regenerate `BP_OSControlPoint.oracle.json` via `DumpBPGraph` commandlet to resolve the 8 fixture-drift failures. **UE 5.6 LevelEditor linkage note for future plugin workers**: `GCurrentLevelEditingViewportClient` requires `LevelEditor` module dep, not `UnrealEd` alone — UnrealEd has `extern` declaration only. Captured for D-log institutional memory. **No blockers for Session 3 dispatch** — can dispatch before or after Noah's user-action items; fixture regen is orthogonal to remaining Session 3 scope. **Authoritative test baseline**: 1129 passing (on clean-oracle state post-fixture-regen; currently 1121 passing + 8 drift-failing pending regen). |
| D76 | M-enhance Session 3 landed CP4-remainder + CP5-MVP + CP6-MVP (commits `0964cd9` CP4-rem, `576d69b` CP5 SidecarWriter + save-hook + regenerate_sidecar MCP cmd, `dc41193` CP6 TOOLSET_TIPS for remote-control / blueprint-read / sidecar toolsets). **Cumulative M-enhance totals across Sessions 1-3**: 35 agent-facing tools live (11 FULL-RC + 10 FULL-TCP + 13 PARTIAL-RC + 1 sidecar command), 14 plugin-C++ command handlers on TCP:55558, HTTP:30010 transport infrastructure + URL translator + RC client, OnObjectPreSave-based save-hook for auto-sidecar writes. Phase 4 absorption confirmed complete — tools.yaml `remote-control` toolset flipped to `priority: 3` in CP2; no `priority: null # Phase 4` remnants. | Source: M-enhance worker Session 3 final report 2026-04-22 / 2026-04-23 boundary. **Pragmatic PARTIAL-RC refinement worth capturing**: FA-ε original verdict (D66) framed the 13 PARTIAL-RC tools as "TCP-external signature with RC-internal substrate" (RC for the metadata-allowlist-covered subset, TCP for the flag-full rest, hybrid dispatch). Worker chose **all-13 dispatch to plugin-TCP this session** since plugin's `reflection_walk` (shipped CP3) already delivers what RC's SanitizeMetadata withholds — plugin-primary is semantically sufficient for correctness. RC augmentation for latency optimization is deferred to a future session when workflow pressure surfaces. **This is a correct framing**: the hybrid dispatch is an optimization, not a requirement. FA-ε verdict preserved in spirit; implementation chose the simpler correctness-first path. **Narrow sidecar schema finalized as `narrow-sidecar-v1`**: `{schema_version, engine_version, asset_path, written_at, compile_status: {status, is_compiled_ok}, runtime_derivatives: {generated_class_path, reflection: <SerializeClassReflection output>}}`. Deliberately narrower than M-enhance handoff §5 suggested — worker correctly dropped `via_knots` annotation because that belongs in offline post-pass per `phase3-resequence-mcp-first-2026-04-20.md §L` (handoff §5 bullet referenced pre-D58 framing; stale). **UE 5.6 API finding for future C++ workers**: `FCoreUObjectDelegates::OnObjectSaved` is `UE_DEPRECATED(5.0)` — use `OnObjectPreSave` (non-deprecated). Save-hook fires before serialization; sidecar reflects in-memory derived state so pre/post-save equivalence is preserved. **Test baseline**: 1137 → **1196 passing / 0 failing** across 10 files (+59: all in test-tcp-tools.mjs growing 262 → 321 for PARTIAL-RC + regenerate_sidecar coverage). **Deferred to Session 4**: (1) 3F-4 production commandlet (batch sidecar generator — promotes Oracle-A to general-purpose sidecar emitter); (2) Editor-menu "Regenerate UEMCP sidecar" (Slate FUICommandList hook); (3) Visual capture plugin handler (`get_asset_preview_render` — FPreviewScene + FWidgetRenderer, heavier infra); (4) Additional TOOLSET_TIPS for animation / data-assets / input-and-pie / editor-utility / asset-registry toolsets; (5) Integration smoke test (user action). **User-action prereq for Session 4 dispatch**: close editor → `sync-plugin.bat "path\to\YourProject.uproject"` → `Build.bat` clean build → open editor → smoke-test the 5 NEW plugin command handlers shipped Sessions 2-3 (get_struct_reflection, get_datatable_contents, get_string_table_contents, list_data_asset_types, regenerate_sidecar) plus verify save-hook auto-writes sidecar on next BP save. CP3's 10 original handlers should already be integration-tested from yesterday's editor smoke. **Open items remaining at Session 3 close** (all require live editor to verify): cross-transport transaction semantics untested (FA-ε §Open 3); PIE teardown race with TCP untested (M-enhance handoff §Biggest-unknowns 4). Both wait on integration smoke. **Session 4 scope is tight and well-bounded** — cleanup + integration + editor-menu. Expected 0.5-1 session. **No blockers**. |
| D77 | **M-enhance ship-complete** 2026-04-23 after 4 sessions (9 commits: `12b1a13` → `7e91e1d` → `ca479f7` → `1e811b7` → `0964cd9` → `576d69b` → `dc41193` → orchestrator `a503372` → `69496a9`). Session 4 landed the final bounded scope (commit `69496a9`): TOOLSET_TIPS for 5 remaining toolsets (animation, data-assets, input-and-pie, editor-utility, asset-registry), `DumpBPSidecarCommandlet` (3F-4 production batch sidecar emitter — Oracle-A promotion with `-BPs=` list or `-PathRoot=` asset-registry query), `get_asset_preview_render` via `ThumbnailTools::RenderThumbnail` + `FObjectThumbnail` (pragmatic thumbnail path; full `FPreviewScene` + `FWidgetRenderer` deferred as noted in tool description), Content Browser context menu extender ("Regenerate UEMCP Sidecar" on BP right-click). | Source: M-enhance worker Session 4 final report 2026-04-23. **Cumulative M-enhance totals**: **36 agent-facing MCP tools** (11 FULL-RC + 10 FULL-TCP + 13 PARTIAL-RC + 1 sidecar + 1 visual-capture), **16 plugin-C++ command handlers** on TCP:55558 (M1's ping + 15 new across Sessions 2-4), HTTP:30010 RC transport (Phase 4 fully absorbed per D66), `OnObjectPreSave` save-hook, Content Browser context menu extender, 8 TOOLSET_TIPS entries covering every M-enhance toolset, `narrow-sidecar-v1` schema, `DumpBPSidecarCommandlet` for batch + CI scenarios. **Test baseline**: 1037 (pre-M-enhance) → **1203 passing / 0 failing** across 10 files (+166 across the 4 sessions). **Pragmatic visual-capture shortcut**: worker shipped `ThumbnailTools::RenderThumbnail` path instead of full `FPreviewScene` + `FWidgetRenderer`. Thumbnails cover the "agent wants to see what asset looks like" use case that's 80% of workflow need; full preview-scene render is a future enhancement if rich capture (custom camera, lighting variants) becomes needed. Tool description documents the shortcut. **UE 5.6 API finding for future Slate/Content-Browser workers**: `FContentBrowserMenuExtender_SelectedAssets` handle retrieval requires `Extenders.Last().GetHandle()` (not returned from the `Add` call as one might expect). `ContentBrowser` module dep added to Build.cs. **D66 Phase 4 absorption FULLY realized**: Layer 4 (HTTP:30010 RC) semantic allocation persists in D23; scheduled Phase 4 milestone does not exist; all 8 `rc_*` primitive tools shipped inside M-enhance. **Phase 3 remaining scope** per D58/D66 re-sequence: M3 (oracle retirement, 6-10 sessions, 3 sub-workers, rebuilds 32 transitional tools on 55558), M4 (reduced reads, 3-5 sessions, 12 tools), M5 (remaining toolsets, 6-10 sessions, 3-4 sub-workers — animation + materials + geometry + input-and-pie + editor-utility + visual-capture). **Integration smoke test** (user action, all paths ready): (1) close editor → `sync-plugin.bat "path\to\YourProject.uproject"` → `Build.bat YourProjectEditor Win64 Development -project=...`; (2) open editor → save a BP → verify `<Project>/Saved/UEMCP/<pkg>.sidecar.json` materializes with `narrow-sidecar-v1` schema; (3) right-click BP in Content Browser → "Regenerate UEMCP Sidecar" appears + click shows confirmation dialog; (4) `UnrealEditor-Cmd.exe <uproject> -run=DumpBPSidecar -PathRoot=/Game/Blueprints -Recursive -unattended -nop4` batch-emits sidecars; (5) MCP `get_asset_preview_render` against `/Game/Meshes/SM_Cube` returns inline base64 PNG; (6) FA-ε §Open 3 (cross-transport transaction semantics) + M-enhance handoff §Biggest-unknowns 4 (PIE teardown race) verified naturally via smoke (6) above. **Phase 3 aggregate status**: Wave 1 (M1 + M-spatial + Oracle-A/v2) + Wave 2 (M-new S-B-base + Verb-surface) + Wave 3 (M-enhance) all **shipped**. Remaining: M3, M4, M5. Session-count estimate drop from 28.5-47 original to **~15-25 sessions remaining** (M3 6-10 + M4 3-5 + M5 6-10). **No blockers**. M-enhance complete pending user-driven integration smoke; M3/M4/M5 dispatchable when Noah opens the next wave. |
| D78 | Integration-smoke compile fixes (commit `15b97f9`) — 3 UE 5.6 API drifts in CP3 + S4-3 plugin C++ code that wire-mock tests couldn't catch; surfaced during Noah's first human-integration-smoke rebuild 2026-04-24. Each fix is a one-line include-or-method swap; validates exactly why integration smoke is on the verification-cycle critical path and why the pre-Wave-4 audit worker should look systematically for the same class of drift. | Source: Noah integration smoke Step 1.2 (Build.bat) 2026-04-24. **Three UE 5.6 API drifts captured for institutional memory**: (1) **`UUserDefinedStruct` moved Engine → CoreUObject in 5.6**: the `Engine/UserDefinedStruct.h` path is a DEPRECATION STUB that only forwards when `UE_ENABLE_INCLUDE_ORDER_DEPRECATED_IN_5_5` is defined (OFF by default in 5.6). Canonical include: `#include "StructUtils/UserDefinedStruct.h"`. This is a 5.5 → 5.6 relocation pattern; future C++ workers migrating 5.5-era code must audit for similar moves. (2) **`FObjectThumbnail` lives in Core/Misc/, not UObject/**: Worker wrote `#include "UObject/ObjectThumbnail.h"` which doesn't exist. Correct: `#include "Misc/ObjectThumbnail.h"`. Core module is baseline dep so no Build.cs change needed. (3) **`FObjectThumbnail::GetCompressedImageData()` doesn't exist — use `AccessCompressedImageData()`**: returns non-const `TArray<uint8>&`; binds cleanly to `const TArray<uint8>&` at call site. Lazy-inits PNG from raw BGRA on first access. **Validation cycle value empirically proven**: three real compile bugs found by human-integration-smoke in <15 min; wire-mock test suite (1203 green) covers JS transport + API shape but cannot validate plugin-C++ API usage against real UE headers. This is the load-bearing gap integration smoke closes. **Action item for Phase 3 post-M-enhance audit worker (handoff `phase3-post-m-enhance-audit.md` queued)**: §1 Tier A CP3 C++ code review should explicitly check for similar UE 5.5 → 5.6 API drift patterns in other CP3 / S4-3 plugin handlers (17 plugin C++ files total shipped; only VisualCaptureHandler and ReflectionWalker triggered compile errors this pass, but other files may hold silent runtime drift). **Rule for future C++ workers on UE 5.6**: `UE_ENABLE_INCLUDE_ORDER_DEPRECATED_IN_5_5` is OFF by default — don't rely on 5.5 include paths; check engine source for canonical 5.6 paths when adding includes. **Rebuild state**: UnrealEditor-UEMCP.dll successfully linked post-fix. Noah can resume integration smoke at Step 2 (editor launch). Other user-action items (save-hook, Content Browser menu, batch commandlet, visual capture, negative paths) remain untested pending editor smoke. **Test baseline unchanged**: 1203 passing / 0 failing (plugin-C++ fixes don't affect JS rotation). |
| D79 | Phase 3 post-M-enhance audit landed 2026-04-24 (commit `a9f5f0d`, docs-only at `docs/audits/phase3-post-m-enhance-audit-2026-04-24.md`). **29 findings classified 7 high / 11 medium / 11 low.** Top 3 high findings: **F-1 plugin C++ handlers run on background FRunnable thread with NO `ENamedThreads::GameThread` marshaling** — affects 12+ handlers (compile/PIE/viewport/thumbnail/GEditor access); threading model inherited from UnrealMCP oracle without game-thread dispatch hardening. **F-2 NodeGuid format mismatch at Verb-surface INPUT**: D72's `toOracleHexGuid` bridged OUTPUT emission but not INPUT acceptance — agent piping `bp_list_entry_points → bp_trace_exec` silently returns `node_not_found` because M-spatial LE-lowercase GUID doesn't match S-B-base BE-uppercase-per-uint32 lookup. Test harness seeds node_ids from Oracle directly, so the gap is invisible to automation. **F-4 + F-5 + F-6 + F-7 RC semantic delegates shipped with placeholder single-function bodies**: `get_curve_asset` covers FloatCurves only (not Vector/Color); `get_mesh_info` covers `GetNumVertices` only; `list_material_parameters` covers `GetAllScalarParameterInfo` only. Yaml descriptions promise broader coverage — agent-visible correctness gap. F-7: `toCdoPath` wrongly scopes non-class paths. | Source: audit worker final report 2026-04-24. **Systemic patterns identified**: (a) game-thread safety inherited from UnrealMCP precedent — UEMCP plugin handlers copied the pattern without the editor-thread marshaling most of them need; (b) description-vs-implementation drift concentrated in 3 RC delegates (worker shipped minimum-viable stubs that pass wire-mock tests but under-deliver vs yaml promise); (c) D44 yaml↔code drift on optional params across multiple tools; (d) NodeGuid format bridging incomplete at Verb input despite D72 bridging output; (e) heuristic filters in plugin C++ over/under-match (e.g., class-name substring matches picking up wrong types); (f) fallback paths in some handlers violate D70 (graph_name, node_guid, pin_id) triple-keying invariant. **Recommended follow-on worker priority (per audit §5)**: (1) thread-safety marshaling (F-1) — highest risk; writes in M3 will compound crashes; (2) RC semantic-delegate batch fix (F-4+F-5+F-6+F-7) — single worker covering all 3 delegates + toCdoPath; (3) NodeGuid format bridge at Verb input (F-2+F-21); (4) D44 yaml-RC drift (F-9); (5) SidecarWriter atomic-write (F-3); (6) heuristic-filter refinement; (7) response-size cap on reflection_walk. **Open items for future audit cycles**: PARTIAL-RC group half-built (D76 pragmatic simplification worked but audit flags the "commit to RC augmentation OR collapse cleanly" question); cross-transport transaction semantics still unverified (FA-ε §Open 3); PIE teardown race root-cause untested; UE 5.7 format-drift candidates (skipFText + FEdGraphPinType + HistoryType additions — relevant when S-B-overrides dispatches); reflection_walk response-size empirical profiling. **Validation-cycle design validated empirically**: 29 findings from 2-3 sessions of read-only review surfaces correctness bugs the wire-mock test rotation of 1203 assertions couldn't catch — integration smoke found 3 compile bugs (D78), audit found 29 correctness/ergonomic bugs. Together with human smoke, these three verification layers close the "green tests but broken agent UX" gap. **Orchestrator triage**: dispatching AUDIT-FIX-1 (F-1 thread safety), AUDIT-FIX-2 (F-4+F-5+F-6+F-7 RC delegates), AUDIT-FIX-3 (F-2+F-21 NodeGuid input bridge) as 3 parallel workers with zero file-level collision. Remaining 22 findings queued as a second wave after high-severity batch lands. |
| D81 | Public-repo sanitization landed 2026-04-24 (§1+§2+§3; history rewrite §4 pending). Audit found target-project names (plus an ancillary project codename + Perforce depot identifier + absolute Windows paths) leaked across ~105 tracked files / 812+ instances on the public `noahbutcher97/UEMCP` GitHub remote. Three-phase scrub: (§1) `.gitignore` fixed — `plugin/Binaries/` pattern never matched `plugin/UEMCP/Binaries/`; updated to `plugin/**/Binaries/` + added `docs/{handoffs,audits,testing,research}/` (session-local artifacts going forward). (§2) `git rm --cached -r` on those 4 doc trees — 95 files deleted from index, preserved on disk for local reference. (§3) Remaining 30 tracked files sanitized: narrative → "Project A"/"Project B"/"primary target"/"secondary target"; absolute paths → `path/to/YourProject` placeholder (templates) or `${UNREAL_PROJECT_ROOT}` (runnable); code comments de-specified; fixture paths made env-var-overridable (`UEMCP_VFX_FIXTURE_RELPATH`). Zero matches remain in `git ls-files` output. Full test rotation stays green (11 files, >1000 assertions). | **Policy shift**: handoffs / audits / testing / research docs are now git-ignored — no sanitization burden on future agent dispatches. **Sibling MCP server references** (`jira-<slug>` etc.) genericized to `jira-<project>` pattern example. **What wasn't scrubbed**: GitHub commit history (212 commits). Every prior commit still contains the leaks — reachable via GitHub search, blame, and raw-commit URLs. §4 follow-on: `git filter-repo --replace-text` + force-push to rewrite history; destructive, breaks any clones/forks, feasible since repo has no visible collaborators. Separate handoff to own that destructive pass. **Handoff artifact**: `docs/handoffs/project-name-sanitization-2026-04-24.md` (local-only post-D81 §1). |
| D80 | T-1b fixture-philosophy migration landed 2026-04-24 (commit `3c7d4a9`, single checkpoint since shared `test-fixtures.mjs` module tight-couples the 4 target files). **~80% drift-surface reduction, exceeding the 70% target**: ~10 high-drift hardcoded expected values → 2 centralised named constants + path-derived names. Migration pattern = **"shared module for path + derived-name constants"** rather than full-synthetic (T-1a's pattern). Per-file migration: `test-inspect-and-level-actors.mjs` 6 → 1 specific (named `GAS_ABILITY_BP_PARENT`); `test-query-asset-registry.mjs` 4 → 0 (path prefixes via shared module; structural asserts already); `test-phase1.mjs` ~47 → 1 (named `GAS_ABILITY_BP_ISBLOCKING_TAG`) via shared module + bootstrap-probe refactor for member_name/target_class filters; `test-offline-asset-info.mjs` unchanged (header-only — byte-accurate integer asserts ARE the assertion; synthetic doesn't make sense there). | Source: T-1b worker final report 2026-04-24. **New shared helper**: `server/test-fixtures.mjs` exporting path constants (`GAS_ABILITY_BP`, `PLAYER_BP`, etc.) + scan prefixes + drift-policy doc. No synthetic byte helpers — not needed since full-package synthetic stayed out of scope (D73 feasibility estimate held up; 3-5× T-1a effort was real). **Empirical surprise**: `offline-tools` `resolveAssetDiskPath` only handles `/Game/` → `<projectRoot>/Content/` — engine-stable `/Engine/` fixtures are NOT supported without production-module changes (out of D49 scope for T-1b). This blocks T-1c's engine-BP Oracle-A vision without a preparatory `/Engine/` path-support worker first. **Filter-test refactor**: `CreateDynamicMaterialInstance` / `MaterialInstanceDynamic` filter tests refactored to bootstrap-from-unfiltered probes → drift-proof by construction (assertions become relational: "at least N matches", "filtered count < total count" — not "exactly X matches"). Worth propagating as a pattern for future filter-test writers. **T-1c recommendation**: still valuable but LOWER priority. T-1b converts tier-3 drift-risk into labelled single-drift-point constants — future BP refactors flag clearly at one line with a named constant, not mystery cascades. Engine-BP Oracle-A (T-1c) would further eliminate drift but requires production-tool `/Engine/` path support first (prereq worker). Reopen trigger: Project B onboarding materializes OR fixture drift recurs post-T-1b. **Test count**: `test-phase1.mjs` 316 → 318 (+2 from bootstrap-probe-refactor assertions); rest unchanged. Full rotation stays green. **Authoritative baseline reaffirmed**: 1203 passing / 0 failing across 10 test files (1205 post-T-1b is new figure; orchestrator will reconcile with next CLAUDE.md pass). **No CL-1-style follow-ons needed**. |
| D83 | AUDIT-FIX-1 F-1 plugin thread-safety marshaling landed 2026-04-24 (commit `4cc6275`; orchestrator wrap-up commit `67f8efb`). Originally numbered D81 in orchestrator session; renumbered to D83 to deconflict with Noah's parallel D81 sanitization work that landed first per git log. **All 18 plugin C++ command handlers** now game-thread-marshaled via single-point `Dispatch` wrap through new `MCPThreadMarshal.h/.cpp` (exports `RunOnGameThread` helper with 30s timeout + `GT_TIMEOUT` envelope + per-call wall-clock instrumentation). Auto-covered: ping, bp_compile_and_report, reflection_walk, get_struct_reflection, get_material_graph, get_event_dispatchers, get_editor_state, start_pie, stop_pie, is_pie_running, execute_console_command, get_widget_blueprint, get_asset_references, get_datatable_contents, get_string_table_contents, list_data_asset_types, regenerate_sidecar, get_asset_preview_render. D49 path-limited to plugin/UEMCP/Source/UEMCP/{Public,Private}/. | Source: AUDIT-FIX-1 worker final report 2026-04-24. **Three load-bearing worker hints worth D-log capture**: (1) **F-14 PIE teardown race UNRESOLVED** — marshaling fixes the REQUEST-side race only (ensuring start_pie / stop_pie fire on game thread). `UEditorEngine::RequestEndPlayMap` is itself asynchronous INSIDE the engine — the teardown completion lag is engine-internal and not solved by game-thread dispatch alone. Separate worker / finding needed to reason about post-RequestEndPlayMap state. Flag for future PIE-adjacent tool work. (2) **Edit tool silently drops multi-line edits on CRLF files in plugin/UEMCP/ tree** — worker observed that multi-line Edit operations against CRLF-encoded C++ files can land incompletely without surfacing a diff error. Mitigation: always `git diff` after each Edit on plugin/*.cpp; use Write tool for full-rewrite fallback when multi-line Edit fails. This is a TOOLING-LAYER hazard affecting every future C++ plugin worker on this codebase — capture as institutional memory. Root cause likely a line-ending mismatch between tool input and file state; may or may not affect non-plugin files depending on Git `autocrlf` behavior. (3) **`Async/Future.h` is canonical UE 5.6 path, NOT `Templates/Future.h`** — D78 5.5→5.6 include-drift pattern extends to yet another header location. Worker discovered this during `TPromise`/`TFuture` integration in MCPThreadMarshal. Fold into future-C++-worker checklist: when UE class/template isn't resolving, grep canonical UE 5.6 path before assuming the pre-5.6 location. **Pure-data handler reasoning**: only `ping` qualifies as pure-data (no GEditor/UObject touch); worker marshaled it anyway for uniformity. Microsecond cost vs structural consistency win — correct engineering trade. **Hitch candidates pending live-editor smoke**: per-call wall-clock logged to `UnrealEditor.log` with "hitch:" prefix. Noah (or next human smoke pass) runs `findstr "hitch:" UnrealEditor.log` post-rebuild + representative tool exercise. Not blocking Wave 4 dispatch. **Regression state**: test-uemcp-gate.bat not re-run (commandlet path unchanged per D57 gate). Live-editor smoke pending — recommend `is_pie_running` + `bp_compile_and_report` as fastest round-trip validators post-sync-plugin.bat + Build.bat. **Test baseline unchanged** at 1205 passing / 0 failing (plugin C++ change; no JS assertion delta). **AUDIT-FIX-2 + AUDIT-FIX-3 remain in-flight or dispatchable** — zero file collision with AUDIT-FIX-1's plugin-only scope; safe to continue parallel. **F-14 unresolved status**: moves from audit "open item" to first-class follow-on finding; may need its own handoff if PIE workflows become important. |

| D82 | Public-repo NDA-codename push gate (2026-04-24). **First pass shipped a defense-in-depth setup** — pre-commit + commit-msg + pre-push hooks, generic credential-shape patterns, install scripts, security policy doc, integrated unit test (~600 lines, 12 files). **Pulled back to a minimal pre-push gate** after operator pushback that the scope was overengineered for the actual threat model: a single-developer repo where (a) D81 history is already clean, (b) high-volume project-specific content (handoffs/audits/testing/research) is gitignored, (c) the only concern is breaking NDA on the target-project codenames — not API keys, personal info, or paths. Final scope: `.githooks/pre-push` (~60 lines, single bash script) reads `.git/info/forbidden-tokens` (per-checkout, untracked, populated once with codenames), scans outgoing diff + commit messages, blocks push if any match. One-time setup: `git config core.hooksPath .githooks`. Bypass: `git push --no-verify`. **No pre-commit hook** (free local commits), **no commit-msg hook** (folded into pre-push diff scan), **no install scripts** (one git config command), **no test-rotation integration**, **no security-policy.md doc** (CLAUDE.md paragraph instead), **no API-key shape patterns** (out of scope per operator). | Source: 2026-04-24 D81 follow-on session + pullback. **Lesson worth capturing**: when the threat model is narrow (one codename category) and the operator pool is small (single developer), pre-push is a strictly better choice than pre-commit — no friction on local commits, fires only at the moment that matters (when content goes public). The first-pass over-spec was a misread of "production-ready and doesn't disrupt our work" as a cue to build full enterprise infrastructure; correct read is to fit the smallest enforcement primitive to the actual threat. **`.gitignore` retains** `.filter-repo-replacements.txt` + `.filter-repo-mailmap.txt` + `*.bundle` + `.git-rewrite/` entries so a future scrub recipe stays available. **No regression**: full rotation green; sanitization scrub from D81 holds (zero tracked-file matches against codename list). |
| D84 | AUDIT-FIX-2 F-4+F-5+F-6+F-7 RC semantic delegate expansion landed 2026-04-24 (commit `c512df2`, predates D81 sanitization wave per git log). Verification confirmed by AUDIT-FIX-2 rerun-as-noop session 2026-04-24 — handoff was stale (referenced HEAD `232832f` but actual HEAD had moved past the ship). | Source: AUDIT-FIX-2 worker verification report 2026-04-24. **Per-finding deltas as shipped**: (a) **F-4 `get_curve_asset`**: probes class via `/remote/object/describe`, dispatches `FloatCurve` (UCurveFloat single-curve) vs `FloatCurves` array (UCurveVector / UCurveLinearColor). Optional `curve_class` hint short-circuits the probe for callers who know in advance. (b) **F-5 `get_mesh_info`**: batches 5 UFUNCTIONs (`GetNumVertices` + `GetNumTriangles` + `GetNumLODs` + `GetBounds` + `GetStaticMaterials`) into a single round-trip; returns flattened `{vertices, triangles, lods, bounds, material_slots}`. (c) **F-6 `list_material_parameters`**: batches `GetAllScalarParameterInfo` + `GetAllVectorParameterInfo` + `GetAllTextureParameterInfo`; returns `{scalar, vector, texture}` arrays. Yaml description narrowed: "**info only**, not values" — value retrieval requires per-param `rc_call_function`. (d) **F-7 `toCdoPath` heuristic fix**: only append `:Default__<ClassName>` when path ends in `_C` (CDO suffix). Raw UObject paths (UMaterial / UCurveFloat / UStaticMesh) pass through unchanged. **toCdoPath approach taken**: heuristic path-suffix check, NOT explicit-hint param. Lower API friction; covers all current callers. If future tools need explicit-CDO-vs-object-path control, can layer on. **Yaml trims** preserve D44 single-source-of-truth: list_material_parameters narrowed to "info only"; get_curve_asset documents describe-probe strategy + curve_class hint; get_mesh_info enumerates all 5 outputs + asset_path/target alternatives. **Test rotation**: `test-rc-wire.mjs` 72 → 110 passing (+38, more than handoff's projected +10-20). Other suites unchanged. **Caveat preserved in commit message**: curve property names (`FloatCurve` vs `FloatCurves`) inferred from FA-ε research, NOT verified against live UE 5.6 WebRemoteControl endpoint. Mock tests pass because both contract sides are author-controlled. Runtime verification belongs to manual integration smoke (live-editor RC round-trip against UCurveBase subclasses). **Open question PARTIAL-RC commit-or-collapse** (audit §F-18 + open items §3) untouched by this fix scope — `list_data_asset_types` is the canonical PARTIAL-RC code-smell instance to evaluate when that decision is dispatched. **Worker discipline noteworthy**: AUDIT-FIX-2 verification session correctly identified the work was already shipped (handoff context stale), did NOT redo work, did NOT commit new changes, surfaced state to orchestrator + raised an out-of-scope observation about D82 staged-deletions that turned out to be stale local-tree state (not a real anomaly — D82 had a slim-down commit `2367285` deliberately removing the heavy infrastructure from initial `81084f7`). Conservative-stay-in-scope behavior is the correct pattern for "find the work is already done." |
| D85 | AUDIT-FIX-3 F-2 + F-21 NodeGuid input bridge landed 2026-04-24 (commit `7edd55d`). 3 Verb-surface handlers normalize NodeGuid input at handler edge: `bp_trace_exec`, `bp_trace_data`, `bp_neighbors`. Verb-surface composition `bp_list_entry_points → bp_trace_exec` no longer silently returns `node_not_found` due to format mismatch. **Test baseline 1205 → 1338 passing / 0 failing across 10 files** (worker reports +17 in test-verb-surface 83→100; other accounting-drift +95 from concurrent commits not enumerated by orchestrator). | Source: AUDIT-FIX-3 worker final report 2026-04-24. **Robust normalize-by-lookup-success pattern**: `normalizeNodeGuidInput(rawInput, nodes)` tries lookups in priority order — (1) raw as-is (caller passed canonical), (2) uppercase-only (mixed-case canonical), (3) `toOracleHexGuid(lowercase)` for M-spatial passthrough. Helper REQUIRES `nodes[candidate]` to actually match the topology before returning the candidate, so bogus 32-hex inputs that don't match any node fall through cleanly to `node_not_found` (existing contract). **More robust than format-detection-by-case** which would mis-route digit-only GUIDs (no letters to disambiguate case). **Worker correctly disambiguated handoff scope vs reality**: 2 of 5 handlers in handoff list (bp_show_node, bp_subgraph_in_comment) verified against yaml as NOT taking NodeGuid input — they accept export_index / objectName instead. F-2 doesn't apply to those; only 3 handlers needed the fix. **F-21 / audit F-10 fallback drop (deliberate trade)**: removed cross-graph NodeGuid scan in `bpShowNode` (lines 2415-2420). Previously when primary `(graph_name, oracleGuid)` lookup returned null, code scanned every graph for coincident-GUID match. **D70 invariant says NodeGuids are NOT unique across sibling graphs** — the fallback scan could return pins from a wrong-graph node, silently masking the disambiguation problem. Removed; pin_block now degrades to `not_available` per FA-β contract. **Trade**: orphan-graph entry-points that previously resolved via the fallback now degrade. Audit prioritized correctness over availability; FA-β not_available envelope is the documented degradation signal. **Observable behavior change locked in by tests**: handler responses now echo CANONICAL form (topology-key uppercase BE-per-uint32) rather than raw input on success. Agents chaining trace calls don't need to renormalize between calls. Canonical-input tests still pass (canonical → canonical is identity). **Composition test seed** (test-verb-surface.mjs:514-526): probe confirmed seed `c18d866a99677344a6e3f56a4e081fa3` (M-spatial lowercase byte-LE) maps to canonical `6A868DC1447367996AF5E3A6A31F084E` (Oracle-A uppercase BE-per-uint32) — distinctly different hex forms across the two emission conventions. Pre-fix: `node_not_found`. Post-fix: `chain.length: 2` proper trace return. **Yaml updates**: 3 entries (`bp_trace_exec.start_node_id`, `bp_trace_data.start_node_id`, `bp_neighbors.node_id`) each documents "Accepts Oracle-A canonical (uppercase BE-per-uint32) OR M-spatial raw (lowercase byte-LE)" with explicit pointer to upstream M-spatial verbs. D44 single-source-of-truth preserved. **Audit-triage top-3 batch COMPLETE**: D81 sanitization + D82 NDA-gate + D83 AUDIT-FIX-1 + D84 AUDIT-FIX-2 + D85 AUDIT-FIX-3 all shipped. Remaining 22 audit findings (medium/low) queued as second-wave fix candidates; none blocking Wave 4 (M3/M4/M5). |
| D86 | Human integration smoke completed 2026-04-24/25 (results doc `docs/testing/human-integration-smoke-results-2026-04-24.md`; bug doc `<target-project>/Local/Audits/2026-04-24/uemcp-plugin-bugs.md`). **Coverage**: 24 steps total — 10 pass / 2 partial / 4 fail / 2 skip-crash / 5 skip-GUI-pending-Noah / 1 prereq-met. **5 plugin bugs surfaced, all UEMCP plugin (not target-project game code)**. **Critical orchestration finding**: 3 of 5 bugs are RECURRENCES of audit findings we believed shipped — Bug 1 (bp_compile_and_report editor crash, IsInAsyncLoadingThread assert), Bug 3 (get_blueprint_* log spam + partial results on cold BPs, same thread class as Bug 1), Bug 4 (get_asset_preview_render editor crash, IsInGameThread assert) all stem from AUDIT-FIX-1's RunOnGameThread marshaling NOT covering these specific handler code paths despite worker reporting "all 18 handlers" coverage. Bug 5 (bp_trace_exec node_not_found from bp_list_entry_points output) is the same identifier-scheme mismatch AUDIT-FIX-3 was supposed to fix — implies that fix didn't land at all the right call sites OR there's a different identifier scheme entirely. | Source: human integration smoke worker final report 2026-04-25. **5 bugs full table**: (1) **Bug 1 BLOCKER** `bp_compile_and_report` editor crash — Kismet compile fires on MCP worker thread; AUDIT-FIX-1 marshaling either bypassed for this handler or doesn't actually marshal compile work; (2) **Bug 2 MEDIUM** `rc_list_objects` HTTP 404 — UE 5.6 removed `/remote/object/list` endpoint; AUDIT-FIX-2 didn't catch this because mock-test contract was author-controlled. Workaround: `rc_passthrough PUT /remote/search/assets`; (3) **Bug 3 LOW-MEDIUM** `get_blueprint_*` log spam + silent partial results on cold BPs — same thread class as Bug 1 (ZenLoader soft-refuses flush) plus path missing `_C` suffix → "Class None." warning; (4) **Bug 4 BLOCKER** `get_asset_preview_render` editor crash (masked by 5s wrapper timeout) — UThumbnailManager IsInGameThread assert; same thread-class as Bug 1; (5) **Bug 5 MEDIUM** `bp_trace_exec` node_not_found for GUIDs returned by `bp_list_entry_points` (both case forms) — identifier-scheme mismatch between list and trace endpoints. **Why automated tests missed all 5**: wire-mock tests don't exercise real UE editor APIs (Bugs 1, 3, 4 — game-thread assertions never fire in mocks); RC wire-mock has author-controlled both sides so endpoint-existence verification didn't apply (Bug 2); test harness seeds node_ids from Oracle directly, never tests cross-endpoint composition with M-spatial → Verb-surface (Bug 5). **AUDIT-FIX-1 incomplete-coverage finding requires investigation**: orchestrator believed all 18 handlers shipped game-thread-marshaled per D83 worker report. Smoke proves at least 3 handlers (compile, blueprint-reads, thumbnail) STILL crash on game-thread asserts. Possibilities: (a) RunOnGameThread wrap got applied but specific handler code paths bypass it (e.g., parsing-on-worker before marshaling), (b) wrap has defect (TPromise.Wait blocking vs async-fire), (c) some handler entries register via different path that doesn't route through MCPThreadMarshal. SMOKE-FIX worker must audit actual plugin source to identify root cause + repair. **AUDIT-FIX-3 incomplete-coverage finding for Bug 5**: D85 worker reported only 3 of 5 originally-listed handlers needed normalization (bp_show_node + bp_subgraph_in_comment correctly disambiguated as not-NodeGuid-input). Bug 5 is bp_trace_exec specifically — which WAS in the AUDIT-FIX-3 fix scope. Smoke shows it still fails. Implies either D85's normalize-by-lookup pattern has a defect for the specific GUID format `bp_list_entry_points` emits, OR there's a third format we haven't catalogued. **Two §Reporting-back items deferred** (FA-ε §Open 3 cross-transport transaction + M-enhance §Biggest-unknowns 4 PIE teardown race) — not blocked by bugs, but blocked by smoke crash-budget consumption. Requires follow-up smoke after Bug 1+4 fix lands. **Smoke surface area validated as verification layer**: wire-mock tests + audit + smoke each caught a different bug class. Smoke's unique value: live-editor game-thread assertions, real RC endpoint validation, real composition between subsystems. Together they close the "automated-tests green but agent-UX broken" gap that motivated the validation cycle (D77 retrospective). **What worked notably well per smoke worker**: save-hook + batch commandlet (9 BPs / 9 success); D57 commandlet gate fires every run; reflection-walk delivers full flag surface RC's allowlist hides; offline-tool negative-path graceful envelopes; AR pagination + heavy-tag stripping (auto-omits FiBData at verbose:false); RC route table introspection survives buggy individual RC wrappers (`rc_passthrough` kept Bug 2 diagnosable). **Methodology note for future smokes**: MSYS/Git-Bash invocations of `UnrealEditor-Cmd.exe` for `/Game/...` paths require `MSYS_NO_PATHCONV=1` prefix or paths get translated to `C:/Program Files/Git/Game/...`; smoke plan command targeted `cmd.exe` and Bash translation produced silent zero-asset-resolved + exit 0 (mistakable for real bug). Annotated in plan doc + propagated to T-1c if engine-BP corpus retargets. **Wave 4 dispatch verdict**: HOLD M3 + M4 + M5 until SMOKE-FIX worker lands Bug 1+3+4 fix (shared `RunOnGameThread<T>` audit) + Bug 5 fix (identifier-scheme audit) + Bug 2 fix (RC endpoint replacement). M3 specifically depends on plugin TCP + thread safety being correct; dispatching pre-fix risks compounding crashes during writes-rebuild work. **Test baseline 1338 unchanged** at JS-rotation level (smoke is live-editor, not JS-test). |
| D87 | SMOKE-FIX shipped 2026-04-25 (commit `151ae4d`) — 4 of 5 smoke-surfaced bugs CLOSED end-to-end; 1 new finding (Bug 4 thumbnail-empty post-marshal) surfaced. **CRITICAL ORCHESTRATION FINDING that supersedes D86's "recurrence" framing**: 3 of 5 bugs were NOT recurrences of audit-fix defects — they were **DEPLOYMENT GAPS**. Worker investigation found the project-local plugin tree at `<target-project>/Plugins/UEMCP/` was never `sync-plugin.bat`'d from upstream after D83 AUDIT-FIX-1 landed; deployed `MCPCommandRegistry.cpp:52` still had unmarshaled `(*Handler)(Params, OutResponse)` and `MCPThreadMarshal.{h,cpp}` files were absent from the deployed copy. AUDIT-FIX-1's single-point Dispatch wrap correctly covers all 18 handlers when built fresh — smoke just ran against a pre-D83 binary. Bug 5 had the equivalent JS deployment gap: MCP server process loaded `offline-tools.mjs` BEFORE D85 landed; ESM modules don't hot-reload so `normalizeNodeGuidInput` was missing in-memory regardless of disk state. **D86 "incomplete coverage" framing officially superseded by D87** — neither AUDIT-FIX-1 nor AUDIT-FIX-3 had defects; both shipped correctly. | Source: SMOKE-FIX worker final report 2026-04-25, with empirical verification (read deployed plugin tree directly, confirmed missing files; piped smoke's exact ReceiveBeginPlay guid through bp_list_entry_points → bp_trace_exec post-fix returns chain.length=2 + canonical echo). **Real code fixes shipped in this CL** (3 distinct items, separate from the deployment-gap closures): (1) `ReflectionWalker.cpp` ResolveClass — pre-normalize bare package paths by appending `.<AssetName>_C` before LoadClass; eliminates `Class None.<package>` warning + cold-BP partial-result risk Bug 3's PATH-half (thread-half is fixed by D83 marshaling once deployed); (2) `rc-url-translator.mjs` + `rc-tools.mjs` + `tools.yaml` Bug 2 — UE 5.6 RemoteControl removed `/remote/object/list`; reimplement against `PUT /remote/search/assets`. Strip U/A prefix on ClassNames per AssetRegistry filter convention (UWorld → World); map outer → Filter.PackagePaths with RecursivePaths flag; (3) test-verb-surface.mjs Bug 5 regression — existing F-2 first-match test picks `bp_list_entry_points[0]` which is DevAttack on the test BP; added deterministic exhaustive block iterating ALL 8 EventGraph entries asserting bp_trace_exec accepts each raw guid + echoes a 32-char canonical form distinct from input. Reproduces the agent-composition gap regardless of find() ordering. **Path-limit deviation per D49** (legitimate per D72 "scope-deviation landed correctly" pattern): `tools.yaml` (D44 single-source-of-truth would rot if left stale) and `rc-url-translator.mjs` (canonical edit site for rcListObjects shape; rc-tools.mjs imports from it). **Test rotation 12 files green: 1338 → 1381 passing / 0 failing** (+43: +25 exhaustive Bug 5 regression in test-verb-surface, +5 test-rc-wire shape assertions for new endpoint, remainder rotation-counter normalization vs D85's 10-file count). **NEW LATENT DEFECT flagged for audit-batch-2** (worth queuing alongside the 22 remaining audit findings): `MCPThreadMarshal::RunOnGameThread` captures `handler/Params/OutResponse` by reference in the outer lambda; on the 30s GT_TIMEOUT path the worker thread unwinds and the queued AsyncTask can still fire afterward, dereferencing freed stack memory. Doesn't trigger in steady-state operation but is a real use-after-free hazard. **NEW FOLLOW-UP FINDING (Bug 4 follow-up)**: `get_asset_preview_render` crash CLOSED via marshaling, but `ThumbnailTools::RenderThumbnail` returns empty bytes for both StaticMesh AND Blueprint post-marshal. `EThumbnailTextureFlushMode::AlwaysFlush` mode in use; theory is UE 5.6 schedules render-thread async work that `AccessCompressedImageData` reads before pixels are written. Recommended fix: `FlushRenderingCommands()` after `RenderThumbnail`, OR test `EThumbnailTextureFlushMode::ConditionallyFlush` mode. **NEW audit-queue item**: `rc_passthrough` body Zod-validation issue — when called via MCP wire, structured-object body param arrives Zod-stringified. Workaround discovered by SMOKE-FIX worker: trust the endpoint hit (returned valid `{Assets: []}` envelope, not 404). Either Zod schema needs `z.union([z.record(z.any()), z.string()])` with parse-on-string, OR rc_passthrough description should warn callers that body must be wired as pre-stringified JSON. **Bugs 1 + 3-thread + 4-crash close on next deployment cycle**: editor close → sync-plugin.bat → Build.bat → relaunch (also restarts MCP server, picking up D85 + this CL's offline-tools / rc-tools changes). No code change in this CL extends C++ marshaling — D83 was always correct. **D61 / D69 / D78 deployment-gap pattern reinforced**: workers ship code commits; deployments don't auto-happen. After an audit-fix commit involving plugin C++ or MCP server JS, someone has to (1) sync-plugin.bat to deploy plugin changes, (2) Build.bat to rebuild DLL, (3) restart editor + MCP server to pick up new code. Without those 3 steps, the fix exists in git but doesn't run. **Wave 4 dispatch verdict**: M3 (writes-rebuild) + M4 (reduced reads) DISPATCHABLE NOW per SMOKE-FIX worker — neither depends on visual-capture. M5 (includes visual-capture toolset) WAIT on the new Bug 4 follow-up landing. **Two §Reporting-back items still deferred** (FA-ε §Open 3 + M-enhance §Biggest-unknowns 4) — the deployment + relaunch cycle would naturally exercise them; recommend bundling with Bug 4 follow-up smoke when Noah next opens editor. **D87 corrects D86's framing**: smoke remains a load-bearing verification layer (caught the deployment gaps that automation misses) but the bug class was misdiagnosed as "fix incomplete" when it was actually "fix not deployed." Future audit-fix dispatches should include explicit deployment-cycle confirmation in success criteria, not just "code committed + tests green." |
| D88 | Deployment cycle complete 2026-04-25 (sync + Build + relaunch + MCP-server restart). **D83 + D85 + D87 fixes verified live end-to-end**: Bug 1 closed (bp_compile_and_report on player BP returns "compiled cleanly — 0 errors, 0 warnings, 0 notes" — no crash); Bug 3 closed (get_blueprint_variables returns 27 vars with full flag surface visible — Replicated/Components/etc. — proves AUDIT-FIX-1 marshaling AND ReflectionWalker `_C` suffix normalization both deployed); Bug 1 thread-safety proven via is_pie_running clean response. D87's "deployment-gap framing supersedes recurrence" finding is empirically validated — once deployed, the audit-fixes work as designed. **Bug 4 follow-up CORRECTED ROOT CAUSE identified during in-session diagnostic**: previous SMOKE-FIX worker theory ("UE 5.6 schedules render-thread async work; AccessCompressedImageData reads before pixels written; fix via FlushRenderingCommands or ConditionallyFlush") was WRONG. Real root cause is structural, not async. | Source: live editor + Claude Code session 2026-04-25 — user-driven investigation against `VisualCaptureHandler.cpp:73`. **The actual pipeline**: (1) `ThumbnailTools::RenderThumbnail()` populates `FObjectThumbnail::ImageData` (raw BGRA) synchronously — no async issue; (2) `RenderThumbnail()` does NOT populate `CompressedImageData` (PNG); (3) PNG compression happens ONLY via explicit `Thumbnail.CompressImageData()` call OR as side effect of `FObjectThumbnail::Serialize()` (i.e., when read from a `.uasset` file); (4) `AccessCompressedImageData()` returns `CompressedImageData` directly with NO fallback to lazy-encode from raw BGRA — so returns empty when no compression has happened; (5) handler at line 73 treats empty as "no renderer registered" via the misleading line-77 error message and fails. **The hitches (284ms first call, 128ms warm) confirm `RenderThumbnail()` ran successfully** — the StaticMesh renderer IS registered and producing pixels. The bytes are just being thrown away because the handler asks for the PNG-compressed buffer that nobody compressed. **Two stale comments / messages to fix alongside**: inline comment at `VisualCaptureHandler.cpp:69-72` claims `AccessCompressedImageData` lazy-inits from raw BGRA — it doesn't; this is a code-comment error that misled the worker writing the code originally. Error message at `VisualCaptureHandler.cpp:77` says "no UThumbnailRenderer may be registered for %s" — but renderer IS registered; the message will misdirect future debuggers. **Real fix is ONE LINE** between current lines 67-73: `Thumbnail.CompressImageData();` after `RenderThumbnail()` succeeds. Tightening the error-message branches into "no registered renderer (lookup failed)" vs "renderer ran but produced 0 bytes" is optional but valuable. **Secondary observation worth carrying forward**: `EThumbnailTextureFlushMode::AlwaysFlush` is what makes the call 100-300ms synchronous on game thread. If thumbnails are ever batched (e.g., M5 visual-capture mass-render workflow), consider `NeverFlush` for warm-cache repeat calls OR move to `FAssetThumbnailPool` which renders async + respects the editor's existing thumbnail cache. **D83 hitch instrumentation proven valuable**: per-call wall-clock log line "hitch: 'get_asset_preview_render' ran 284.0ms on game thread" is exactly what the audit handoff §6 specified. Future C++ workers can `findstr "hitch:" UnrealEditor.log` after representative tool exercise to surface optimization candidates. **UE 5.6 API gotcha to add to institutional memory** (alongside D78 `Misc/UObjectToken.h`, D81 CRLF Edit hazard, D81 `Async/Future.h`, D87 OnObjectPreSave deprecation): `FObjectThumbnail` has TWO independent buffer fields — `ImageData` (raw BGRA, populated by RenderThumbnail) and `CompressedImageData` (PNG, populated only by explicit CompressImageData() OR Serialize from disk). Code that assumes "render → access compressed" without the explicit compression step gets empty bytes silently. **Wave 4 dispatch verdict**: M3 + M4 unblocked + verified (deployment confirmed all three audit-fixes live + working). M5 still gated on the 1-line CompressImageData fix landing. CLEANUP-MICRO worker handoff updated to replace the previous async-fix theory with the corrected 1-line structural fix. **Test baseline 1381 unchanged at JS-rotation level** (this finding is C++-only). **Two §Reporting-back items still deferred** (FA-ε §Open 3 cross-transport transaction + M-enhance §Biggest-unknowns 4 PIE teardown race) — recommend bundling with CLEANUP-MICRO smoke since editor is up. |
| D89 | D81-SANITIZATION-AUDIT landed 2026-04-25 — read-only audit doc at `docs/audits/d81-sanitization-regressions-2026-04-25.md` (~18.7 KB). **No commit SHA — audit deliverable lives in `docs/audits/` which is gitignored per D81 §1; force-adding would re-violate D81 policy.** **10 numbered findings: 1 high · 4 medium · 5 low.** Top 3: **F-1 (HIGH)** `test-uemcp-gate.bat` post-commit `66bf214` still violates the CLAUDE.md §.bat convention (no EXIT_CODE tracking, no AUTO_YES sentinel, no exit-code echo, no `endlocal & exit /b` propagation idiom — Noah's GUI-fallback patch shipped without bringing the script into convention compliance). **F-3 (MED)** CLAUDE.md has 4 dead pointers into the now-gitignored `docs/audits/` tree (lines 92, 93, 368, 369) — fresh-clone agents hit 404s during onboarding. **F-5 (MED)** `server/test-uasset-parser.mjs` int64-salvage test silently skips for everyone by default; new fixture path `Content/VfxCorpus/SM_auraHousya.uasset` doesn't exist; `UEMCP_VFX_FIXTURE_RELPATH` env-var override isn't documented in CLAUDE.md run command. | Source: D81-SANITIZATION-AUDIT worker final report 2026-04-25 (audit doc local-only at `docs/audits/d81-sanitization-regressions-2026-04-25.md`). **Systemic patterns identified**: (a) D81 §1+§2 untracked four whole doc trees but tracked content in §3 wasn't refreshed for the broken pointers (F-2/3/4/7 cluster); (b) §3 sanitized values in scripts/tests but didn't audit whether placeholder substitutions still exercise documented behavior (F-1 + F-5 cluster); (c) placeholder vocabulary diverged across files because §3 sanitized each file independently (F-9). **Recommended fix order from worker**: F-1 → F-5 → batch (F-2/3/4/7) → batch (F-6/8/9) → F-10. Single commit feasible (~9 files); D49-tolerable since co-motivated by D81. **META-FINDING about handoff template**: the worker correctly flagged that the audit handoff's "Single commit / Commit SHA" requirement was templated from earlier worker patterns and breaks when the deliverable lands in a gitignored tree. **Future post-D81 audit handoffs should drop the SHA requirement and replace with "filesystem path for orchestrator pickup."** This is a one-line handoff-template fix worth absorbing across all `docs/handoffs/*-audit-*.md` going forward. **Out-of-scope items the worker surfaced** (queued for future audits, not part of D81-FIXES scope): README.md:124 troubleshooting typo (pre-D81); CLAUDE.md numbering drift (D1-D50 → D88) — D-log header in CLAUDE.md is stale; CLAUDE.md test-assertion count claim 1203 vs current ~1376-1381 (drifted across D85→D87→D90); `testing-strategy.md:28-29` server-path drift (sibling-MCP-server template carryover). **Coordination-gap learning for future multi-commit refactors**: D81's two-commit split (gitignore+rm in §1+§2, content rewrite in §3) created the F-2/3/4/7 dead-pointer cluster because §3 wasn't given the §1+§2 file-removal list. A merged single-commit pass would have caught it. Pattern for future structural refactors: when one commit removes references, the same commit should refresh the things that pointed at them. **D81-SANITIZATION-FIXES dispatchable** — handoff at `docs/handoffs/d81-sanitization-fixes-worker.md` references this audit doc as input + has §4 convention-compliance pass that directly addresses F-1. Test rotation unchanged at this layer (read-only audit). |
| D90 | CLEANUP-MICRO shipped 2026-04-25 (commit `c95c2bb`, single PR-shaped, 5 files +103/-12). **Three file-disjoint fixes bundled per the cleanup-micro handoff scope.** **§1 Bug 4 thumbnail PNG-compression — closed-by-construction**: `Thumbnail.CompressImageData()` inserted between `RenderThumbnail()` and the PNG-byte read in `VisualCaptureHandler.cpp`. Stale "lazy-inits" comment replaced (it didn't lazy-init — D88's corrected root cause). Empty-bytes error message branched via `UThumbnailManager::Get().GetRenderingInfo()` lookup to distinguish "no UThumbnailRenderer registered for class" vs "renderer ran but produced 0 bytes" — same outcome, very different debug story. Verified pending-deployment (live-fire smoke gated on next sync + Build + relaunch cycle). **§2 use-after-free fix landed at the CALL SITE, not the helper**: worker investigation found `MCPThreadMarshal::RunOnGameThread<T>` itself was correctly capturing — the defect was in `MCPCommandRegistry::Dispatch` where callers passed handler/Params/OutResponse references that outlived the dispatch frame on the 30s GT_TIMEOUT path. Fixed via `HandlerPtr` (singleton-lifetime) + `ParamsCopy` (TSharedPtr by value) + `SharedOut: TSharedRef<TSharedPtr<FJsonObject>>` payload owned by the lambda. Added CALLER RESPONSIBILITY note to `MCPThreadMarshal.h` so future helper users can't repeat the same pattern. Verified pending-deployment. **§3 rc_passthrough body normalization — chose variant beyond (a)/(b)**: `z.preprocess(JSON.parse-or-passthrough, z.record(z.any()))` mirroring the existing F-1.5 pattern in `zod-builder.mjs` rather than a fresh `z.union`. Caller-friendly, zero handler changes, project-idiomatic. test-rc-wire 115→119 (+4 regression assertions). | Source: CLEANUP-MICRO worker final report 2026-04-25, commit `c95c2bb`. **Path-limit deviation per D49** (legitimate D72-style — defect lives in the call site, not the helper named in the handoff): `MCPCommandRegistry.cpp` added to scope. Worker correctly surfaced rather than rejected. **D72 scope-deviation pattern reinforced**: workers should land deviations alongside primary scope when the actual defect location differs from the handoff's named target file, document the deviation, and flag for D-log. Rejecting forces a follow-on dispatch for the same logical fix. **No UE 5.6 API surprises**: `FThumbnailRenderingInfo` and `UThumbnailManager::Get().GetRenderingInfo()` exposed by the existing `ThumbnailRendering/ThumbnailManager.h` include; no new Build.cs deps. **NEW audit-batch-2 hint surfaced by worker**: `MCPThreadMarshal::RunOnGameThread` timeout path leaves orphan AsyncTasks running indefinitely. Wallclock instrumentation only logs successful completions — orphan tasks that complete after `WaitFor` returned false are invisible. Worth a metric / counter to surface them post-timeout. Add to F-14 PIE teardown queue or its own audit-batch-2 line. **Test baseline reconciliation**: worker observed ~1376 (1372 baseline + 4 new) vs prior CLAUDE.md / D-log claims of 1203 (CLAUDE.md, stale per D89 §out-of-scope) and 1381 (D87, includes test-m1-ping skip-clean which is live-editor gated and skips on rotation runs). **The 1203 number in CLAUDE.md needs refresh; D87's 1381 is correct including the live-editor-gated skip; CLEANUP-MICRO's 1376 reflects unit-runnable-only count + 4 new.** This drift is one of the D89 out-of-scope items; absorb during D81-SANITIZATION-FIXES if scope permits, otherwise standalone CLAUDE.md grooming pass. **M5 visual-capture readiness**: Bug 4 closed-by-construction in code; live-fire confirmation = next deployment cycle (sync-plugin.bat + Build.bat + editor relaunch). Per D87 framing: "code committed + tests green ≠ fix is live." M5 gate clears empirically once Noah deploys + smokes `get_asset_preview_render` against StaticMesh + BP. **Two §Reporting-back items still deferred** (FA-ε §Open 3 + M-enhance §Biggest-unknowns 4) — would naturally exercise during the next deployment-cycle smoke pass. **Wave 4 verdict update**: M3 + M4 dispatchable as before (D87 verdict); M5 dispatchable post-deployment + Bug 4 live-fire confirmation (logically unblocked, deployment-gated). |
| D91 | Project B identified as on **UE 5.7.4** — NOT 5.6 as implied by its parent directory hierarchy. Discovered 2026-04-25 during a sidecar CDO arrayed-value spot-check. UEMCP plugin is deployed in Project B and 24+ sidecar files have been generated under `<project-B-root>/Saved/UEMCP/**/*.sidecar.json` dated 2026-04-25 — Project B is empirically materialized AND on 5.7. **Sidecar question definitively answered (NO, by design)**: `narrow-sidecar-v1` schema does not capture CDO scalar OR arrayed values; only CDO *shape* (per-UPROPERTY name/cpp_type/class/flags/metadata + function signatures + compile_status envelope). Verified across all Project B sidecars — zero `default*` / `value*` / `cdo*` payloads. CDO instance values live in the package binary and have two existing offline retrieval paths: `read_asset_properties` (D46/D47/D50 L2.5 parser, full TArray/TSet/TMap support) and `inspect_blueprint` with `include_defaults: true` (D38 Option C). The sidecar-vs-binary scope split is by design — sidecars cover what the offline parser can't get (compile state + reflection-walked CDO shape post-instantiation); the binary parser covers what bytes already know (FPropertyTag-iterated default values). | Source: 2026-04-25 user-asked spot-check; A/B verified empirically against Project A (5.6, control) vs Project B (5.7, test) via direct invocation of `executeOfflineTool('read_asset_properties', ...)` from a throwaway probe script (deleted post-spot-check). **CRITICAL CROSS-VERSION FINDING**: the offline `uasset-parser.mjs` **fails catastrophically on every 5.7 .uasset attempted** — errors land at offsets 370-502 (FPackageFileSummary / name table / export table region) with `truncated read: need 7,799,221,272 bytes at offset 502, only 169159 remain` (length-field misread interpreted as huge unsigned) or `offset out of range. Received -3793359237` (same misread interpreted as negative signed). Parser hits header-walk failure BEFORE reaching any CDO iteration. Failure consistent across 4 Project B BPs tried (player + ability + effect + game-mode samples). **5.6 control proves parser is correct on its target version**: same script against Project A's canonical test player BP `/Game/Blueprints/Character/BP_OSPlayerR` returns 25 props decoded cleanly with **3 arrays surfaced including `DefaultAbilities` (TArray<TSubclassOf<>>, length=16, structured `{objectName, packagePath, kind}` entries) and `DefaultEffects` (length=3) and `Rigged Character 2Colours` (FLinearColor[4])** — exactly the L2.5 simple-element TArray contract from D46. **S-B-overrides scope amendment**: was originally framed (D70 §7 / backlog) as K2Node/UEdGraph pin-type drift (`FEdGraphPinType.Serialize` trailing bool `bSerializeAsSinglePrecisionFloat`, FText HistoryType enum additions). The 5.7 reality is broader: there's also a **header-layer binary delta** (FPackageFileSummary fields, FNameEntrySerialized format, OR FObjectExport stride change — D38 noted UE 5.0+ uses 112-byte stride; 5.7 may have added fields) that breaks the parser before the K2Node walk is even reached. **S-B-overrides handoff (when drafted) needs to scope BOTH layers** — header-layer + K2Node-layer — not just the K2Node delta originally documented. **Sidecar artifacts on 5.7 ARE reliable** (engine-generated by C++ commandlet, no parser drift) — so workflows needing Project B BP shape + compile state work via the sidecar path; only the binary parser path is broken. **D71 claim "BP_OSPlayerR lost DefaultAbilities/DefaultEffects in gameplay refactor" empirically OUTDATED** as of 2026-04-25 — those arrays are back (16 + 3 entries respectively, decoded cleanly via L2.5 array path). The CL-1 fixture-swap to `BP_OSPlayerR_VikramProto` (which now ENOENTs — also outdated state) may be re-evaluable as part of T-1b follow-on or a future fixture-cleanup pass. **S-B-overrides priority signal bumped**: was tracked in orchestrator-state §Open orchestrator calls as "lower priority until secondary target materializes." Project B is now empirically materialized AND on 5.7 AND in active use (24 sidecars dated today). Whether to dispatch S-B-overrides next is a Noah call — current Wave 4 sequencing is M3/M4/M5 first, S-B-overrides parallelizes; new question is whether Project-B-cross-version pressure outranks Wave 4. |
| D92 | D81-SANITIZATION-FIXES shipped 2026-04-25 (commit `5c48718`, single PR-shaped, 7 files, +63/-27). **All 10 audit findings closed in single commit per audit recommended order** (D49-tolerable since co-motivated by D81). **F-1 (HIGH)** `test-uemcp-gate.bat` now §.bat-convention compliant — AUTO_YES + EXIT_CODE map (0=PASS / 1=missing-file / 2=gate-didn't-fire / 3=cancelled) + single `:end` + `endlocal & exit /b %EXIT_CODE%` idiom. Commit `66bf214` had only patched the GUI fallback; this brings the script fully into convention compliance, joining `setup-uemcp.bat` + `sync-plugin.bat`. **F-2/F-7** `docs/README.md` 4 dead audit-file links → "session-local artifact" notes. **F-3** CLAUDE.md 4 dead pointers (lines 92-93 + 368-369) → past-tense narrative (D-log already captures the findings). **F-4** `docs/specs/phase3-plugin-design-inputs.md` "Source audit" pointer → past-tense. **F-5** `server/test-uasset-parser.mjs` int64-salvage skip log tightened to `[SKIP-NEED-ENV]` format + CLAUDE.md run command now documents `UEMCP_VFX_FIXTURE_RELPATH` env var. **F-6** `setup-uemcp.bat` INTERACTIVE → AUTO_YES rename + polarity flip (uniform sentinel naming across all 3 scripts). **F-8** README.md hardcoded `D:\DevTools\UEMCP\server` → `<UEMCP_REPO_PATH>/server` (line 56 only; lines 67/124 explicitly out-of-scope per audit Open Items). **F-9** README forward-slash normalization + CLAUDE.md §Public-Repo Hygiene now codifies per-file-type vocabulary table. **F-10** CLAUDE.md asset-namespace trade-off paragraph in §Public-Repo Hygiene (forward-write hygiene clarification). | Source: D92 worker final report 2026-04-25, commit `5c48718`. **All 3 repo-root .bat scripts converge on AUTO_YES + EXIT_CODE + :end + endlocal & exit /b %EXIT_CODE% idiom** — verified empirically by worker via `cmd //c setup-uemcp.bat fake.uproject` returning 1 + `cmd //c test-uemcp-gate.bat fake.uproject` returning 1. **Pre-commit codename grep verified**: 0 matches in diff + 0 in commit message against `.git/info/forbidden-tokens` (4 tokens) — D82 NDA-gate clean. **Test rotation post-commit**: 11 unit-runnable JS files, **1372 assertions / 0 failures** (UNREAL_PROJECT_ROOT pointed at primary target). 4-assertion delta vs CLEANUP-MICRO's 1376 baseline = fixture-availability noise (likely test-phase1 fixture variance — `BP_OSPlayerR_VikramProto` ENOENTs per D91; T-1b migrations in flight). No actual failure surface. test-m1-ping deliberately excluded (live-editor gated). **No new findings while implementing** — audit's 10 findings were exhaustive for the D81-regression class. **Out-of-scope drift from audit Open Items §2-§5 held untouched per dispatch**: CLAUDE.md (D1-D50) numbering at lines 157+367 (D-log header is stale — should be D1-D92 now); CLAUDE.md "1203 assertions" claim (drifted across D85→D87→D90→D92, current empirical = ~1372-1376); README.md:124 Binaries Intermediate typo (pre-D81); README.md:67 hardcoded path (later instance not bundled with F-8); `testing-strategy.md` template path. **Queueable as a single-commit CLAUDE.md/README grooming pass when convenient** — small scope, no architectural decisions, parallel-safe with everything. **Audit-handoff template fix** (D89 meta-finding): post-D81 audit handoffs that produce gitignored deliverables should drop the "Single commit / Commit SHA" requirement and replace with "Filesystem path for orchestrator pickup." This was respected by D92 dispatch implicitly (audit doc reference path not commit SHA) but should be codified into the next audit handoff drafted (e.g., when audit-batch-2 dispatches to close the 22 remaining D79 findings). **D81 sanitization saga officially closed** — D81 (initial sweep) → D82 (NDA-gate) → D89 (regression audit) → D92 (regression fixes) → end. Future regressions would be new audit cycles, not D81 reopening. |
| D93 | M3-actors shipped 2026-04-26 (commits `916a688` ship + `c161aec` numeric-string enum parity follow-on). **All 10 actors-toolset tools live on TCP:55558**: `get_actors_in_level`, `find_actors_by_name`, `spawn_actor`, `delete_actor`, `set_actor_transform`, `get_actor_properties`, `set_actor_property`, `spawn_blueprint_actor`, `focus_viewport`, `take_screenshot`. tools.yaml `actors:` `layer:` flipped from `tcp-55557` → `tcp-55558`. Test rotation **1376 → 1401 / 0 failures across 12 suites** (+25 net: +107 in new `server/test-m3-actors.mjs`, ~16 stripped from `server/test-tcp-tools.mjs` actors-section duplicates that got replaced with BP/widget vehicles). **Pattern decisions for M3-blueprints-write + M3-widgets to inherit** (umbrella overview at `docs/handoffs/m3-oracle-retirement-overview.md` updated; sub-handoffs at `m3-blueprints-write-rebuild.md` + `m3-widgets-rebuild.md` updated 2026-04-26 to reflect): (a) **flat-file layout** — single `Private/<Toolset>Handlers.cpp` + `Public/<Toolset>Handlers.h` pair per toolset, NOT a `Private/<Toolset>/` subdir (diverges from initial handoff suggestion for marginal organizational gain — sibling files become `BlueprintHandlers.cpp` + `WidgetHandlers.cpp`). (b) **No per-handler MCPThreadMarshal** — central marshal at `MCPCommandRegistry::Dispatch` (D90 ownership) covers all handlers automatically + provides P0-11 hitch instrumentation automatically. Handlers are pure business logic + `BuildSuccessResponse`/`BuildErrorResponse` calls; no per-handler thread plumbing required. (c) **Wire-type strings stay identical to oracle** (modulo P0-1 envelope) — minimizes migrated-caller churn; P0-4 `code` field is purely additive in the error envelope. (d) **Test file naming**: `server/test-m3-<toolset>.mjs` (per-toolset for clear failure attribution). (e) **Build.cs unchanged** — Engine + UnrealEd + LevelEditor + Json cover everything M3-actors needed; oracle's Build.cs is a strict subset of ours. | Source: M3-actors worker final report 2026-04-26, commits `916a688` + `c161aec`. **Conformance verification**: response shape parity verified vs canned oracle TCP:55557 fixtures (Group 5 in `test-m3-actors.mjs`). All 5 actor JSON fields preserved (name/class/location/rotation/scale); oracle vector ordering [x,y,z] and rotator ordering [pitch,yaw,roll] confirmed. **Numeric-string enum coercion follow-on (commit `c161aec`)**: `set_actor_property` enum-string path needed parity patch with oracle's coercion behavior; landed cleanly. **D87 deployment-gap status**: VERIFIED-BY-TEST (registration, port routing, wire-translation, Zod, error propagation, conformance fixtures); VERIFIED-PENDING-DEPLOYMENT (compile via Build.bat); VERIFIED-PENDING-LIVE-SMOKE (end-to-end against running editor). Noah owns sync-plugin.bat → Build.bat → relaunch → restart cycle per D87. Smoke plan drafted (6 calls, with §0.0 process pre-flight per `feedback_smoke_handoff_preflight.md` memory — worker followed the latest convention). **UE 5.6 deprecation flag for institutional memory**: `FImageUtils::CompressImageArray` is deprecated-since-5.0 but oracle uses identical signature in 5.6 production; if Build.bat fails on M3-actors compile, this is the first place to look (migrate to `FImageUtils::CompressImage(FImageView, "png", 0)`). Add to D78/D81/D87/D88 institutional-memory list. **Architectural takeaway captured by worker**: the central-marshal-at-Dispatch model means handler files are just business logic — no thread plumbing, just response builder calls. M3-blueprints-write + M3-widgets get this for free. **M3 sub-worker dispatch now unblocked**: M3-blueprints-write + M3-widgets handoffs updated 2026-04-26 with the inherited pattern decisions; both can dispatch in parallel post-M3-actors. **Next dispatch options**: M3-blueprints-write (15 BP-write + 6 orphan node handlers) + M3-widgets (7 widget tools, 2 KNOWN-ISSUE flagged for in-rebuild fix) — both file-disjoint with each other per the flat-file pattern (BlueprintHandlers.cpp vs WidgetHandlers.cpp + separate yaml sections + separate test files). |
| D94 | DOCS-GROOMING shipped 2026-04-26 (commit `1a65d0f`). **6 of 6 audit Open Items §2-§5 closures landed in single commit** (path-limited per D49; co-motivated with D89 audit follow-through): §1 CLAUDE.md `D1-D50` → `D1-D92` at lines 157 + 390; §2 CLAUDE.md `1203 / 10 files` → `~1376 unit-runnable / 11 files (±4 per T-1b)` at lines 94 + 395 with growth cadence extended through D85/D87/D90; §3 README.md:124 typo (second `Intermediate` path now has `<project>\Plugins\UEMCP\` prefix); §4 README.md:67 hardcoded `D:\DevTools\UEMCP\server` → `<UEMCP_REPO_PATH>/server`; §5 `testing-strategy.md:28-29` sibling-template path `~/.claude/mcp-servers/unreal` → `<UEMCP_REPO_PATH>/server`; **§6 `.githooks/pre-commit` hook shipped + tested** mirroring `.githooks/pre-push` shape — scans staged diff via `grep -F -i -f` against `.git/info/forbidden-tokens`, exits 1 with clear message on match. CLAUDE.md §Public-Repo Hygiene rewritten to describe both hooks. | Source: DOCS-GROOMING worker final report 2026-04-26, commit `1a65d0f`. **Pre-commit codename grep verified**: 0 matches in staged diff + 0 matches in commit message against `.git/info/forbidden-tokens` (11 tokens). **Test rotation structurally unchanged** (no test code touched): test-mock-seam 45/0; test-mcp-wire 54/55 (1 env-dependent skip, expected without real `UNREAL_PROJECT_ROOT`). **§6 pre-commit hook empirically verified**: clean-diff path returns exit 0; grep machinery verified against synthetic in-diff token at lines 128/129 of test diff. **CRITICAL NEW SAFETY FINDING — pre-existing UTF-8 locale bug in `.githooks/pre-push`**: empty `LANG` on Git Bash for Windows causes `grep -F -i -f` to silently miss matches on lines containing multi-byte chars (Unicode box-drawing in CLAUDE.md, em-dashes in READMEs). The pre-push gate has been LEAKIER THAN IT APPEARED for as long as it's existed — codenames adjacent to multi-byte chars on the same line could pass the gate undetected. Discovered experimentally by DOCS-GROOMING worker while wiring the parallel pre-commit hook (which works around it via `export LC_ALL=C.UTF-8`). **Inline orchestrator fix landed in D-log commit alongside D93/D94/D95**: same `export LC_ALL=C.UTF-8` line added to `.githooks/pre-push` to close the gap. Both hooks now UTF-8-safe. **Implication for prior pushes**: theoretically, codenames pushed alongside multi-byte chars in earlier commits may have passed the gate without triggering. Audit value low — the gate's primary defense is intent (orchestrator scrubs proactively per `feedback_pre_commit_codename_scrub.md` memory) + the public history scrub already happened (D81). But orchestrator should consider a one-time history grep against `.git/info/forbidden-tokens` with `LC_ALL=C.UTF-8` set to confirm no historical leaks slipped through the bug. Queue as a follow-on if the audit value rises. **Pattern lesson**: locale assumptions in shell scripts are silent failures — they don't error, they just match less than expected. Future shell hooks / scripts that do text matching should default to `export LC_ALL=C.UTF-8` (or equivalent) at script start. |
| D95 | M4 scope-empty redirect 2026-04-26. **No commit, no code change.** M4 worker session correctly STOPPED before any implementation upon discovering all 12 M4-scoped reads-side tools are already shipped via M-enhance (D77). Worker provided 12-row evidence table mapping each "M4 tool" to its existing JS schema in `server/menhance-tcp-tools.mjs` + registered plugin C++ handler. Empirical verification of yaml `layer:` post-M-enhance: blueprint-read = `tcp-55558` (line 619 of tools.yaml), asset-registry = `tcp-55558` (line 679), data-assets = `tcp-55558` (line 788). The M4 handoff's premise — "filter tools.yaml for `layer: tcp-55557`" — returns empty for these 3 toolsets because they were never on `tcp-55557`. **Orchestrator decision: close M4 as superseded** (worker option 1 of 3); update `docs/handoffs/m4-reduced-reads.md` with SUPERSEDED header (already landed). | Source: M4 worker final report 2026-04-26 (no commit; scope-empty finding). **Framing-error analysis**: D58 §Q5.3 (re-sequenced plan at `docs/research/phase3-resequence-mcp-first-2026-04-20.md:339`) explicitly enumerated M4 as `blueprint-read (6) + asset-registry (2) + data-assets (4) = 12 tools`. M-enhance (D77) shipped 36 tools across `11 FULL-RC + 10 FULL-TCP + 13 PARTIAL-RC + 1 sidecar + 1 visual-capture` — and absorbed the M4 12-tool reads-side scope as part of the FULL-TCP + PARTIAL-RC sets. **The scope absorption was never explicitly recorded as "M-enhance absorbing M4"** in D77's ship narrative or any subsequent D-log entry. Orchestrator drafted M4 handoff in prior turn by analogy to M3's "tcp-55557 → tcp-55558 migration" without verifying M4 toolsets' starting state empirically — exactly the failure mode `feedback_handoff_empirical_verification.md` memory was created to prevent. The worker's restraint (no scope invention; advisor consulted; clear redirect options) is the correct empirical-verification pattern at the receiving-session edge — it caught the orchestrator's framing error before any code shipped. **Worker's 3 redirect options preserved as future-dispatch candidates**: (1) close as superseded — chosen; (2) audit M-enhance reads implementation for gaps the M4 handoff implicitly assumed (P0-1 envelope parity, conformance vs `docs/specs/conformance-oracle-contracts.md` reads subset, hitch instrumentation coverage on 5 reflection_walk-fanned-out tools, edge-case coverage for PARTIAL-RC transforms like `transformBlueprintComponents` at `menhance-tcp-tools.mjs:322-339`); (3) repurpose as RC-augmentation follow-on per FA-ε §Q6 (PARTIAL-RC promotion from "plugin-primary" to genuine RC-internal-substrate). Option (2) is the realest M4-shaped follow-on if a follow-on is needed; bundle with audit-batch-2 (22 remaining D79 findings) when that fires. Option (3) is real but FA-ε author flagged as deferred optimization, not near-term need. **Pattern lesson for orchestrator handoff drafting**: never draft a "rebuild X from layer Y to layer Z" handoff by analogy to a sibling milestone without first running `grep "layer:" tools.yaml` (or equivalent empirical check) against the actual current state. The drafted-by-analogy pattern is the failure mode; the empirical-check-first pattern is the corrective. Saved to memory at `feedback_orchestrator_handoff_starting_state_check.md`. **Phase 3 progress correction**: M4 was counted as a remaining 3-5 sessions in orchestrator-state Wave 4 estimate; it's actually 0 sessions remaining (already shipped under M-enhance). Wave 4 remaining is now M3-blueprints-write + M3-widgets + M5 = ~10-15 sessions instead of ~13-20 (3-5 session reduction). **No commit for D95** — deliverable is the SUPERSEDED handoff header + this D-log entry. |
| D96 | M3-widgets shipped 2026-04-26 (commit `315efb2`, 9 files, +1590/-359). **All 7 widgets-toolset tools live on TCP:55558**: `create_widget`, `add_text_block`, `add_button`, `bind_widget_event`, `set_text_block_binding` (FIXED — was Bug-1), `add_widget_to_viewport` (FIXED — was Bug-2), `add_input_action_node`. tools.yaml `widgets:` `layer:` flipped to `tcp-55558`; KNOWN-ISSUE annotations removed from the 2 fixed handlers. **Bug-1 root cause + fix** (oracle `UMGCommands.cpp:528-534`): connected EntryThenPin (exec) → GetVarOutPin (data), invalid K2 schema; AND never registered `FDelegateEditorBinding`. Fix: pure function graph (`FUNC_BlueprintPure | FUNC_Const`), `K2Node_FunctionResult` with `FText ReturnValue` UDP, data→data wiring, then `WidgetBlueprint->Bindings.Add({ObjectName, PropertyName=Text, FunctionName, Kind=Function})`. Wire response gains `function_name` field. **Bug-2 root cause + fix** (oracle `UMGCommands.cpp:198-237`): NO-OP returning a "use Blueprint nodes" note. Fix: gates on PIE — running → `CreateWidget(PC, class) + AddToViewport(z_order)`; off → `NOT_IN_PIE` typed error. Wire response gains `added_to_viewport: true`. Test rotation **1401 → 1435 / 0 across 13 suites** (+88 in new `test-m3-widgets.mjs` across 12 groups; ~38 stripped from `test-tcp-tools.mjs` widgets duplicates). | Source: M3-widgets worker final report 2026-04-26, commit `315efb2`. **Conformance verification**: 5 working handlers — exact oracle parity verified vs canned 55557 fixtures. 2 fixed handlers — intentional divergence (additive fields; oracle "use Blueprint nodes" note removed). Divergence IS the fix. **D87 status**: VERIFIED-BY-TEST (registration, routing, translation, Zod, error propagation, conformance fixtures, both bug-fix regressions); VERIFIED-PENDING-DEPLOYMENT (Build.cs unchanged — UMG/UMGEditor/Engine/UnrealEd/Json cover all referenced symbols); VERIFIED-PENDING-LIVE-SMOKE for both fixed handlers — wire-shape doesn't prove UMG evaluates the binding per-frame nor that AddToViewport renders. **UE 5.6 institutional-memory additions** (extends D78/D81/D87/D88/D93): `FDelegateEditorBinding{Kind=EBindingKind::Function}` is the canonical UMG property-binding registration (no 5.6 deprecation); `K2Node_FunctionEntry::AddExtraFlags(FUNC_BlueprintPure | FUNC_Const) + ReconstructNode()` marks a function pure post-creation; `K2Node_FunctionResult::CreateUserDefinedPin(name, type, EGPD_Input)` adds return values (input pins on result = function outputs); prefer `WidgetTree->ConstructWidget<T>` over the oracle's `NewObject<UButton>(GeneratedClass->GetDefaultObject(), ...)` (which violates hierarchy invariants). **Smoke plan drafted** (Noah's deploy cycle, per-handler validation): (1) create_widget; (2) add_text_block; (3) Bug-1 live: set_text_block_binding → BP editor confirms pure FText getter + binding visible in TextBlock.Text dropdown; (4) Bug-2 sad: PIE-off → code:NOT_IN_PIE; (5) Bug-2 happy: start_pie → added_to_viewport:true + visible in PIE; (6) bind_widget_event(OnClicked) on Button → BP event node. **CRITICAL ORCHESTRATION FINDING — parallel-worker shared-file collision** (codified in `feedback_parallel_subworker_shared_config_collision.md` memory): umbrella overview claimed M3-blueprints-write + M3-widgets are file-disjoint. True at handler-source level (`WidgetHandlers.*` vs `BlueprintHandlers.*`) — false at integration layer: `MCPCommandRegistry.cpp`, `server.mjs`, `tcp-tools.mjs`, `tools.yaml` are file-SHARED. Both workers needed hunks in all four. Manifested as edit/revert thrash — M3-widgets edited shared files, M3-bpw's WIP arrived externally in working copy, M3-widgets stashed + re-edited, repeat (3 stashes total before clean commit landed). M3-bpw landed atop M3-widgets via re-application. **Lesson for future M5 (3-4 sub-workers) + any other parallel-sub-worker dispatch**: orchestrator must inventory shared files BEFORE drafting umbrella overview + pick integration strategy: (a) sequence shared-file landings explicitly, or (b) shard the registration/config files per toolset upfront. Orchestrator amended `docs/handoffs/m3-oracle-retirement-overview.md §File-collision-safety strategy` post-hoc with this finding for future readers. **TCP:55557 retirement decision (recommendation forwarded for orchestrator pickup)**: post-M3-bpw, all 32 transitional tools live on TCP:55558. Recommendation: defer formal deprecation by ~1 deployment cycle so live-fire smoke confirms all 32 end-to-end (particularly the 2 PIE-dependent fixes). Then strip TCP:55557 client code (ConnectionManager 55557 paths + cm.send routing + ResultCache key prefix) and remove `<project>/Plugins/UnrealMCP/`. |
| D97 | M3-blueprints-write shipped 2026-04-26 (commit `0c7448c`). **15 BP-write tools live on TCP:55558** (NOT 21 — handoff prose double-counted; per `docs/specs/conformance-oracle-contracts.md §8.1` the 6 BP-node "orphans" were already absorbed into the 15-tool toolset, not separate endpoints). All 15 register cleanly in `MCPCommandRegistry::Dispatch`. tools.yaml `blueprints-write:` `layer:` flipped to `tcp-55558`. **Orphan-handler dispatch shape decision**: one named handler per command type (mirrors oracle + M3-actors precedent). Switch-by-node-type rejected — would burn an extra match in the hot path for no test/call-site benefit. Each handler is ~30-100 lines, focused on its single command. Test rotation **post-both-M3-sub-worker-ships: 1563 / 0 across 13 suites** (+162 in new `test-m3-blueprints-write.mjs`; baseline reconciled per worker report — pre-widgets 1313 + 88 widgets + 162 BPW = 1563). | Source: M3-blueprints-write worker final report 2026-04-26, commit `0c7448c`. **Landed on top of M3-widgets** via shared-file re-application after the parallel-worker collision noted in D96. Worker's tcp-tools.mjs deletion stash `@{0}` + broader pre-widgets edits stash `@{1}` are now obsolete (superseded by clean commit). **Smoke plan drafted** (idempotent, ~7 calls): (1) create_blueprint(name="BP_M3Smoke", parent_class="Actor") → expect {name, path}; (2) add_component(blueprint_name="BP_M3Smoke", component_type="StaticMesh", component_name="Mesh") → auto-compiles; (3) set_component_property(blueprint_name="BP_M3Smoke", component_name="Mesh", property_name="Mobility", property_value="Movable"); (4) add_event_node(blueprint_name="BP_M3Smoke", event_name="ReceiveBeginPlay") → captures GUID; (5) add_function_node(blueprint_name="BP_M3Smoke", function_name="PrintString", target="KismetSystemLibrary") → captures GUID; (6) connect_nodes linking BeginPlay.Then → PrintString.exec; (7) compile_blueprint(blueprint_name="BP_M3Smoke") → {compiled:true}. Cleanup: delete `/Game/Blueprints/BP_M3Smoke` in editor before re-running. **Conformance verification**: wire-shape parity vs oracle confirmed by canned-fixture tests for all 15 tools (Group 4 in `test-m3-blueprints-write.mjs`). Modulo P0-1 envelope (code field additive on errors), responses are identical. **UE 5.6 API findings** (extends institutional memory): `PC_Float` deprecated → use `PC_Real` with `PC_Float` subcategory (used in `add_blueprint_variable`); oracle's `FindOrCreateEventGraph` helper not in CommonUtils we link — reimplemented locally via `FBlueprintEditorUtils::FindEventGraph`. **D87 status**: VERIFIED-BY-TEST (registration, routing, conformance fixtures); VERIFIED-PENDING-DEPLOYMENT (Build.cs unchanged, no new module deps); VERIFIED-PENDING-LIVE-SMOKE (Noah owns sync-plugin.bat → Build.bat → editor relaunch → Claude Code restart cycle). **2 stale stashes for Noah convenience-cleanup**: `stash@{0}` (M3-bpw's old tcp-tools.mjs deletion attempt) + `stash@{1}` (broader pre-widgets shared-file edits) — both obsolete after `0c7448c`; safe to `git stash drop stash@{0}` twice when convenient. |
| D98 | **M3 milestone complete 2026-04-26** — all 32 transitional oracle-retirement tools (10 actors per D93 + 15 BP-write per D97 + 7 widgets per D96) now live on TCP:55558. tools.yaml verification: ZERO toolsets remain on `tcp-55557` (offline + 14 toolsets × tcp-55558 + 1 toolset × http-30010, per post-D97 grep). The conformance oracle (existing UnrealMCP plugin TCP:55557, used since Phase 2) is now functionally redundant for the 32-tool set it covers. **D23 oracle retirement decision now empirically actionable**. | Source: orchestrator pickup of D96 + D97 worker reports + post-ship tools.yaml grep verification. **Phase 3 status update**: Wave 1 (M1 + M-spatial + Oracle-A/v2) ✅ + Wave 2 (S-B-base + Verb-surface) ✅ + Wave 3 (M-enhance HYBRID + Phase 4 absorbed) ✅ + Validation cycle (audit + T-1b + smoke + audit-fixes 1/2/3 + SMOKE-FIX + deployment) ✅ + M3 (3 sub-workers — actors + BP-write + widgets) ✅ + M4 (SUPERSEDED per D95, scope absorbed under M-enhance) = M5 is now the ONLY remaining Wave 4 milestone. **Phase 3 ~95% complete** (was 85% before this batch). Remaining session estimate: M5 (6-10 sessions, 3-4 sub-workers per orchestrator-state) + optional follow-ons (S-B-overrides per D91 if cross-version pressure surfaces; M-enhance reads conformance audit per D95 as M4 redirect option 2; audit-batch-2 22 remaining D79 findings; TCP:55557 client deletion). **TCP:55557 formal retirement queued** (per D96 worker recommendation): defer by ~1 deployment cycle so live-fire smoke confirms all 32 tools end-to-end, particularly the 2 PIE-dependent widgets fixes (Bug-1 binding + Bug-2 viewport). After smoke confirms: (1) strip TCP:55557 client code from `server/connection-manager.mjs` (the 55557 transport paths + cm.send routing + ResultCache key prefix); (2) remove `<project>/Plugins/UnrealMCP/` directory from project deployments (Noah action); (3) follow-on cleanup-worker handoff drafted post-smoke. **Wave 4 dispatch shape going forward**: M5 is now the operative dispatch surface. Per D96 collision lesson: when drafting M5 umbrella overview, inventory shared registration/config files BEFORE drafting sub-worker handoffs + pick (a) sequence-landings or (b) shard-upfront integration strategy. M5 has 3-4 sub-workers (animation + materials + visual-capture + etc.); option (b) shard-upfront is recommended for clean parallelism. |
| D99 | Post-M3 deployment smoke complete 2026-04-26 (results doc gitignored at `docs/testing/post-m3-deployment-smoke-2026-04-26.md`). **Bug 4 CLOSED LIVE — M5 visual-capture gate empirically clears**: `get_asset_preview_render` returned 1985-byte StaticMesh + 883-byte Blueprint thumbnails post-deployment. **M3 hotfix 94e115a verified live** implicitly via §7 BP-write smoke (bp_call_function path exercised cleanly). **Pre-flight (§0.0-§0.3) all PASS** — editor PID 13068; both CompressImageData + Blueprint->GeneratedClass.Get() source markers present; DLL mtime 11:46:36 post all source mtimes. **M5 dispatch gate: CLEAR.** **TCP:55557 retirement gate: PARTIAL** — BP-write CLEAR (M3-bpw §7 full pass 7/7); M3-actors + M3-widgets need targeted bug-fix workers first per findings below. | Source: post-M3 deployment smoke worker final report 2026-04-26. **8 findings surfaced** (the kind wire-mock + audit + commit-time tests can't catch; only live-fire surfaces this class): (1) **`set_actor_property` Mobility fails on StaticMeshActor** — handler doesn't traverse to RootComponent for component-property writes; M3-actors handler gap. (2) **`take_screenshot` silent file-write failure** — returns `status:success` but no file on disk; M3-actors handler gap. (3) **M3-widgets Bug-2 HAPPY path NOT VERIFIED** — widget lookup fails when PIE is running; the `add_widget_to_viewport` PIE-gated fix from D96 ships correctly but the upstream lookup (find widget instance after CreateWidget) breaks when PIE worlds are active. (4) **M3-widgets `bind_widget_event` post-`add_button` broken** — chain of widgets-mutation calls breaks delegate binding. (5) **M3-widgets perf hitches 2-4s per widget mutation** — well above the ~300ms hitch threshold; 4 of 8 hitch log entries are widgets. (6) **FA-ε §Open 3 write-side isolation INCONSISTENT** — TCP `set_blueprint_property bCanBeDamaged=false` returned success; RC `rc_get_property` returned `true`. Cross-transport transaction semantics broken in the write→read direction. (7) **RC toolset tip has wrong CDO path form** — claims `BP.BP_C:Default__BP_C`; actual working form is `BP.Default__BP_C`. tools.yaml doc fix. (8) **Bug 4 response mime label is `image/png` but bytes are JPEG/JFIF** — UE's `CompressImageData` produces JPEG, not PNG. Label is misleading; bytes are valid (just wrong-typed). VisualCaptureHandler.cpp + tools.yaml fix. **Findings categorization for orchestrator dispatch**: Code bugs needing targeted fixes (1+2+3+4+8): bundle as CLEANUP-M3-FIXES worker — file-disjoint across ActorHandlers.cpp + WidgetHandlers.cpp + VisualCaptureHandler.cpp + tools.yaml; small surgical scope; ~1-2 sessions. Investigation needed (5): WIDGETS-PERF worker — measure first then fix; ~1-2 sessions. Investigation needed (6): FA-ε WRITE-SIDE AUDIT worker — cross-transport transactional semantics; ~1-2 sessions; could expand into a structural fix or just document the limitation. Doc fix (7): bundle into CLEANUP-M3-FIXES worker (tools.yaml only). **§3 NO-RACE-OBSERVED for §4 PIE teardown** is the M-enhance §Biggest-unknowns 4 closure — no observable race across 3 start/stop cycles (FA-ε §Open 4 closes here pending future high-frequency stress). **Hitch instrumentation (D83/D88) producing the right signal** — 8 entries from `findstr "hitch:" <ProjectName>.log` post-smoke; 4 above 300ms all in M3-widgets cluster (corroborates finding #5). **Verification-cycle pattern proving its value AGAIN**: wire-mock + commit-time tests reported 1563/0 green; 8 live-fire bugs needed real-editor smoke to surface. None of these would have been caught by automation alone. Smoke remains a load-bearing verification layer for M3-class plugin C++ ships. **Wave 4 dispatch verdict**: M5 unblocked — ready when Noah signals go. CLEANUP-M3-FIXES + WIDGETS-PERF + FA-ε WRITE-SIDE AUDIT all dispatchable now (file-disjoint with M5 + each other). TCP:55557 formal retirement still queued; CLEANUP-M3-FIXES landing + a re-smoke would close it. |
| D100 | Streamlined gauntlet shipped 2026-04-26 (combined session with the post-M3 smoke worker). 30-call streamlined gauntlet executed at smoke-tail; sharpened 3 D99 smoke findings + surfaced 3 new ones (9 total findings across 6 categories). Results doc at `docs/testing/post-m3-gauntlet-2026-04-26.md` (gitignored). New full-gauntlet handoff at `docs/handoffs/post-milestone-gauntlet.md` (~250 calls, for major milestone boundaries). **Three sharpenings the gauntlet caught that the original smoke missed**: (1) **M3-widgets PIE-lookup is broader than D99 #3 framed it** — gauntlet proved ALL 3 widgets-toolset asset-lookup tools fail under PIE (`add_text_block`, `set_text_block_binding`, `add_widget_to_viewport`), not just one. AND that BP-write / actors / blueprint-read tools all work fine under PIE. Fix is narrowly scoped to the widgets-handler resolution path (probably uses GetWorld() instead of explicit world selection). (2) **NEW cross-transport finding (extends D99 #6)** — newly-created BP CDOs get unloaded from memory after PIE cycles, so RC HTTP can lose access to assets that disk-side still exist. CDO lifecycle issue. (3) **subobject traversal pattern is universal, not just Mobility (extends D99 #1)** — gauntlet added Visibility (same RootComponent path) + Intensity (different subobject — LightComponent on PointLight). Pattern: `set_actor_property` reaches ZERO subobjects, not just "doesn't traverse to RootComponent." Fix needs to be the broader subobject-resolution chain, not just RootComponent special-casing. | Source: combined post-M3 smoke + gauntlet worker final report 2026-04-26. **CLEANUP-M3-FIXES is currently in flight** with the narrower D99 framing. Gauntlet's broader scope is a likely scope-amendment; CLEANUP-M3-FIXES worker may catch the broader pattern (engineers often do during root-cause investigation), or may fix narrowly. **Orchestrator decision**: do NOT modify the in-flight CLEANUP-M3-FIXES handoff (handoff already in worker context); when CLEANUP-M3-FIXES lands, evaluate breadth + queue CLEANUP-M3-FIXES-2 follow-on if narrow patches missed the broader patterns. **Streamlined-gauntlet pattern** (worker recommendation worth codifying): ~30 calls, ~30 min, executed at smoke-tail as a "dessert course." High-yield lower-cost middle ground vs the full ~250-call gauntlet handoff (kept available for major milestone boundaries). Bake this into the next post-deployment-smoke handoff: §11+ section running the streamlined gauntlet after the focused per-milestone smoke completes. **New handoff `post-milestone-gauntlet.md`** is now an institutional resource for major milestone boundaries (Phase 3 close, Phase 5 distribution gate, Phase 6 per-project tuning). **Test artifacts pending manual delete** (Noah convenience-cleanup whenever): `/Game/Blueprints/BP_Gauntlet1`, `/Game/Widgets/WBP_PIETest`. **Verification-cycle pattern reinforcement**: smoke catches surface bugs; streamlined gauntlet at smoke-tail catches scope-aliasing (1-tool finding really being 3-tool pattern; one-property finding really being subobject-resolution pattern). The two layers together produce sharper bug reports than either alone. |
| D101 | M5 scope-verifier shipped 2026-04-26 (audit doc gitignored at `docs/audits/m5-scope-verification-2026-04-26.md`). **Aggregate verdict: PARTIALLY-REMAINING (option b per handoff §2)**. Empirical scope is **19 tools across 5 toolsets, not the orchestrator-state's 31** — M-enhance D77 absorbed 12/31 tools as reads-side coverage (every M5 toolset shipped its reads + left its mutations unshipped, mirroring the M4 absorption pattern but partially). Per-toolset shipped/total: animation 4/8, materials 2/5, input-and-pie 4/7, geometry 1/4, editor-utility 1/7. **Recommended 3 sub-workers, ~4-5.5 sessions** (down from orchestrator-state's 6-10): (a) **M5-animation+materials** — 7 not-shipped (4 anim + 3 materials), ~1.5-2 sessions; (b) **M5-input+geometry** — 6 not-shipped Enhanced Input + procedural mesh, ~1-1.5 sessions; (c) **M5-editor-utility** — 6 not-shipped, security-sensitive (Python exec + asset delete), ~1.5-2 sessions. **File-collision-safety per D96 lesson**: shared files inventoried (MCPCommandRegistry.cpp, server.mjs, tools.yaml, optionally menhance-tcp-tools.mjs). **Recommended (b) shard-upfront**: orchestrator pre-lands a tiny prep commit reserving stub `#include`/`Register*Handlers()` insertion points + new per-handler file stubs before dispatching sub-workers. Per-handler `.cpp` files fully disjoint per sub-worker; per-toolset JS file split (`m5-animation-tools.mjs` etc.) keeps schema files disjoint. Fallback: (a) sequence-landings editor-utility → animation+materials → input+geometry. | Source: M5 scope-verifier worker final report 2026-04-26. **Empirical-check-before-drafting pattern paid off** — exactly the M4-style framing-error this handoff was created to avoid. M5 sub-worker count dropped from 3-4 to 3 (small win); aggregate session estimate dropped 35% (4-5.5 vs 6-10); avoided the wasted-session class entirely. Worker followed the `feedback_orchestrator_handoff_starting_state_check.md` memory + applied the empirical-table methodology from §1 of the verifier handoff cleanly. **5 open orchestrator decisions resolved with worker's recommendations**: (i) **audit M-enhance reads conformance? DEFER to audit-batch-2** (worker recommendation accepted; bundle when audit-batch-2 fires alongside the 22 remaining D79 findings + the M4 redirect option 2 from D95). (ii) **ship `set_material_parameter` as RC delegate not plugin C++? YES** (accepted; consistent with M-enhance HYBRID transport per D66 — flat parameter writes are exactly what RC HTTP is designed for; saves a plugin C++ handler). (iii) **confirm Geometry Script plugin enabled in both projects** — Noah action; flag for verification before dispatching M5-input+geometry. If not enabled in Project B, the geometry sub-worker may need Project A as primary smoke target until Project B catches up (compounds with D91 cross-version concerns). (iv) **`run_python_command` deny-list-only or also confirmation-dialog/startup-flag-gated? deny-list + startup flag** (accepted; security-sensitive — Python execution warrants defense-in-depth; deny-list catches known dangerous patterns + startup flag means the tool is opt-in per session, not always-on). M5-editor-utility worker handoff must scope this carefully: deny-list defaults + `--enable-python-exec` startup flag + log every call. (v) **`get_audio_asset_info` may be 0-tool (offline-displaced per yaml)** — worker flagged for verification; if confirmed offline-displaced, drop from M5-animation+materials sub-worker scope (8 anim → 7 anim → effective 6 not-shipped after subtracting the 4 already-shipped + the displaced one). **Next steps for orchestrator**: (1) draft + dispatch tiny **M5-PREP** worker to land the shard-upfront infrastructure (5 stub handler-file pairs + 5 helper Register*Handlers stubs + 5 stub JS schema files); (2) draft 3 M5 sub-handoffs with the inherited M3-actors pattern decisions baked in (flat-file layout, central marshal, wire-type identity, test naming); (3) dispatch the 3 sub-workers in parallel post-M5-PREP for clean parallelism. **Wave 4 remaining session estimate corrected**: M5 actual = ~4.5-6.5 sessions (3 sub-workers + M5-PREP + integration buffer), down from orchestrator-state's previous 6-10 estimate. Phase 3 ~95% complete; M5 + optional follow-ons close it out. |
| D102 | CLEANUP-M3-FIXES shipped 2026-04-26 (commit `78032c4`, 8 files, +421/-66; D49-clean). **All 5 D99 smoke-finding fixes + 1 doc fix landed in single commit, AND worker proactively covered the broader gauntlet patterns from D100 — CLEANUP-M3-FIXES-2 NOT NEEDED.** Per-fix breakdown: **§1 set_actor_property** walks Actor→RootComponent→named subobjects + emits `set_on` field (covers gauntlet's "subobject traversal universal" sharpening — Mobility + Visibility + Intensity all fixed; not just RootComponent special-case). **§2 take_screenshot** resolves rel paths under ProjectDir + returns absolute path + byte_length; **migrates `FImageUtils::CompressImageArray` → `FImageUtils::CompressImage(FImageView)`** (closes D93 5.7-deprecation — two birds, one stone). **§3 widget PIE lookup** shared helper now uses `LoadObject<UWidgetBlueprint>` with canonical doubled object-path (covers gauntlet's "all 3 widgets-toolset asset-lookup tools fail under PIE" sharpening — fix is the resolution-path change, narrowly scoped). **§4 bind_widget_event** resolves FObjectProperty on GeneratedClass + recompiles if needed + matches `UK2Node_ComponentBoundEvent` canonical fields + emits `node_id` + typed `WIDGET_PROPERTY_MISSING` error. **§5 mime label** `image/png` → `image/jpeg` in VisualCaptureHandler + tools.yaml. **§6 RC CDO tip** `server.mjs` TOOLSET_TIPS corrected to `<AssetName>.Default__<AssetName>_C` (single-dot, no `BP_C:`). | Source: CLEANUP-M3-FIXES worker final report 2026-04-26, commit `78032c4`. **Worker proactively caught broader gauntlet patterns** despite handoff being scoped to narrower D99 framing — engineers root-causing properly often catch the broader pattern (this is the verification cycle's other compound-interest dimension: well-disposed workers extend their fix scope when investigation surfaces breadth). **Test rotation deltas**: m3-actors 117/0 (+10), m3-widgets 97/0 (+9), test-tcp-tools updated (+5 fixture/assertion, but file is currently broken at HEAD per the new finding below), rc-wire 119/0, mock-seam 45/0, mcp-wire 54/0 (1 env-var skip). **CRITICAL NEW DRIFT FINDING (worker discovery)**: `server/test-tcp-tools.mjs` is broken at HEAD since the M3-blueprints-write split (D97 / `0c7448c`) — static import from `./tcp-tools.mjs` references the deleted barrel file. Empirical confirmation: `tcp-tools.mjs` is gone (no such file). Imports must be re-pointed: `executeBlueprintsWriteTool, getBlueprintsWriteToolDefs, BLUEPRINTS_WRITE_SCHEMAS` → `./blueprints-write-tcp-tools.mjs`; `initTcpTools` → `initBlueprintsWriteTools` (6 usages in test body at lines 48 + 272 + 281 + Group 21 wire-map tests). **CLAUDE.md "1563/0" rotation claim is therefore stale** by however many assertions test-tcp-tools.mjs contributed (was the largest legacy file before M3 split; estimated ~150-200 assertions). The real rotation count is somewhere lower. Recommend tiny follow-on TEST-IMPORTS-FIX worker — 0.25-0.5 session; mechanical re-pointing + verify Group 21 tests still apply (some may need to migrate to per-toolset semantics). **Why this drift slipped past everyone**: M3 sub-workers each shipped per-toolset and stripped their section from the barrel; the test-file imports were never updated to match. Smoke runs MCP wire (not test rotation) so it didn't catch. M3 sub-worker post-ship test runs didn't catch because each sub-worker checks their OWN test file. CLEANUP-M3-FIXES worker found it because they ran the full rotation as part of their verification. **Pattern lesson**: when a milestone has multiple sub-workers each owning per-toolset file splits, the LAST sub-worker (M3-bpw in this case) inherits the cleanup-debt for legacy files that referenced the old structure. Add to next M5 sub-worker handoff drafts: "if you delete from a barrel file, grep for imports of the barrel and re-point or flag for orchestrator." **D87 deployment-gap status** for §1-§4 (plugin C++): VERIFIED-BY-TEST + PENDING-DEPLOYMENT + (for §3 + §4) PENDING-LIVE-SMOKE. **§5 + §6** are JS / yaml: VERIFIED-AT-EDIT (no deployment needed for §6; §5 needed in-rotation regression). **Smoke plan** (per fix, post Noah's deploy): set_actor_property StaticMeshActor Mobility/Visibility/Intensity → set_on field present; take_screenshot Saved/test.png → file exists; PIE-on add_widget_to_viewport → added_to_viewport:true; bind_widget_event → node_id returned; get_asset_preview_render → mime:"image/jpeg" + base64 starts with /9j/. **UE 5.6 institutional memory additions** (extends D78/D81/D87/D88/D93/D96): `FImageUtils::CompressImage(TArray64<uint8>&, TCHAR*, FImageView, int32)` is the canonical 5.6 replacement for the deprecated CompressImageArray; `FImageView(FColor*, W, H)` infers BGRA8/sRGB; `FKismetEditorUtilities::CreateNewBoundEventForClass` returns nullptr silently when 4th arg `FObjectProperty* ComponentProperty` is null — must resolve via `FindFProperty<FObjectProperty>(GeneratedClass, WidgetName)` after compile; `UK2Node_ComponentBoundEvent` identifies bindings by `DelegatePropertyName + ComponentPropertyName`, not `CustomFunctionName/EventReference`; `UEditorAssetLibrary::LoadAsset` can fail in PIE for asset paths that resolve cleanly editor-only — prefer `LoadObject<T>` with the doubled `/Pkg/Path/<name>.<name>` form; `FObjectThumbnail::CompressImageData()` is a JPEG encoder despite naming (JPEG SOI = FFD8 FFE0); `AActor::GetComponents()` enumerates all SCS-tree components incl. grandchildren — sufficient for property traversal without manual recursion. **TCP:55557 retirement gate**: closes after Noah deploys + re-smoke confirms §1-§4 live (per D96 worker recommendation). **Wave 4 dispatch verdict**: M5-PREP next per D101 recommendation; CLEANUP-M3-FIXES-2 NOT QUEUED (worker covered broader patterns); TEST-IMPORTS-FIX queued as tiny ~0.25-0.5 session worker; WIDGETS-PERF + FA-ε write-side audit + TCP:55557 client deletion stay queued. |
| D103 | M5-PREP shipped 2026-04-26 (commit `a5b565f`). **All 19 stubs landed matching D101 verifier audit exactly**: animation 4 (3 montage mutations + get_audio_asset_info), materials 3 (2 creates + set_material_parameter), input-and-pie 3 (Enhanced Input creates only — PIE shipped under M-enhance), geometry 3 (procedural mesh — gating note in handler), editor-utility 6 (incl. run_python_command + delete_asset_safe). 5 plugin C++ handler-file pairs + 5 helper `Register*Handlers()` calls in MCPCommandRegistry.cpp + 5 stub `m5-<toolset>-tools.mjs` files + server.mjs imports/init. **Empty-loop scaffold pattern (worker design call)**: single shared registration loop in server.mjs keyed off `m5ToolsetGroups[]` array. Sub-workers ship by populating ONE `m5-*-tools.mjs` file's `M5_*_SCHEMAS = {}` — they NEVER touch server.mjs or each other's files. **Lambda factory pattern**: each `Register*Handlers` function uses inline `NotImplemented(TEXT(...))` lambda factory; sub-workers replace stubs by literal grep on `NotImplemented(TEXT(` — every site identifiable in seconds. Both patterns are the structural fix for the M3 collision pattern (D96) — file-disjointness genuinely achieved, not just claimed. | Source: M5-PREP worker final report 2026-04-26, commit `a5b565f`. **Test rotation**: test-mock-seam 45/45, test-mcp-wire 64/64 (full server.mjs import + startup path validates 5 imports + 5 init calls + new for-loop — zero MCP tools register from empty SCHEMAS, exactly the desired baseline), test-rc-wire 119/119. test-tcp-tools.mjs failed but pre-existing per D102 drift (TEST-IMPORTS-FIX queued). **Three worker-flagged findings**: (1) **`get_audio_asset_info` stubbed per handoff §1**; M5-animation+materials sub-worker still owns the SUPERSEDED-vs-implement decision per D101 (v) — read disposition from yaml + offline-tools to confirm before filling in. (2) **`.git/info/forbidden-tokens` absent from worker's checkout** despite CLAUDE.md describing one-time setup. Verified in orchestrator's checkout: file IS present (337 bytes, 11 lines, hooks path correctly set to `.githooks`). Worker session likely had degraded gate state (fresh clone or git worktree without one-time-setup propagated). **Risk**: any worker session in degraded gate state silently bypasses the pre-push codename check. **Mitigation queued**: small follow-on improvement to `.githooks/pre-commit` + `.githooks/pre-push` — print a loud WARNING (not block, to avoid breaking fresh-contributor onboarding) when `.git/info/forbidden-tokens` is absent. Bundle into next CLAUDE.md/.githooks grooming pass OR ship as a tiny standalone fix. (3) **One UNREAL_PROJECT_ROOT bash command containing codename leaked into shell history before the hook intercepted the second attempt** — non-fatal (committed content scrubbed clean) but a process gap. Codename grep in shell history is a separate exposure vector from committed content; worth noting but lower-priority than push-gate gap. **M5 sub-worker dispatch readiness CLEAR** — 3 sub-workers can dispatch in parallel after Noah's deploy cycle (sync-plugin.bat + Build.bat + editor relaunch + MCP server restart). Per the m5ToolsetGroups[] + NotImplemented lambda design, sub-workers will hit ZERO file-collisions even with full-parallel dispatch — D96's expensive lesson empirically vindicated by the design. |
| D104 | TEST-IMPORTS-FIX shipped 2026-04-26 (commit `5028c47`). **D102 drift closed — 197 assertions restored to rotation** (pre/post: silent-0 broken-on-import → 197 passed / 0 failed; net rotation gain +197). Group 21 wire-map test kept-with-symbol-rename: `initBlueprintsWriteTools` resets the wire map identically to old barrel for the blueprints-write slice; empty-toolsets test still validates identity-fallback; full-init test restores `fakeToolsYaml`. No fixture migration needed — per-toolset init semantics turned out to be wire-compatible with old barrel for this single-toolset file's purposes. CLAUDE.md lines 140 + 412 adjusted (description for post-M3 split, count `234 → 197`). Aggregate "1376" rotation claim left alone — drift is 2.7%, under handoff §4 5% threshold. **Bonus fixes landed in same commit**: (1) Group 16 port assertion was stale (`55557 → 55558` needed post-M3 layer flip); (2) header comment line 12 lied about the file's intent (corrected). | Source: TEST-IMPORTS-FIX worker final report 2026-04-26, commit `5028c47`. **Worker insight worth promoting to institutional memory**: per-toolset init pattern (D93/D96/D97 trio) made Group 21's empty-init test MORE meaningful, not less — it now uniquely guards `initBlueprintsWriteTools`'s map-reset behavior in a single file, whereas the old barrel covered all 3 toolsets at once. Per-toolset granularity = sharper test guards. **CRITICAL META-FINDING worth queuing as separate fix**: **silent-zero test failures are the worst kind of regression** — 234 assertions vanished from the rotation for ~one full milestone (D97→D102, 5 days) because import errors in one test file don't break OTHER test files' runs; the rotation tooling treats the broken file as `0/0` rather than failing loudly. Recommendation: add a **rotation-runner harness improvement** that detects import errors → fails loud (non-zero exit + clear error message naming which file errored). Could be a tiny standalone improvement OR fold into next test-rotation infrastructure pass. Without this, future barrel-deletion-style refactors risk repeating the same silent-drift class. **Pattern-aware testing for future M5 sub-workers**: post-M5-sub-worker-ships, run the full rotation END-TO-END (not per-toolset) + verify aggregate assertion count grew by the expected delta. If aggregate is FLAT or DROPPED despite new test files added, suspect import-error silent-zero. **No new findings beyond the bonus fixes** — worker confirmed no other test files appear silent-broken (presumably grepped imports across rotation). **Test rotation actual current count**: 1376 (orchestrator's stale baseline) was off by ~+187 from M3 sub-worker test-file additions but ALSO off by -234 from the broken file = net ~-47 vs the claimed 1376. Post-D104 actual: ~1500-1600 across 13+ files. CLAUDE.md will need next-grooming-pass refresh (queue alongside `.git/info/forbidden-tokens` warn-on-missing fix per D103). |
| D105 | M5-animation+materials shipped 2026-04-26 (commit `24cb115`). **6 of 7 candidate tools shipped** (animation 3 + materials 3): `create_montage`, `add_montage_section`, `add_montage_notify` (plugin C++ TCP:55558); `create_material`, `create_material_instance` (plugin C++ TCP:55558); `set_material_parameter` (RC HTTP delegate per D101 (ii)). **`get_audio_asset_info` SUPERSEDED-as-offline** per D101 (v) — yaml `displaced_by: read_asset_properties` is load-bearing. D50 tagged-fallback covers SoundCue/SoundWave CDO metadata; AkAudioEvent (Wwise) requires SDK access neither offline NOR reflection_walk would extend. Stub dropped from `RegisterAnimationHandlers` + `M5_ANIMATION_SCHEMAS`; yaml entry preserved as discovery breadcrumb. **`set_material_parameter` RC route**: `PUT /remote/object/call` with `objectPath = <MIC asset path>`, `functionName = SetScalar/Vector/TextureParameterValueEditorOnly`, `parameters = {ParameterInfo: {Name, Association: 2, Index: -1}, Value}`. Auto-detects `parameter_type` from value shape (number→scalar, array/object→vector, string→texture) with explicit override. Test rotation: **+93 net assertions** (target 30-50, exceeded). test-rc-wire 119→141 (+22); test-m5-animation NEW 40/40; test-m5-materials NEW 31/31. D104 silent-zero check satisfied. | Source: M5-animation+materials worker final report 2026-04-26, commit `24cb115`. **All 3 animation handlers call `RefreshCacheData() + PostEditChange()`** per D102 idiom so editor view reflects mutations (PostEditChange alone leaves track view stale — silent live-fire failure mode wire-mocks can't catch). **Materials handlers**: `create_material` via `UMaterialFactoryNew` with `EMaterialDomain/EBlendMode` parsing (loud `UNKNOWN_DOMAIN/UNKNOWN_BLEND_MODE` on bad input); `create_material_instance` via `UMaterialInstanceConstantFactoryNew` with parent `UMaterialInterface` lookup. Both factories live in UnrealEd (already private dep); no Build.cs additions for materials. **5 UE 5.6 institutional-memory additions** (extends D78/D81/D87/D88/D93/D96/D102): (1) `UAnimMontage::RefreshCacheData()` REQUIRED after Notifies/CompositeSections mutation; PostEditChange() alone leaves editor track view stale. (2) `EMaterialParameterAssociation::GlobalParameter = 2` NOT 0 — LayerParameter occupies 0; common foot-gun for RC ParameterInfo struct construction. (3) `FAnimNotifyEvent::Link(UAnimSequenceBase*, float, EAnimLinkMethod::Type)` — pass `EAnimLinkMethod::Absolute` explicitly; 2-arg form's relative-link semantics surprise programmatic callers. (4) `UMaterialFactoryNew + UMaterialInstanceConstantFactoryNew` both live in UnrealEd (already private dep). (5) `UAnimNotify / UAnimNotifyState` instances created via `NewObject<T>(Outer=Montage, Class=ResolvedClass)` — fail loudly if `CLASS_Abstract` flag set (added explicit guard). **D87 status**: VERIFIED-BY-TEST (40+31+22=93 assertions); PENDING-DEPLOYMENT (Noah owns sync-plugin.bat → Build.bat → editor relaunch); PENDING-LIVE-SMOKE (6-call smoke plan in worker report). **Smoke plan**: create_montage from existing AnimSequence → BP-editor verification; add_montage_section 'Hit' at t=0.5; add_montage_notify AnimNotify_PlaySound at t=0.25; create_material M_Test (Surface, Opaque); create_material_instance MIC_Test from M_Test; set_material_parameter scalar (Roughness=0.4) + vector (Tint=[0.5,0.5,0.5,1]) + texture (Diffuse=/Game/Textures/T_X.T_X) → editor MIC override panel. **First M5 sub-worker to ship**; observed concurrent activity from M5-input+geometry + M5-editor-utility but landed cleanly per M5-PREP empty-loop scaffold design (D103). |
| D106 | M5-input+geometry shipped 2026-04-26 (commit `112b749`, 8 files +1608/-79). **All 6 tools shipped on TCP:55558**: `create_input_action`, `create_mapping_context`, `add_mapping`, `create_procedural_mesh`, `mesh_boolean`, `generate_uvs`. **§0 Pre-flight PASSED**: GeometryScripting enabled in Project A's `<ProjectName>.uproject` Plugins block (per D101 (iii) Noah-action gating); EnhancedInput is engine-built-in (no .uproject change needed). Test rotation: **18 files, 1819 assertions, zero regressions, +109 net** (test-m5-input-pie NEW 48; test-m5-geometry NEW 61). | Source: M5-input+geometry worker final report 2026-04-26, commit `112b749`. **Build.cs additions**: `EnhancedInput` (engine module) + `GeometryScriptingCore + GeometryFramework + GeometryCore` (under GeometryScripting plugin → also added to UEMCP.uplugin Plugins[]). **4 UE 5.6 institutional-memory additions** (extends D78/D81/D87/D88/D93/D96/D102/D105): (1) `UGeometryScriptLibrary_MeshPrimitiveFunctions::AppendBox/AppendSphereLatLong/AppendCylinder/AppendCone` take `EGeometryScriptPrimitiveOriginMode::Center` for centered output. (2) `ApplyMeshBoolean` returns void; debug surfaced via out-param `FGeometryScriptDebug`. (3) `UDynamicMeshComponent::NotifyMeshUpdated()` REQUIRED after edits to refresh viewport. (4) `UInputMappingContext::MapKey()` returns `FEnhancedActionKeyMapping&` but assignment unnecessary for simple bindings. **CRITICAL CONFIRMATION — M5-PREP scaffold parallel-safety promise empirically vindicated**: M5-animation+materials worker (24cb115) and M5-input+geometry (112b749) committed back-to-back without ANY merge conflicts — disjoint per-handler files with shared Build.cs / .uplugin edits riding along in whichever commit lands first. The cost was minor commit-history confusion (M5-input+geometry's commit notes editor-util's deps "ride along") versus the **3-stash-cycle thrash that M3-widgets+M3-bpw incurred before the scaffold pattern existed (D96)**. D96's expensive lesson + D101 verifier's shard-upfront recommendation + D103 M5-PREP's empty-loop scaffold = end-to-end structural fix verified across 3 parallel sub-workers. **D87 status**: VERIFIED-BY-TEST (109 assertions); PENDING-DEPLOYMENT; PENDING-LIVE-SMOKE (6-call smoke plan in worker report). **Smoke plan**: create_input_action(Axis2D, "IA_Move") → asset registry; create_mapping_context("IMC_Default") → asset registry; add_mapping(IMC, IA_Move, "W") → mapping_count=1; create_procedural_mesh("sphere") → ADynamicMeshActor in level; spawn second box → mesh_boolean(target=Sphere, tool=Box, "difference") → mesh updated; generate_uvs(target=result) → UV channel 0 populated. **No PIE / world-state caveats** because Enhanced Input + procedural mesh are editor-asset operations (don't depend on PIE world like the widgets handlers did). |
| D107 | M5-editor-utility shipped 2026-04-26 (commit `f36e4e1`). **All 6 tools shipped on TCP:55558**: `run_python_command`, `get_editor_utility_blueprint`, `run_editor_utility`, `duplicate_asset`, `rename_asset`, `delete_asset_safe`. Closes editor-utility scope from D101 verifier audit (1/7 → 7/7 shipped). Highest-security M5 sub-worker — Python execution + asset deletion both warrant defense-in-depth per D101 (iv). **Security posture (4-layer defense per D101 (iv))**: **Layer 1 (server-side, m5-editor-utility-tools.mjs)**: `--enable-python-exec` argv flag OR `UEMCP_ENABLE_PYTHON_EXEC=1` env var gates `run_python_command` pre-flight; off by default returns `PYTHON_EXEC_DISABLED` with NO wire dispatch (no plugin-side audit footprint — flag denials are server policy, not editor events). **Layer 0 (plugin C++)**: `IPythonScriptPlugin::Get()->IsPythonAvailable()` runtime check; absent plugin returns `PYTHON_PLUGIN_NOT_AVAILABLE`. **Layer 2 (plugin C++)**: D14 deny-list (`os`, `subprocess`, `eval(`, `exec(`, `open(`, `__import__`) — simple `FString::Contains`, fail-closed (false positives tolerated). **Layer 3 (plugin C++)**: per-call `LogUEMCPSecurity` audit log with `[UEMCP-PYTHON-EXEC]` / `[UEMCP-DELETE-ASSET]` prefixes — SHA1 fingerprint + first-100-chars preview to `<ProjectName>.log`. **`delete_asset_safe` 5-row decision matrix**: soft-delete default to `/Game/_Deleted/<name>_<hash>` with reference fixup; hard delete requires `permanent:true + force:true` sentinel pair; row-5 forbids `permanent:true` alone with `BAD_PARAMS`. Dependency-check via `IAssetRegistry::GetReferencers()` before delete; emits `ASSET_HAS_DEPENDENCIES` with referencers detail unless `force:true`. Test rotation: **14 files / 1571 / 0** (94 new assertions in test-m5-editor-utility; was 1477 baseline pre-add per worker's snapshot — taken before M5-input+geometry's 109-assertion landing was visible in their checkout). | Source: M5-editor-utility worker final report 2026-04-26, commit `f36e4e1` (7 files, +1475/-80; Build.cs + UEMCP.uplugin co-author ride-along in `112b749` already merged). **`delete_asset_safe` defaults**: `move_to_trash:true, permanent:false, force:false` → soft-delete to `/Game/_Deleted/<name>_<sha1-8>` with reference fixup. **Row-5 (permanent:true w/o force) explicitly returns BAD_PARAMS** per advisor's tighter-default decision. **94 test assertions cover** (silent-zero check satisfied per D104 — exactly +94 net = new file's count): 6 deny-list patterns × 2 assertions; PYTHON_EXEC_DISABLED short-circuit (no wire dispatch confirmed); gate-on success path; schema-validation-precedence; full 5-row delete decision matrix. **CLAUDE.md `### Security flag — --enable-python-exec` subsection** under "Common Tasks" documents both invocation forms + `.mcp.json` env-block recipe. tools.yaml editor-utility section updated with security signal in toolset description + per-tool descriptions; server.mjs TOOLSET_TIPS rewritten with 4-layer model + cross-toolset workflow tip referencing `asset-registry.get_asset_references` for pre-delete impact analysis. **Smoke plan at `docs/handoffs/m5-editor-utility-smoke-plan.md`** (gitignored; NEW file) — per-handler steps + explicit Layer-0/1/2 security-path validation incl. log-line presence checks; cleanup procedure for `/Game/_Smoke/` + `/Game/_Deleted/` test residue. **6 UE 5.6 institutional-memory additions** (extends D78/D81/D87/D88/D93/D96/D102/D105/D106): (1) `IPythonScriptPlugin::Get()` returns nullptr when PythonScriptPlugin module isn't loaded; `->IsPythonAvailable()` further checks runtime initialization. Both required before `ExecPythonCommandEx`. (2) `FPythonCommandEx` carries `Command + ExecutionMode (ExecuteStatement` for multi-line; `ExecuteFile` for file paths) + populates `CommandResult + LogOutput[]` after exec; `EPythonLogOutputType::{Info, Error}` for log entry classification. (3) `UEditorAssetLibrary::RenameAsset(src, dst)` does reference-fixup automatically; serves cleanly as a "move to trash" implementation when dst is `/Game/_Deleted/<name>`. (4) Blutility module headers (`EditorUtilityBlueprint.h` / `EditorUtilityWidgetBlueprint.h`) live behind a plugin dep that must be declared in BOTH `.Build.cs` AND `.uplugin Plugins[]` (D60 rule). (5) PythonScriptPlugin module can be declared `"Optional": true` in `.uplugin Plugins[]` to silence the UBT module-without-plugin-dep warning while leaving the plugin opt-in at the project level — perfect match for our security model. (6) `DEFINE_LOG_CATEGORY_STATIC(LogUEMCPSecurity, Log, All)` at file scope creates an audit-trail channel that lands in `<ProjectName>.log` at Warning verbosity without engine.ini overrides — `grep [UEMCP-PYTHON-EXEC]` extracts the audit stream. **4 worker-flagged open items**: (a) **Deny-list false-positive class**: `import os` inside a Python triple-quoted-string literal trips the gate. Intentional fail-closed posture; tighter regex would inevitably miss something. If painful, right next step is server-side `--allow-deny-list-pattern <pattern>` per-pattern allow-flag, NOT regex tightening. (b) `run_editor_utility` only supports zero-arg Run; EUBs that take parameters return `RUN_FUNC_HAS_PARAMS`. Parameterized invocation deferred — caller can drop into `run_python_command` for arg passing (still passes through security gates). (c) Soft-delete trash collisions on long names: `/Game/_Deleted/<basename>_<sha1-8>` collides only on same-path repeat soft-deletes within SHA1 8-byte preimage space (essentially never), but basename truncation to UE's MAX_PATH applies — bulk-delete cleanup may be needed at 100k+ scale. (d) Layer 0 `PYTHON_PLUGIN_NOT_AVAILABLE` not unit-testable without editor (mock plugin missing). Smoke plan §6.d covers via project-config side trip; left to live-fire. **CRITICAL CROSS-WORKER CONFIRMATION**: M5-editor-utility worker EMPIRICALLY REPRODUCED D103 §2 finding — `.git/info/forbidden-tokens` absent from this worker's checkout too (manually grepped against codename, contributor email, and `D:\UnrealProjects` path; all clean). **Two workers in two days** observed degraded gate state. **Recommendation now promoted from "queue alongside next grooming pass" to "SHIP SOON"** — the queued `.githooks pre-commit/pre-push missing-file warning` (orchestrator-state row) is a real fresh-clone risk, not a hypothetical. **Last M5 sub-worker to ship**; D101 verifier's PARTIALLY-REMAINING verdict now empirically closed — 19/19 not-shipped tools shipped across the 3 M5 sub-workers per the verifier's enumeration. |
| D108 | **M5 milestone complete 2026-04-26** — all 19 not-shipped M5 tools landed on TCP:55558 (or RC HTTP for `set_material_parameter`) across the 3 parallel sub-workers (D105 animation+materials + D106 input+geometry + D107 editor-utility). **D101 verifier's PARTIALLY-REMAINING verdict empirically closed**; M5 actual sessions: ~3 (one per sub-worker, all parallel) + 0.5-1 M5-PREP = ~3.5-4 sessions vs orchestrator-state's 6-10 estimate (D101 already corrected to ~4-5.5; actual landed at the lower bound). **Phase 3 dispatch surface**: Wave 1 (M1 + M-spatial + Oracle-A/v2) ✅ + Wave 2 (S-B-base + Verb-surface) ✅ + Wave 3 (M-enhance HYBRID + Phase 4 absorbed) ✅ + Validation cycle (audit + T-1b + smoke + audit-fixes 1/2/3 + SMOKE-FIX + deployment) ✅ + M3 (3 sub-workers — actors + BP-write + widgets) ✅ + M4 (SUPERSEDED per D95) ✅ + **M5 (3 sub-workers — animation+materials + input+geometry + editor-utility) ✅** = ALL primary dispatch milestones complete. **Phase 3 is ~99% complete** (was ~95% pre-M5; M5 was the only remaining primary milestone). | Source: orchestrator pickup of D105 + D106 + D107 worker reports + git log verification (24cb115 + 112b749 + f36e4e1). **CRITICAL ARCHITECTURAL VALIDATION — M5-PREP scaffold (D103) verified end-to-end**: 3 sub-workers committed back-to-back-to-back without ANY merge conflicts. Per D106 worker insight: "the cost was minor commit-history confusion versus the 3-stash-cycle thrash that M3-widgets+M3-bpw incurred before the scaffold pattern existed." D96's expensive lesson + D101 verifier's shard-upfront recommendation + D103 M5-PREP's empty-loop + lambda factory = end-to-end structural fix vindicated. **Aggregate test rotation expected**: ~+296 across the 3 M5 sub-workers (D105 +93 + D106 +109 + D107 +94). Adding to post-D104 baseline (~1500-1600), expected current = ~1800-1900 across 14+ test files. **Authoritative count requires fresh rotation in Noah's checkout** (D104 silent-zero defense-in-depth check) — orchestrator estimate is rough; flag for next CLAUDE.md grooming pass. **Aggregate UE 5.6 institutional-memory additions across M5**: 9+ items (5 from D105 + 4 from D106 + multiple from D107's security spec). Total UEMCP UE 5.6 institutional-memory across D78/D81/D87/D88/D93/D96/D102/D105/D106/D107 is now ~30+ documented findings — a substantial UE 5.6 plugin-development knowledge base. **Phase 3 remaining items (all post-primary-dispatch)**: (a) Noah's deployment cycle for M5 commits + bundled smoke (with streamlined gauntlet at tail per D100); (b) live-fire verification of all 19 M5 tools + 6 CLEANUP-M3-FIXES patches; (c) TCP:55557 formal retirement (gated on smoke-confirms-all-32-M3-tools live + 19-M5-tools live); (d) Optional follow-ons: WIDGETS-PERF investigation (D99 #5), FA-ε write-side audit (D99 #6 + D100 BP-CDO-PIE-unload), audit-batch-2 (22 D79 findings), M-enhance reads conformance audit (M4 redirect option 2 per D95), .git/info/forbidden-tokens warn-on-missing fix (D103 #2), rotation-runner harness fail-loud (D104 meta-finding mitigation), CLAUDE.md/README grooming pass (aggregate rotation count refresh + D-log header D1-D108 + etc.); (e) Phase 5 distribution + Phase 6 per-project tuning (long-term scope). **Wave 4 dispatch surface is now ENTIRELY OPTIONAL FOLLOW-ONS** — no remaining primary milestones. The Phase 3 scope-refresh sequence (D58 §Q5) is empirically closed. |
| D109 | **BLUEPRINT-ASSET-PATH-RESOLUTION bug surfaced 2026-04-28** by a session running M3 + M5 against a third UEMCP-deployed test target with a non-`/Game/Blueprints/` content layout. Asset under test: `/Game/<custom-content-tree>/.../BP_*`; load-able by every offline handler (`inspect_blueprint`, `bp_list_graphs`, `bp_find_in_graph`, etc.) but NOT by `spawn_blueprint_actor` or any `blueprints-write` toolset handler. **Root cause**: `ActorHandlers.cpp:630` hardcodes `FString::Printf(TEXT("/Game/Blueprints/%s"), *BlueprintName)` and uses `FPackageName::DoesPackageExist` (filesystem probe) instead of consulting `FAssetRegistryModule`. **Wider blast radius via orchestrator-grep verification**: same assumption baked into `BlueprintHandlers.cpp:46` `LegacyPath()` helper (which ALL 15 `blueprints-write` tools route through) + `BlueprintHandlers.cpp:431` (role TBD; possibly list-creation root) + `WidgetHandlers.cpp:734-736` (embedded-BP reference lookup) + toolset tip text in `MCPCommandRegistry.cpp` documenting the `/Game/Blueprints/`-only convention to users. **NOT affected**: offline toolset (full-path reads), remote-control toolset (full UObject paths), `actors.spawn_actor` (primitive class spawning), already-spawned-actor handlers, static fixture files. **Severity**: BLOCKING for projects whose Blueprint content tree doesn't sit under `/Game/Blueprints/` — every project that doesn't follow Epic-template default loses ~16+ tools (1 actors + 15 blueprints-write + 1 widgets handler) until fixed. | Source: third-test-target repro session 2026-04-28 (handoff at `docs/handoffs/blueprint-asset-path-resolution-fix.md`, gitignored). **Fix scope**: centralize lookup into `ResolveBlueprintAssetPath()` helper with 3-case chain — (1) fully-qualified `/Game/...` path passthrough; (2) legacy `/Game/Blueprints/<Name>` back-compat for Epic-template-derived projects; (3) AssetRegistry project-wide fallback by ObjectName with explicit ambiguity-error-listing-all-matches on multi-hit. Centralization = ~5 edit sites in 3 plugin C++ files (cascades to 17 affected tools). Tip text + tools.yaml description updates round out the surface. **Discovery context provides empirical-validation framework**: the third-test-target session is essentially a structural pen-test for layout assumptions baked into M3 sub-worker handler implementations. The reporting session correctly identified the spawn_blueprint_actor case but didn't trace the centralized helper — orchestrator's grep surfaced 4-5 additional sites. **Pattern lesson for future M-* primary-dispatch sub-workers**: when implementing handlers that resolve user-supplied asset names, prefer `IAssetRegistry::GetAssetsByClass + ObjectName-match fallback` over `FPackageName::DoesPackageExist` of a printf'd path. Add to UE 5.6 institutional-memory list for future M-anything-touching-asset-paths workers. **Worker recommendation**: dispatch as standalone fix worker; ~1-2 sessions; D49-clean (5 plugin C++ files + tools.yaml + 3 test files). Closes the layout-assumption Phase-3-followup gap; does NOT block the Post-M5 deployment smoke (smoke can run against Project A's standard layout in parallel). **Third-test-target codename hygiene**: a 3rd UE project on this machine has UEMCP vendored in (alongside the Project-A primary target + Project-B secondary). Codename not currently on `.git/info/forbidden-tokens` block-list; defensive practice = use placeholder vocabulary in committed content (this entry uses "third test target"). Recommend Noah add the codename to the block-list per the existing convention. **Post-fix expectations**: 17 tools that were silently broken on non-standard layouts work; existing `/Game/Blueprints/`-layout projects unaffected (Case 2 back-compat); ambiguity case errors explicitly with all matches. |
| D110 | SETUP-BAT-PLUGIN-DEPS shipped 2026-04-27 (commits `d45ca29` main + `43fc722` Blutility-fix + `3b705de` .uplugin cleanup). **Setup script now enables UEMCP's required built-in plugin deps in target .uproject's `Plugins[]` array**: RemoteControl + PythonScriptPlugin + GeometryScripting (NOT Blutility — see below). Idempotent: skips already-enabled plugins; flips `Enabled: false` to `true`; appends missing entries; atomic write via `<file>.uemcp-tmp` + Move-Item. Convention compliance per CLAUDE.md §.bat: EXIT_CODE 5 added (plugin-deps update failed); single :end exit point; AUTO_YES sentinel honored; endlocal idiom intact. **§6 `.githooks` warn-on-missing BUNDLED** — pre-commit + pre-push both got +7-line additions printing loud WARNING when `.git/info/forbidden-tokens` absent. **Closes D103 #2 + D107 cross-worker reproduction** — fresh-clone gate-degradation risk structurally addressed. CLAUDE.md §Onboarding paragraph documents the new behavior + EXIT_CODE 5; `### Security flag — --enable-python-exec` subsection notes that PythonScriptPlugin enablement makes `run_python_command` REACHABLE but the tool itself is gated by `--enable-python-exec` flag per D107. | Source: SETUP-BAT-PLUGIN-DEPS worker final report 2026-04-27. **CRITICAL UE 5.6 INSTITUTIONAL MEMORY (extends D78/D81/D87/D88/D93/D96/D102/D105/D106/D107)**: **Blutility is NOT a plugin — it's an engine MODULE**. Lives in `Engine/Source/Editor/Blutility/` (provides `UEditorUtilityBlueprint` / `UEditorUtilityWidgetBlueprint` headers). Adding `Blutility` to `.uproject Plugins[]` OR `.uplugin Plugins[]` triggers UBT "Missing Plugin" dialog at editor startup (even though build itself succeeds). Module dep is satisfied by `Blutility` in `UEMCP.Build.cs PrivateDependencyModuleNames` ONLY. Setup script (initially placed Blutility in `.uproject Plugins[]`) was incorrect; `43fc722` fix-commit corrects + adds self-healing logic that REMOVES any stale `Blutility` entry from `.uproject Plugins[]` on every run. `3b705de` separately removes the spurious Blutility entry from `UEMCP.uplugin` (M5-PREP era oversight). **Pattern lesson**: when adding UE editor-feature deps, distinguish "engine module" (Build.cs only) vs "engine-shipped plugin at `Engine/Plugins/*/`" (.uproject + .uplugin) vs "third-party plugin" (.uproject + .uplugin + project Plugins/ tree). Future M-* sub-workers + setup-script extensions must check the actual filesystem location before adding entries. **D106/D107 confirm the empirical contract**: `.uproject Plugins[]` gating IS load-bearing for full toolset coverage — transitive auto-enable from the parent plugin's `.uplugin Plugins[]` declaration alone is NOT load-bearing for these (RemoteControl + PythonScriptPlugin + GeometryScripting all need explicit `.uproject` enablement). Setup script's defense-in-depth ENABLE-them-explicitly approach validated empirically. **Test rotation unchanged** — script-only change. **Post-fix expectations confirmed**: fresh-clone contributors running `setup-uemcp.bat` get the dependent plugins enabled (was the manual gap pre-D110). 3rd contributor onboarding scenario closes structurally. |
| D111 | M5 deployment compile-fix wave 2026-04-27 (3 commits: `59d7c63` + `6059d4c` + `f42c04e`). **5 build errors blocking M5 deployment cycle resolved**. (a) **`59d7c63`**: 5 build errors across `EditorUtilityHandlers.cpp` + `GeometryHandlers.cpp` + `WidgetHandlers.cpp` + `UEMCP.Build.cs` + `UEMCP.uplugin`. (b) **`6059d4c`**: `LogObj` → `JsonEntry` rename in `EditorUtilityHandlers.cpp` — UE LogCategory naming collision (UE has a `LogObj` symbol that conflicts with the M5-editor-utility worker's audit-log local variable). (c) **`f42c04e`**: `UEMCP.Build.cs` link-time fixes — added `Projects` + `InputCore` modules. | Source: Noah's deployment-cycle attempts 2026-04-27 surfacing build-time errors that wire-mock tests couldn't catch. **Classic D87 deployment-gap pattern continuing**: wire-mock-green M5 sub-worker code surfaces build-time errors only when Noah runs Build.bat against real UE 5.6 module graph. None of these would have been caught by automation alone. **3 NEW UE 5.6 institutional-memory items** (extends D78/D81/D87/D88/D93/D96/D102/D105/D106/D107/D110): (1) **`LogObj` is a UE-engine symbol** (likely log-category macro name); local variables in handler audit-log code must NOT shadow it. Worker convention to add: avoid 3-letter `Log*` local-variable names in audit-log code; use descriptive names like `JsonEntry` or `AuditPayload`. (2) **`Projects` module** is required for any plugin C++ that touches `IPluginManager` / `FPaths::ProjectDir` / similar runtime project-path APIs. M5-editor-utility's `delete_asset_safe` reference-fixup likely triggered this. (3) **`InputCore` module** is required for any plugin C++ that references `FKey` / `EKeys::*` / Enhanced Input key constants in handler signatures. M5-input+geometry's `add_mapping` triggered this. **Pattern lesson**: when M-* sub-worker handoffs draft, include "Build.cs deps to add" enumeration based on what the handler actually uses; sub-workers can't always predict link-time deps from compile-time success. Add to handoff template: "after first Build.bat attempt, capture link errors → add module deps; iterate until clean." **Test rotation unchanged** — pure plugin C++ build fixes; no JS-side delta. **Wire-mock vs build-time gap**: this batch is the empirical proof that wire-mock-green ≠ build-clean — wire-mock tests can validate JS schema + handler dispatch + response shape, but cannot validate UE link/compile against real module graph. Smoke-as-final-gate is load-bearing precisely because of this gap. **D87 framing reinforced**: "code committed + tests green ≠ fix is live" extended to "+ build clean against real UE." Future M-* sub-workers must include build-on-real-UE-target as a final pre-deploy verification before final-report. **Post-fix expectations**: M5 sub-workers' code now compiles cleanly; deployment cycle (sync + Build + relaunch + Claude restart) can complete; Post-M5 deployment smoke is now actually-runnable (no longer blocked on compile). **Smoke gate update**: the Post-M5 smoke (handoff `docs/handoffs/post-m5-deployment-smoke.md`, opener prior turn) can dispatch as soon as Noah completes the deployment cycle that drove these compile fixes. |
